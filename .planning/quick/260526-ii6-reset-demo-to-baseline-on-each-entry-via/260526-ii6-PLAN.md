---
phase: quick-260526-ii6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/demo/seed.ts
  - app/(auth)/login/actions.ts
  - app/api/demo/reset/route.ts
  - components/layout/demo-reset-beacon.tsx
  - app/app/layout.tsx
autonomous: true
requirements: [DEMO-RESET]

must_haves:
  truths:
    - "Entering the public demo wipes the demo user's app data and re-seeds the pristine baseline before sign-in"
    - "The reseed only ever touches DEMO_USER_ID rows; an unset/empty DEMO_USER_ID is a no-op (no wipe)"
    - "Closing or hiding the demo tab fires a best-effort reset beacon to /api/demo/reset"
    - "Reseed failures never block demo entry, the reset route, or the redirect"
    - "tsc strict is clean and the existing vitest suite (351) stays green"
  artifacts:
    - path: "lib/demo/seed.ts"
      provides: "Server-only reseedDemo() that ports /tmp/oz_seed.mjs onto serviceDb"
      exports: ["reseedDemo"]
      contains: "serviceDb.transaction"
      min_lines: 250
    - path: "app/api/demo/reset/route.ts"
      provides: "POST handler that reseeds when the caller is the demo user, always 204"
      exports: ["POST"]
    - path: "components/layout/demo-reset-beacon.tsx"
      provides: "Client component firing navigator.sendBeacon on pagehide/visibilitychange(hidden)"
      exports: ["DemoResetBeacon"]
  key_links:
    - from: "app/(auth)/login/actions.ts"
      to: "lib/demo/seed.ts"
      via: "await reseedDemo() before signInWithPassword in enterDemo"
      pattern: "reseedDemo"
    - from: "app/api/demo/reset/route.ts"
      to: "lib/demo/seed.ts"
      via: "await reseedDemo() guarded by isDemoUser"
      pattern: "reseedDemo"
    - from: "components/layout/demo-reset-beacon.tsx"
      to: "/api/demo/reset"
      via: "navigator.sendBeacon"
      pattern: "sendBeacon\\(.*api/demo/reset"
    - from: "app/app/layout.tsx"
      to: "components/layout/demo-reset-beacon.tsx"
      via: "rendered under the existing isDemo condition"
      pattern: "DemoResetBeacon"
---

<objective>
Make the shared public demo reset to a pristine baseline on every entry (and best-effort on tab close) by porting the proven raw-postgres seed (`/tmp/oz_seed.mjs`) into a committed, server-only `lib/demo/seed.ts` that runs on the app's `serviceDb` (Drizzle, service-role, RLS-bypass), then invoking it from `enterDemo()` and a new reset route, with a client beacon that fires the reset on tab close.

Purpose: The public demo is shared. Without a reset, one visitor's edits (approvals resolved, workflows toggled, threads added) pollute the experience for the next. Resetting on entry guarantees every visitor sees the same curated "Wanderbound" baseline.

Output: `lib/demo/seed.ts` (new), updated `enterDemo()`, `app/api/demo/reset/route.ts` (new), `components/layout/demo-reset-beacon.tsx` (new), and a one-line wire-in to `app/app/layout.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# THE DATA SOURCE — port this faithfully. Read it in full before writing seed.ts.
@/tmp/oz_seed.mjs

# Service-role Drizzle client (RLS-bypass) — reseedDemo uses serviceDb from here.
@lib/db/client.ts

# Schema re-exports (table object names) — import tables from "@/lib/db/schema".
@lib/db/schema/index.ts

# Demo helpers (DO NOT MODIFY) — reseedDemo reads process.env.DEMO_USER_ID directly;
# the reset route uses isDemoUser().
@lib/auth/demo.ts

# Wiring targets.
@app/(auth)/login/actions.ts
@app/app/layout.tsx
@lib/auth/server.ts

<interfaces>
<!-- Exact Drizzle column names per table (camelCase TS keys map to snake_case columns
     via the schema; use the TS keys shown below in .values({...})). Extracted from
     lib/db/schema/*. The /tmp seed used raw snake_case SQL columns — these are the
     identical fields, addressed by their Drizzle key. -->

Import: `import { serviceDb } from "@/lib/db/client";`
Import tables: `import { userProfiles, integrations, brandVoiceProfiles, brandVoiceSamples, autonomyThresholds, memoryItems, userSessions, shopifyProducts, shopifyProductVariants, workflows, workflowVersions, workflowRuns, approvals, activityEntries, threads, messages } from "@/lib/db/schema";`
Import for delete/update predicates: `import { eq } from "drizzle-orm";`
Import: `import { randomUUID } from "node:crypto";`

userProfiles (user_profiles): user_id, display_name, avatar_url, shopify_shop, onboarding_step, onboarding_completed_at, deletion_requested_at, created_at, updated_at
  — PK is user_id. Use .onConflictDoUpdate({ target: userProfiles.user_id, set: {...} }).

integrations: id, user_id, provider, status, access_token_encrypted, refresh_token_encrypted, scopes (text[]), provider_account_id, last_synced_at, last_error, connected_at, expires_at
brandVoiceProfiles (brand_voice_profiles): user_id (PK), profile_markdown, tone_tags (text[]), forbidden_phrases (text[]), example_url, updated_at
brandVoiceSamples (brand_voice_samples): id, user_id, sample_text, embedding, source, created_at  — /tmp seeds NO samples; only DELETE this table in the wipe.
autonomyThresholds (autonomy_thresholds): user_id (PK), default_level, per_action_overrides (jsonb), updated_at
memoryItems (memory_items): id, user_id, category, content, source_type, source_reference_id, confidence (real), soft_deleted_at, created_at, updated_at
userSessions (user_sessions): id, user_id, supabase_session_id, refresh_token_hash, device_label (NOT NULL), raw_ua, ip_geo_label, last_seen_at, created_at, revoked_at
shopifyProducts (shopify_products): user_id, product_gid, title, body_html, vendor, product_type, status, tags (text[]), meta_title, meta_description, shopify_created_at, shopify_updated_at, last_synced_at  — composite PK (user_id, product_gid)
shopifyProductVariants (shopify_product_variants): user_id, variant_gid, product_gid, sku, price (numeric — pass a string like "189.00"), inventory_qty (integer), last_synced_at  — composite PK (user_id, variant_gid)
workflows: id, user_id, name, description, automation_level, status, trigger_type, trigger_config (jsonb), current_version_id, source, source_reference_id, created_at, updated_at
workflowVersions (workflow_versions): id, workflow_id, version_number, definition (jsonb), schema_version, created_at, created_by_thread_id
workflowRuns (workflow_runs): id, user_id, workflow_id, workflow_version_id, workflow_version_snapshot (jsonb), trigger_source, status, current_step_id, inngest_function_id, error_summary, metadata (jsonb), started_at, completed_at
approvals: id, user_id, workflow_run_id, thread_id, message_id, status, action_type, action_summary, stakes, preview (jsonb), reasoning_summary, reasoning_chain (jsonb), downstream_impact, estimated_review_seconds, proposed_action (jsonb), inngest_event_key, snoozed_until, expires_at (NOT NULL), resolved_at, resolved_by_path, reject_reason, created_at
activityEntries (activity_entries): id, user_id, workflow_run_id, workflow_id, thread_id, agent_context, action_type, action_summary, result, result_details, automation_level, before_state (jsonb), after_state (jsonb), reasoning_chain_url, reasoning_chain (jsonb), target_type, target_id, step_id, is_revertable, reverted_at, reverted_by_entry_id, occurred_at
threads: id, user_id, title, agent_context, context_workflow_id, last_message_at, created_at, archived_at
messages: id, thread_id, user_id, role, content, tool_calls (jsonb), tool_call_id, inline_block_type, inline_block_payload (jsonb), status, model_id, token_input, token_output, latency_ms, created_at

Service client contract (lib/db/client.ts):
  export const serviceDb = drizzle(client, { schema });  // bypasses RLS — MUST filter by user_id
  serviceDb.transaction(async (tx) => { ... })            // tx.insert/.delete/.update like serviceDb

Demo helper contract (lib/auth/demo.ts — DO NOT MODIFY):
  export function isDemoUser(userId: string | null | undefined): boolean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the seed into server-only lib/demo/seed.ts (reseedDemo)</name>
  <files>lib/demo/seed.ts</files>
  <action>
Create `lib/demo/seed.ts` exporting `export async function reseedDemo(): Promise<void>`. This is a faithful Drizzle port of `/tmp/oz_seed.mjs` — same data arrays, same names, same copy, same JSON shapes, same relative timestamps, same cross-reference wiring. Read `/tmp/oz_seed.mjs` in full first; it is the single source of truth. Do NOT move, commit, or delete `/tmp/oz_seed.mjs`.

Top of file: a server-only guard comment block mirroring lib/auth/demo.ts ("MUST NOT have 'use client'; reads process.env server-side only"). Import serviceDb from "@/lib/db/client", all the table objects listed in the interfaces block from "@/lib/db/schema", `eq` from "drizzle-orm", and `randomUUID` from "node:crypto".

USER GUARD (replaces the hardcoded USER const in /tmp): read `const USER = process.env.DEMO_USER_ID;`. If `!USER` (unset/empty) → `return;` early WITHOUT touching the database. This is the safety invariant: never wipe an arbitrary user. SHOP stays the literal "wanderbound.myshopify.com"; keep the TOK placeholder ciphertext string.

TIME HELPERS: port the /tmp helpers verbatim, computed at call time (inside reseedDemo, not module scope) so reseeded rows stay "recent" on every reset: `const now = new Date();` then `H(h)`, `D(d)`, `MIN(m)`, `future(d)` exactly as in /tmp. Pass JS `Date` objects directly to Drizzle timestamp columns.

Wrap everything in ONE `await serviceDb.transaction(async (tx) => { ... })`. Inside, in this exact order:

1) WIPE (FK-safe, same order as /tmp lines 34-48), each filtered by user_id via `eq(table.user_id, USER)`:
   delete approvals, activityEntries, messages, threads, workflowRuns; then `tx.update(workflows).set({ current_version_id: null }).where(eq(workflows.user_id, USER))`; then delete workflows (cascades workflow_versions); then delete memoryItems, brandVoiceSamples, brandVoiceProfiles, autonomyThresholds, userSessions, integrations, shopifyProductVariants, shopifyProducts.

2) PROFILE: `tx.insert(userProfiles).values({ user_id: USER, display_name: "User", shopify_shop: SHOP, onboarding_step: 5, onboarding_completed_at: D(16), created_at: D(16), updated_at: now }).onConflictDoUpdate({ target: userProfiles.user_id, set: { display_name: "User", shopify_shop: SHOP, onboarding_step: 5, onboarding_completed_at: D(16), updated_at: now } })`.

3) INTEGRATIONS: two rows (shopify, gmail) — port scopes arrays as JS string arrays (text[]), status "active", access_token_encrypted = TOK, provider_account_id, last_synced_at, connected_at exactly as /tmp lines 62-65.

4) BRAND VOICE: brandVoiceProfiles row with the full `brandMd` template string (port verbatim, including the markdown), tone_tags + forbidden_phrases as JS arrays, example_url, updated_at = D(9).

5) AUTONOMY THRESHOLDS: default_level "L2", per_action_overrides as a plain JS object (the 8-key object from /tmp lines 88-97 — Drizzle serializes jsonb from the object; do NOT wrap in any sql.json equivalent), updated_at = D(4).

6) MEMORY ITEMS: loop the same 6-tuple array (category, content, source_type, confidence, created); insert each with created_at = created and updated_at = created.

7) SESSIONS: loop the same 3-tuple array; supabase_session_id = randomUUID(), device_label, ip_geo_label, last_seen_at, created_at as /tmp.

8) SHOPIFY PRODUCTS + VARIANTS: loop the same 14-product array with the same `pidx` starting at 100000001. For each: build pgid/vgid the same way; insert shopifyProducts (body_html = meta_desc ? `<p>${meta_desc}</p>` : null, tags as JS array, the D(120)/D(random)/MIN(12) timestamps), then shopifyProductVariants (sku "WB-"+pidx, price = (price/100).toFixed(2) passed as a STRING for the numeric column, inventory_qty as number).

9) WORKFLOWS (+VERSIONS): port the `wfDefs` array verbatim. For each: id=randomUUID(), verId=randomUUID(); build `def` as the plain JS object `{ entry_step, steps: [...] }` (jsonb from object). Insert workflows with trigger_config = the plain object (schedule → {cron,tz}, event → {event:name}, manual → {}), current_version_id = verId, created_at = D(16), updated_at = H(updAgo). Insert workflowVersions (version_number 1, definition = def, schema_version 1, created_at = D(16)). Keep the `wf` map name→{id, verId, def}. Then the extra-version-history loop for the two named workflows: insert a v2 version, `tx.update(workflows).set({ current_version_id: v2 }).where(eq(workflows.id, w.id))`, update `w.verId = v2`.

10) WORKFLOW RUNS: port `runDefs`; build the `runs` map name→[runId]; insert workflowRuns with workflow_version_snapshot = w.def, current_step_id = status==="paused_for_approval" ? "approve" : null, started_at = H(sAgo), completed_at = cAgo==null ? null : H(cAgo).

11) APPROVALS: port `apprDefs`; for each, runId = runWf ? runs[runWf]?.[0] ?? null : null; preview and proposed_action as plain JS objects (jsonb), reasoning_chain = null, inngest_event_key = "demo-"+id, expires_at = future(13), created_at = H(cAgo). Keep the `appr` map summary→id (Thread B's approval_card references appr["Set 20% weekend discount on Explorer Series (6 SKUs)"]).

12) ACTIVITY ENTRIES: port the `acts` array verbatim; wfId = wfName ? wf[wfName]?.id ?? null : null; before_state/after_state as plain JS objects or null; insert with occurred_at = H(oAgo).

13) THREADS + MESSAGES: replicate the /tmp `mkThread` and `msg` helpers as local async closures over `tx`. Maintain the `mseq` counter and the assistant-only model_id/token/latency formula exactly. Port threads A, B, C, and the archived thread D, with every message in order, including the workflow_plan block (Thread A), the preview block (Thread A), and the approval_card block (Thread B — its payload.approval_id MUST be `appr["Set 20% weekend discount on Explorer Series (6 SKUs)"]`). inline_block_payload passed as plain JS objects (jsonb); inline_block_type/payload null for plain messages.

Drop the /tmp verification-count SELECT and the postgres-driver connect/teardown (lines 4-30, 363-386) — none of that belongs in the Drizzle port; serviceDb owns the connection.

The code MUST pass `npx tsc --noEmit`. Drizzle's typed `.values()` will surface any column-name or shape mismatch — fix to match the schema keys in the interfaces block (e.g. numeric price as string, real confidence as number, jsonb as objects, text[] as arrays). Do NOT use `any` to silence type errors; address the actual mismatch.
  </action>
  <verify>
    <automated>cd /Users/abhisingh/my-os/dev/Operator-Zero && npx tsc --noEmit 2>&1 | grep -v '^$' | grep -i "lib/demo/seed" ; test ${PIPESTATUS[1]} -ne 0 && echo "SEED TYPES CLEAN" || echo "SEED TYPE ERRORS"</automated>
  </verify>
  <done>lib/demo/seed.ts exists, exports reseedDemo, returns early when DEMO_USER_ID is empty, wraps wipe+seed in one serviceDb.transaction, ports all 13 data sections from /tmp/oz_seed.mjs faithfully, and `npx tsc --noEmit` reports no errors for lib/demo/seed.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire reseed into enterDemo, the reset route, and the tab-close beacon</name>
  <files>app/(auth)/login/actions.ts, app/api/demo/reset/route.ts, components/layout/demo-reset-beacon.tsx, app/app/layout.tsx</files>
  <action>
Four edits, all importing `reseedDemo` from "@/lib/demo/seed".

(a) app/(auth)/login/actions.ts — in `enterDemo()` only: add `import { reseedDemo } from "@/lib/demo/seed";` to the imports. Immediately BEFORE `const supabase = await createClient();` (i.e. before signInWithPassword), insert a non-blocking reseed:
  `try { await reseedDemo(); } catch (e) { console.error(JSON.stringify({ level: "warn", event: "demo.reseed_failed", error: String(e) })); }`
Leave `login()` and the rest of `enterDemo()` (session registry block, redirect) exactly as-is. Do not touch the destructive-action guards anywhere.

(b) NEW app/api/demo/reset/route.ts — Route Handler:
  Imports: `import { createClient } from "@/lib/auth/server";` `import { isDemoUser } from "@/lib/auth/demo";` `import { reseedDemo } from "@/lib/demo/seed";`
  `export const dynamic = "force-dynamic";` (precedent: app/api/chat/[threadId]/send/route.ts).
  `export async function POST(): Promise<Response>` — `const supabase = await createClient();` `const { data } = await supabase.auth.getClaims();` if `isDemoUser(data?.claims?.sub as string | undefined)` then `try { await reseedDemo(); } catch (e) { console.error(JSON.stringify({ level: "warn", event: "demo.reseed_failed", error: String(e) })); }`. ALWAYS `return new Response(null, { status: 204 });` regardless of branch or failure. No request body is read (sendBeacon may send none).

(c) NEW components/layout/demo-reset-beacon.tsx — `"use client";` component that renders `null`:
  `export function DemoResetBeacon()` with a `useEffect(() => { ... }, [])`. Guard: if `typeof navigator === "undefined" || typeof navigator.sendBeacon !== "function"` return. Define `const fire = () => { navigator.sendBeacon("/api/demo/reset"); };` Add a `pagehide` listener calling `fire`, and a `visibilitychange` listener that calls `fire` only when `document.visibilityState === "hidden"`. Return a cleanup that removes both listeners. Component returns `null`.

(d) app/app/layout.tsx — add `import { DemoResetBeacon } from "@/components/layout/demo-reset-beacon";`. Inside the existing `{isDemo && <DemoBanner />}` region, render the beacon under the SAME isDemo condition (e.g. change to `{isDemo && (<><DemoBanner /><DemoResetBeacon /></>)}` or add a second `{isDemo && <DemoResetBeacon />}` line). Non-demo users must still render the shell unchanged and never mount the beacon.

Constraints: tsc strict must stay clean; the existing vitest suite (351 tests) must stay green. Do NOT modify middleware, RLS, the auth model, lib/auth/demo.ts, or any env files. Do NOT weaken destructive-action guards.
  </action>
  <verify>
    <automated>cd /Users/abhisingh/my-os/dev/Operator-Zero && npx tsc --noEmit 2>&1 | grep -iE "actions\.ts|demo/reset|demo-reset-beacon|app/layout" ; test $? -ne 0 && echo "WIRING TYPES CLEAN" || echo "WIRING TYPE ERRORS" ; grep -q "reseedDemo" "app/(auth)/login/actions.ts" && grep -q "sendBeacon" components/layout/demo-reset-beacon.tsx && grep -q "DemoResetBeacon" app/app/layout.tsx && echo "WIRING PRESENT" || echo "WIRING MISSING"</automated>
  </verify>
  <done>enterDemo() calls reseedDemo() (non-blocking) before sign-in; app/api/demo/reset/route.ts POST reseeds only for the demo user and always returns 204; DemoResetBeacon fires sendBeacon to /api/demo/reset on pagehide and on hidden visibility, cleans up listeners, and is mounted in app/app/layout.tsx only under the existing isDemo condition; tsc clean; vitest suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public visitor → enterDemo / reset route | Anonymous/shared-demo input triggers a privileged service-role wipe+seed |
| reseedDemo → database (serviceDb) | RLS-bypass writes; isolation depends entirely on the explicit user_id filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ii6-01 | Tampering | reseedDemo wipe/seed on serviceDb | mitigate | USER = process.env.DEMO_USER_ID; early `return` when unset/empty, and every delete/update/insert filters by `eq(table.user_id, USER)` — RLS-bypass writes can never touch a non-demo user |
| T-ii6-02 | Elevation of Privilege | POST /api/demo/reset | mitigate | Reseed runs ONLY when `isDemoUser(claims.sub)` is true; non-demo callers get a 204 no-op. No request body is trusted or read |
| T-ii6-03 | Denial of Service | reset beacon spam / reseed cost | accept | Reset is a bounded single-tenant transaction guarded to the demo user only; sendBeacon is best-effort and fire-and-forget. Acceptable for a shared demo; existing per-user rate limits unchanged |
| T-ii6-04 | Information Disclosure | demo credentials / service role | mitigate | reseedDemo is server-only (no "use client"); DEMO_USER_ID read server-side only; serviceDb confined to server module; TOK is a non-decryptable placeholder, never a real token |
| T-ii6-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies are installed by this plan; nothing to verify |
</threat_model>

<verification>
- `npx tsc --noEmit` is clean across the repo (strict mode).
- `npx vitest run` passes — existing 351 tests still green (no test files modified or added).
- Static checks: `reseedDemo` is exported from lib/demo/seed.ts and imported by both enterDemo and the reset route; DemoResetBeacon mounted only under isDemo in app/app/layout.tsx.
- Safety: reseedDemo returns early with no DB access when DEMO_USER_ID is empty (read the function — the guard precedes the transaction).
- The reset route always returns 204 (success, failure, and non-demo branches all reach the same Response).
- /tmp/oz_seed.mjs is NOT moved, committed, or deleted.
</verification>

<success_criteria>
- lib/demo/seed.ts exists, server-only, exports `reseedDemo()`, and faithfully ports all 13 data sections of /tmp/oz_seed.mjs onto serviceDb inside one transaction.
- reseedDemo only ever touches DEMO_USER_ID rows; empty DEMO_USER_ID is a no-op.
- enterDemo() reseeds (non-blocking) before sign-in; failures are logged, never thrown.
- POST /api/demo/reset reseeds for the demo user and always returns 204.
- DemoResetBeacon fires navigator.sendBeacon("/api/demo/reset") on pagehide + hidden visibilitychange, with listener cleanup, mounted only for the demo user.
- tsc strict clean; vitest suite (351) green.
- Destructive-action guards, middleware, RLS, auth model, lib/auth/demo.ts, and env files unchanged.
</success_criteria>

<output>
Create `.planning/quick/260526-ii6-reset-demo-to-baseline-on-each-entry-via/260526-ii6-SUMMARY.md` when done.
</output>
