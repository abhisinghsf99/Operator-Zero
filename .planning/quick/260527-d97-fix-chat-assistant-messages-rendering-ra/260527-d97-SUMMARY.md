---
phase: quick-260527-d97
plan: "01"
subsystem: chat-ui
tags: [markdown, rendering, gfm, tables, xss-protection]
dependency_graph:
  requires: []
  provides: [formatted-assistant-messages, gfm-table-rendering]
  affects: [components/chat/message-stream.tsx]
tech_stack:
  added: [remark-gfm@^4.0.1]
  patterns: [ReactMarkdown with remarkGfm, styled components map, security comment mirroring content-preview.tsx]
key_files:
  created: []
  modified:
    - package.json
    - package-lock.json
    - components/chat/message-stream.tsx
decisions:
  - remark-gfm v4.x installed (compatible with react-markdown v10.x)
  - Inline components map in message-stream.tsx (mirrors content-preview.tsx pattern; no shared abstraction needed for two-file change)
  - Security comment added at ReactMarkdown call citing rehype-raw omission and T-2-06-02
  - whiteSpace:pre-wrap removed from outer div (ReactMarkdown handles block layout; pre-wrap would double-space paragraphs)
  - Streaming caret span placed after ReactMarkdown (not inside it) to avoid markdown re-parsing the caret
metrics:
  duration: "~8 minutes"
  completed: "2026-05-27"
  tasks: 2
  files: 3
---

# Phase quick-260527-d97 Plan 01: Fix Chat Assistant Messages Rendering Summary

**One-liner:** Added remark-gfm and ReactMarkdown components map to assistant MessageBubble so inventory reports, headings, bold, bullets, and GFM pipe tables render as formatted HTML instead of raw markdown source.

## What Was Built

- **Task 1 (chore 0d9b027):** Installed `remark-gfm ^4.0.1` via `npm install remark-gfm`. Package added to `package.json` dependencies and resolved in `package-lock.json`.

- **Task 2 (feat 0bd45f4):** Replaced the raw `whiteSpace: pre-wrap` text div in the assistant MessageBubble with a `<ReactMarkdown remarkPlugins={[remarkGfm]}>` render with a full styled components map covering: h1/h2/h3 (semibold, top margin), p (bottom margin), ul/ol/li (list-disc/list-decimal, left indent), strong, em, a (target=_blank + rel=noreferrer noopener), code (monospace, bg-deeper, r-sm), pre (monospace, bg-deeper, overflow-x:auto), blockquote (left border, text-secondary), hr (border). GFM table elements styled with bordered cells (border: 0.5px solid var(--border)), collapsed borders, semibold th with bg-subtle, and a block-level overflow-x:auto wrapper for horizontal scroll on narrow widths. Streaming caret preserved after ReactMarkdown output. User messages (role=user) branch unchanged.

## Security

T-2-06-02 (XSS via model-generated markdown) preserved: no rehype-raw import or rehypePlugins prop present. Security comment added at the ReactMarkdown call mirroring the pattern in content-preview.tsx. Anchor hrefs rendered with `rel="noreferrer noopener"` (T-2-06-03).

## TypeScript Typecheck

`npx tsc --noEmit` exits 0 with no errors — before and after the change. No type regressions introduced.

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, or schema changes introduced. The only trust boundary touched (model output → chat UI render) was explicitly in the plan's threat model and is mitigated.

## Self-Check: PASSED

- `components/chat/message-stream.tsx` exists and contains `remarkGfm` and `ReactMarkdown`: confirmed
- No `rehype-raw` import or usage in message-stream.tsx: confirmed (only appears in security comment)
- Commits exist:
  - `0d9b027` chore(quick-260527-d97-01): install remark-gfm
  - `0bd45f4` feat(quick-260527-d97-02): render assistant messages as styled markdown with GFM tables
- TypeScript typecheck: exit 0, no errors
