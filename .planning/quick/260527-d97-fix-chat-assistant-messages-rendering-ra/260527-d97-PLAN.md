---
phase: quick-260527-d97
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - components/chat/message-stream.tsx
autonomous: true
requirements: [QUICK-260527-d97]

must_haves:
  truths:
    - "Assistant chat messages render formatted markdown (headings, bold, bullets, GFM pipe tables) instead of raw markdown source"
    - "GFM pipe tables in inventory reports render as bordered, legible HTML tables"
    - "The streaming caret still appears after rendered content while status is streaming"
    - "User messages remain plain text (no markdown rendering)"
    - "No raw-HTML passthrough is introduced (rehype-raw absent) — T-2-06-02 XSS protection preserved"
  artifacts:
    - path: "package.json"
      provides: "remark-gfm dependency"
      contains: "remark-gfm"
    - path: "components/chat/message-stream.tsx"
      provides: "ReactMarkdown render of assistant message body with remark-gfm"
      contains: "remarkGfm"
  key_links:
    - from: "components/chat/message-stream.tsx"
      to: "remark-gfm"
      via: "remarkPlugins prop on ReactMarkdown"
      pattern: "remarkPlugins=\\{\\[remarkGfm\\]\\}"
---

<objective>
Fix the chat Orchestrator surface so assistant message text renders formatted markdown instead of raw markdown source.

Purpose: Assistant messages (e.g., inventory reports with GFM pipe tables, `### headings`, `**bold**`, `-` bullets) currently display as literal markdown because the assistant MessageBubble renders `{message.streamingContent ?? message.content}` directly inside a `white-space: pre-wrap` div with no markdown parser. Sarah sees inventory reports as raw pipe-tables.

Output: `remark-gfm` added as a dependency; the assistant MessageBubble renders its text body through `<ReactMarkdown remarkPlugins={[remarkGfm]}>` with a design-system styled components map, while preserving the streaming caret and keeping user messages plain text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@components/chat/content-preview.tsx
@components/chat/message-stream.tsx

<interfaces>
<!-- Existing markdown render pattern to mirror. From components/chat/content-preview.tsx -->
<!-- Executor must follow this approach: react-markdown + a `components={{...}}` map, NO rehype-raw. -->

content-preview.tsx already renders sanitized markdown:
```tsx
import ReactMarkdown from "react-markdown";

<ReactMarkdown
  // SECURITY: rehype-raw is NOT included — raw HTML passthrough disabled (T-2-06-02)
  components={{
    h1: ({ children }) => <h1 className="...">{children}</h1>,
    p:  ({ children }) => <p className="...">{children}</p>,
    ul: ({ children }) => <ul className="...">{children}</ul>,
    strong: ({ children }) => <strong className="...">{children}</strong>,
    code: ({ children }) => <code className="...">{children}</code>,
    // ...etc
  }}
>
  {content}
</ReactMarkdown>
```
NOTE: content-preview.tsx does NOT use remark-gfm and therefore does NOT style table elements. The assistant body MUST add remark-gfm AND table element styling (table/thead/tbody/tr/th/td) on top of the content-preview pattern.

The assistant MessageBubble text block to replace (components/chat/message-stream.tsx, ~lines 438-463):
```tsx
{(message.streamingContent || message.content) && (
  <div style={{ fontSize: 14, lineHeight: 1.55, color: "var(--text)", whiteSpace: "pre-wrap" }}>
    {message.streamingContent ?? message.content}
    {message.status === "streaming" && (
      <span aria-hidden="true" style={{ /* blinking caret */ }} />
    )}
  </div>
)}
```
`react-markdown@^10.1.0` is already a dependency. `remark-gfm` is NOT yet present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install remark-gfm dependency</name>
  <files>package.json, package-lock.json</files>
  <action>Run `npm install remark-gfm` to add GFM support to react-markdown. This is required because the inventory output uses GFM pipe tables, which react-markdown's default (CommonMark) parser does NOT render — without remark-gfm the tables would still show as raw pipe text. Verify remark-gfm lands in package.json `dependencies` and in package-lock.json. Pin to whatever stable version npm resolves (remark-gfm v4.x is compatible with react-markdown v10).</action>
  <verify>
    <automated>cd /Users/abhisingh/my-os/dev/Operator-Zero && grep -q '"remark-gfm"' package.json && grep -q 'node_modules/remark-gfm' package-lock.json && echo OK</automated>
  </verify>
  <done>remark-gfm appears in package.json dependencies and is resolved in package-lock.json; `node -e "require.resolve('remark-gfm')"` succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render assistant message body as styled markdown</name>
  <files>components/chat/message-stream.tsx</files>
  <behavior>
    - Assistant message containing `### heading`, `**bold**`, `- bullet`, and a GFM `| a | b |` pipe table renders as formatted HTML (h3, strong, li, table) — NOT literal markdown text.
    - While `message.status === "streaming"`, the blinking caret `<span>` still renders after the markdown content.
    - User messages (role === "user" branch) continue to render as plain text — unchanged.
    - No raw HTML in markdown is executed (no rehype-raw) — `<script>` or raw tags in content render as escaped text, not live elements.
  </behavior>
  <action>
Add imports `import ReactMarkdown from "react-markdown";` and `import remarkGfm from "remark-gfm";` at the top of components/chat/message-stream.tsx with the other imports.

Replace ONLY the assistant MessageBubble text block (~lines 438-463 — the `{(message.streamingContent || message.content) && (...)}` div). Keep the outer container `<div style={{ fontSize: 14, lineHeight: 1.55, color: "var(--text)" }}>` but REMOVE `whiteSpace: "pre-wrap"` (ReactMarkdown handles block layout; pre-wrap would double-space). Inside it, render a ReactMarkdown element with `remarkPlugins={[remarkGfm]}` and a `components` map, passing `message.streamingContent ?? message.content` as the child, followed by the existing streaming caret `<span>` — keep the `message.status === "streaming"` guard and its exact inline styles so the caret appears after the rendered content during streaming.

Provide a `components` map styled to the Operator Zero design system, mirroring content-preview.tsx's approach but tuned to the message-bubble typography (base fontSize ~14, lineHeight ~1.55) and using CSS custom properties. Style at minimum: h1/h2/h3 (semibold, var(--text), top margin between blocks), p (var(--text), bottom margin for spacing, no margin on last child), ul/ol/li (list-disc/list-decimal, left indent, var(--text)), strong (semibold var(--text)), em (italic var(--text-secondary)), a (var(--text), underline, target="_blank" with rel="noreferrer noopener"), code (mono, bg var(--bg-deeper) or var(--bg-subtle), small radius via var(--r-sm), padding), pre (mono, bg var(--bg-deeper), padding, overflow-x:auto, var(--r-sm)), blockquote (left border var(--border), padded, var(--text-secondary)), hr (border var(--border)).

Table styling is the priority of this fix — make GFM tables legible: render the table inside a wrapper with `display:block; overflow-x:auto` (or set overflow-x:auto on a wrapping container) so narrow widths scroll horizontally; bordered cells using var(--border); collapse borders (borderCollapse:"collapse"); th with header emphasis (semibold, bg var(--bg-subtle), var(--text), padding ~6px 10px); td with padding (~6px 10px), var(--text), border var(--border); thead/tbody/tr pass children through. Use only the existing CSS custom properties (var(--text), var(--text-secondary), var(--text-tertiary), var(--border), var(--bg-subtle), var(--bg-deeper), var(--r-sm)) — do not introduce new hardcoded colors.

SECURITY (MANDATORY — do not regress): Do NOT add rehype-raw or any rehypePlugins that enable raw-HTML passthrough. react-markdown's default renderer emits safe React elements only; this preserves the documented T-2-06-02 XSS protection. Add a short comment at the ReactMarkdown call noting rehype-raw is intentionally omitted (mirror content-preview.tsx).

SCOPE GUARDRAILS: Do NOT touch the user-message branch (role === "user") — it stays plain `{message.content}`. Do NOT change SSE streaming logic, the realtime subscription, the composer, or the InlineBlock routing. This is a presentation change to the assistant text body only.

An inline `components` map in message-stream.tsx is acceptable (content-preview.tsx defines its own); a small shared helper is also acceptable but keep it simple — do not over-engineer a shared component for a two-file change.
  </action>
  <verify>
    <automated>cd /Users/abhisingh/my-os/dev/Operator-Zero && grep -q 'remarkGfm' components/chat/message-stream.tsx && grep -q 'ReactMarkdown' components/chat/message-stream.tsx && ! grep -Eq 'rehype-raw|rehypeRaw' components/chat/message-stream.tsx && (npx tsc --noEmit 2>&1 | grep -i 'message-stream' && exit 1; echo OK)</automated>
    <human-check>Open the Orchestrator chat, trigger an inventory report (or paste a message with `### heading`, `**bold**`, a `-` bullet list, and a `| col | col |` pipe table). Confirm: headings/bold/bullets render formatted, the pipe table renders as a bordered legible HTML table (scrolls horizontally if narrow), the blinking caret still appears at the end of text while a response is streaming, and a user message you type still shows as plain text.</human-check>
  </verify>
  <done>Assistant messages render formatted markdown including GFM tables; streaming caret preserved; user messages unchanged; no rehype-raw added; `tsc --noEmit` reports no new errors in message-stream.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → chat UI render | Assistant message content (model-generated markdown) is rendered in the browser. Untrusted text crosses into the DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06-02 | Tampering/XSS | message-stream.tsx assistant body ReactMarkdown | mitigate | Render via react-markdown default renderer (safe React elements only); rehype-raw and all raw-HTML rehype plugins intentionally omitted, mirroring content-preview.tsx. Verify gate asserts no rehype-raw reference. |
| T-2-06-03 | Tampering | markdown link href in assistant content | mitigate | Anchor components open with `rel="noreferrer noopener"` and `target="_blank"`; react-markdown sanitizes `javascript:` URI schemes by default. |
</threat_model>

<verification>
- remark-gfm present in package.json + package-lock.json.
- message-stream.tsx imports and uses ReactMarkdown with `remarkPlugins={[remarkGfm]}`.
- No rehype-raw / rehypeRaw reference anywhere in message-stream.tsx.
- `npx tsc --noEmit` introduces no new type errors in message-stream.tsx.
- Manual: assistant markdown (incl. GFM tables) renders formatted; caret preserved during streaming; user messages plain text.
</verification>

<success_criteria>
- Assistant chat messages render formatted markdown (headings, bold, italics, bullets, links, code, blockquotes) instead of raw markdown source.
- GFM pipe tables render as bordered, legible HTML tables with horizontal scroll on narrow widths.
- The streaming caret continues to appear after rendered content while status is streaming.
- User messages remain plain text.
- T-2-06-02 XSS protection preserved (no rehype-raw / raw-HTML passthrough).
</success_criteria>

<output>
Create `.planning/quick/260527-d97-fix-chat-assistant-messages-rendering-ra/260527-d97-SUMMARY.md` when done.
</output>
