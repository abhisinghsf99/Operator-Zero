---
phase: quick-260608-j3y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/activity/_activity-view.tsx
  - app/app/activity/loading.tsx
  - components/chat/thread-sidebar.tsx
  - app/app/chat/page.tsx
  - app/app/chat/[threadId]/page.tsx
  - components/chat/message-stream.tsx
  - components/design/primitives.tsx
  - app/app/settings/loading.tsx
  - app/app/workflows/loading.tsx
  - app/app/approvals/loading.tsx
autonomous: true
requirements: [MOBILE-ACTIVITY, MOBILE-CHAT, MOBILE-PADDING]
user_setup: []

must_haves:
  truths:
    - "At ~390px, Activity shows a single full-width log; tapping an entry opens the detail full-width with a ← Back affordance that returns focus to the activating row"
    - "At ~390px, an active chat thread shows the message stream full-width with a ← Threads control; the chat index shows the thread list full-width"
    - "All Activity actions (select-mode, bulk-revert, filters, detail revert) and all Chat actions (composer, send, header menu) work at 390px — no read-only stripping"
    - "Desktop (md+) layout and behavior for Activity, Chat, and every padded header are visually and functionally unchanged"
    - "Oversized surface headers use responsive horizontal padding (smaller base, full value at md+)"
  artifacts:
    - path: "app/app/activity/_activity-view.tsx"
      provides: "Responsive Activity drill-down (mobile single-column, desktop resizable split)"
    - path: "components/chat/thread-sidebar.tsx"
      provides: "Breakpoint-gated thread sidebar (hidden below md when a thread is active)"
    - path: "components/design/primitives.tsx"
      provides: "SurfaceHeader with responsive horizontal padding"
  key_links:
    - from: "app/app/activity/_activity-view.tsx"
      to: "md: breakpoint"
      via: "Tailwind className gating, not JS window-width checks"
      pattern: "md:"
    - from: "components/chat/message-stream.tsx"
      to: "/app/chat"
      via: "mobile back-to-threads navigation"
      pattern: "/app/chat"
---

<objective>
Fix mobile (<md / ~390px) layout breakage on the Activity and Chat surfaces, where always-on multi-column splits overlap/crush content, and make oversized surface-header horizontal padding responsive. Mirror the existing Approvals drill-down convention (`md:` gated, list-or-detail, focus-restoring Back affordance). Desktop (md+) appearance and behavior must remain byte-for-byte identical.

Purpose: Mobile parity is a CLAUDE.md build constraint — no read-only stripping of the 5 core surfaces, WCAG 2.1 AA throughout.
Output: Activity + Chat usable at 390px; responsive header padding across surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- THE reference drill-down pattern to copy. Already correct — do NOT modify approvals. -->
Approvals drill-down (app/app/approvals/_list.tsx) establishes the convention:
- `const [activeId, setActiveId] = useState<string | null>(null);`  // mobile starts on list (no default select)
- List panel className: `cn("shrink-0 overflow-y-auto", "md:flex md:w-[380px]", activeId ? "hidden md:flex" : "flex w-full")`
- Detail panel className: `cn("overflow-hidden", "md:flex md:flex-1", activeId ? "flex flex-1" : "hidden md:flex")`
- Mobile-only Back bar: wrapped in `className="md:hidden"`, `<button aria-label="Back to approvals list">` with `<ArrowLeft size={15} aria-hidden="true" />`
- Focus management: `lastActivatedRowRef` captured on row activate; `requestAnimationFrame(() => lastActivatedRowRef.current?.focus())` on Back; detail heading focused on open.
- `cn` from `@/lib/utils`; `ArrowLeft` from `lucide-react`.

App shell (already correct — reuse breakpoint convention):
- Sidebar: `hidden md:flex`; BottomTabs: `flex md:hidden` fixed bottom with safe-area inset.
- Main content (app/app/layout.tsx): `pb-[56px] md:pb-0` clears the bottom tab bar — mobile panels must respect this, not cover it.

SurfaceHeader (components/design/primitives.tsx:380) is a SHARED primitive used by Activity + Approvals.
Currently: inline `style={{ padding: "32px 40px 22px", ... }}`. This is the single lever for header padding on those surfaces.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activity mobile drill-down (single-column below md, desktop split unchanged)</name>
  <files>app/app/activity/_activity-view.tsx, app/app/activity/loading.tsx</files>
  <action>
Rework the Activity log+detail split in `_activity-view.tsx` to mirror the Approvals drill-down (see `<interfaces>`), using Tailwind `md:` classes (NOT JS window-width checks) so SSR/hydration stays stable.

1. Default selection: change `useState<ActivityEntryRow | null>(initialEntries[0] ?? null)` to `useState<ActivityEntryRow | null>(null)`. Mobile lands on the log; desktop still wants a default. Restore the desktop-only default in a client-only `useEffect` that runs once: if `selectedEntry` is null AND `window.matchMedia("(min-width: 768px)").matches`, set it to `initialEntries[0] ?? null`. This follows the existing client-only panel-width restore pattern in this file (avoids hydration mismatch). Guard against clobbering a user selection (only set when still null).

2. Replace the inline-style `<div style={{ flex:1, display:"flex", ... }}>` split wrapper and its children with `cn`-gated Tailwind classes (import `cn` from `@/lib/utils`, `ArrowLeft` from `lucide-react`):
   - Outer split row: keep `flex-1 flex overflow-hidden min-h-0`.
   - Log `<main aria-label="Activity log">`: gate so it is full-width when no entry is selected, hidden below md when an entry IS selected. e.g. className `cn("flex flex-col overflow-hidden min-w-0", selectedEntry ? "hidden md:flex md:flex-1" : "flex flex-1 w-full")`. Keep all ActivityLog props unchanged.
   - Resize divider + `<aside>`: these are DESKTOP-ONLY. Wrap the entire `{selectedEntry && (<> divider + aside </>)}` block's desktop split (divider + fixed-width aside) in `className` that is `hidden md:flex` so the divider/aside never render below md. The 460px fixed `width: panelWidth` and `flexShrink: 0` apply at md+ only.

3. Mobile detail: below md, when `selectedEntry` is set, render the detail full-width (className `flex flex-col flex-1 w-full md:hidden`... but note desktop already renders the aside — so the mobile detail is a SEPARATE `md:hidden` block that renders `<ActivityDetail>` full-width). At the top of the mobile detail block add a `md:hidden` Back bar: `<button aria-label="Back to activity">` with `<ArrowLeft size={15} aria-hidden="true" />` + text "Back to activity". On click: `setSelectedEntry(null)` then `requestAnimationFrame(() => lastActivatedRowRef.current?.focus())`. Add a `lastActivatedRowRef` (useRef) and capture the activating row element — if ActivityLog's `onSelectEntry` does not pass the element, capture `document.activeElement` as the row at selection time, or store the entry id and refocus the row by a stable selector. Keep it simple: store the triggering element via a wrapper around `onSelectEntry`.

4. The divider's pointer/keyboard handlers must not be reachable on mobile: since the divider is inside the `hidden md:flex` desktop block, it is not rendered below md, so its handlers cannot fire. Confirm `touchAction: "none"` and drag logic only exist in the desktop-only block.

5. Bottom-tab clearance: the mobile detail panel must not cover the bottom tab bar — the existing `pb-[56px] md:pb-0` on the app main wrapper already reserves space; do NOT add `position: fixed`/full-viewport overlays. The detail scrolls within the content area.

6. Filter bar + error banner inline horizontal padding: the ActivityFiltersBar and error banner use 40px horizontal padding which overflows 390px. Where this file owns inline horizontal padding for full-width bars (error banner `padding: "12px 40px"`), make it responsive — e.g. swap the inline `40px` for a className `px-4 md:px-10` wrapper, or keep inline but reduce base. Match the header convention from Task 3. (ActivityFiltersBar internal padding lives in its own component — leave it unless trivially gated here; the header/banner are the visible offenders.)

Preserve ALL functionality at 390px: select-mode toggle, bulk-revert modal, filters, and the detail's revert actions must all work (they are prop-driven and unchanged). WCAG: keep aria labels, keyboard nav, reduced-motion. Desktop resize separator behavior MUST be byte-identical at md+.

7. In `app/app/activity/loading.tsx`, change the three `px-10` skeleton bars to `px-4 md:px-10` so the loading state matches the responsive header (desktop unchanged).
  </action>
  <verify>
    <automated>cd /Users/abhisingh/my-os/dev/Operator-Zero && npx tsc --noEmit && npx vitest run --silent 2>&1 | tail -15</automated>
  </verify>
  <done>typecheck clean; tests green; at <md the activity log is full-width with no default-selected detail, tapping an entry opens a full-width detail with a focus-restoring "Back to activity" control, and the resize divider/aside do not render below md; at md+ the resizable split is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Chat mobile drill-down (sidebar gated below md, message stream full-width)</name>
  <files>components/chat/thread-sidebar.tsx, app/app/chat/page.tsx, app/app/chat/[threadId]/page.tsx, components/chat/message-stream.tsx</files>
  <action>
Mirror the Approvals drill-down for Chat using `md:` breakpoint gating.

1. `thread-sidebar.tsx`: the `<aside>` is inline `width: 260, flexShrink: 0`. Add an optional prop `className?: string` to `ThreadSidebarProps` and apply it to the `<aside>` (via `className={className}` alongside the existing inline style). Keep the inline `width: 260` for desktop. The visibility/width gating is driven by the consumer pages (below). Do NOT change desktop appearance.

2. `[threadId]/page.tsx` (a thread IS active): a thread is "drilled into", so hide the sidebar below md. Pass `className="hidden md:flex"` to `<ThreadSidebar>`. The `<ChatThreadView>` message stream is already `flex: 1` so it fills full width when the sidebar is hidden. Outer wrapper already `flex h-full overflow-hidden` — fine.

3. `page.tsx` (chat index, NO active thread): the sidebar IS the list the user picks from, so it must be full-width below md and the centered empty-state hidden below md. Pass `className="w-full md:w-[260px] flex"` to the sidebar (override the fixed 260 on mobile — apply `md:!w-[260px]` or wrap: simplest is to let the className set `w-full` and use `md:w-[260px]`, and on the `<aside>` make the inline `width` conditional). Cleanest: in `thread-sidebar.tsx`, when a `className` containing width utilities is passed, drop the inline `width` so Tailwind wins — OR keep inline `width:260` but on the index page wrap the sidebar so mobile gets full width. Pick the approach that keeps desktop at exactly 260px. Hide the centered empty-state `<div>` below md with `className="hidden md:flex"` (convert its inline flex to a className wrapper, or add a `md:hidden`-inverse). Outer wrapper: add `md:overflow-hidden` if needed so the full-width sidebar scrolls.

4. Mobile "← Threads" affordance on the active-thread view: in `components/chat/message-stream.tsx`, the thread header (the `padding: "20px 32px"` flex row with Avatar/title/Search/ChatHeaderMenu) is where to add a back control. Add a `md:hidden` icon button at the START of that header row: `<button aria-label="Back to threads" onClick={() => router.push("/app/chat")}>` with `<ArrowLeft size={18} aria-hidden="true" />` (import ArrowLeft from lucide-react, useRouter from next/navigation — check if already imported). Style it to match existing IconButtons (ghost, focus-visible ring, reduced-motion respected). Do NOT duplicate ChatHeaderMenu functionality — this is purely navigation back to the thread list. Reduce the header's inline horizontal padding to be mobile-friendly (e.g. wrap `px-4 md:px-8` or reduce the 32px base) so Avatar+title+Search+menu+back don't overflow at 390px.

5. Composer + message stream must remain usable at 390px and clear the bottom tab bar — the app shell `pb-[56px] md:pb-0` already handles clearance; verify the composer is inside that padded main and not fixed over the tabs. The message stream inner `maxWidth: 760, padding: "0 32px"` is fine (it shrinks). If 32px side padding crowds 390px, reduce to a responsive `px-4 md:px-8` on that inner wrapper.

WCAG: the back control needs `aria-label="Back to threads"`, keyboard reachable (native button), reduced-motion respected. Desktop (md+) chat must be visually/behaviorally unchanged — sidebar visible at 260px, no back button shown.
  </action>
  <verify>
    <automated>cd /Users/abhisingh/my-os/dev/Operator-Zero && npx tsc --noEmit && npx vitest run --silent 2>&1 | tail -15</automated>
  </verify>
  <done>typecheck clean; tests green; at <md an active thread shows the message stream full-width with a "Back to threads" control and no sidebar; the chat index shows the thread list full-width with the empty-state hidden; at md+ the 260px sidebar + empty-state are unchanged and no back button appears.</done>
</task>

<task type="auto">
  <name>Task 3: Responsive surface-header padding (shared SurfaceHeader + skeletons)</name>
  <files>components/design/primitives.tsx, app/app/settings/loading.tsx, app/app/workflows/loading.tsx, app/app/approvals/loading.tsx</files>
  <action>
Make oversized header horizontal padding responsive (smaller base, full value at md+); desktop unchanged.

1. `components/design/primitives.tsx` — `SurfaceHeader` (around line 380) uses inline `style={{ padding: "32px 40px 22px" }}`. This is the SHARED header for Activity + Approvals. Split vertical vs horizontal padding so horizontal becomes responsive: keep vertical inline (`paddingTop: 32, paddingBottom: 22`) and move horizontal to a Tailwind className `px-4 md:px-10` on the `<header>` (40px = px-10, so md+ is identical to today; base px-4 ≈ 16px for mobile). Verify visually that md+ renders exactly `40px` horizontal as before. Do not alter the inner flex layout, font sizes, or any other style.

2. Skeleton loading files that hardcode `px-10` (Tailwind) so the loading state doesn't jump:
   - `app/app/settings/loading.tsx`: change each `px-10` → `px-4 md:px-10`.
   - `app/app/workflows/loading.tsx`: change each `px-10` → `px-4 md:px-10`.
   - `app/app/approvals/loading.tsx`: change the `px-10` rows → `px-4 md:px-10`; the header skeleton uses inline `style={{ padding: "28px 40px 20px" }}` — convert its horizontal to `px-4 md:px-10` (keep `pt`/`pb` inline) to mirror the real SurfaceHeader.
   (Activity loading.tsx px-10 is handled in Task 1.)

Note: Workflows surface header — check whether `app/app/workflows/_workflows-view.tsx` renders its own header with a fixed horizontal padding (40px / px-10). If it does and it overflows 390px, apply the same `px-4 md:px-10` treatment there for consistency. If it uses SurfaceHeader, step 1 already covers it.

Desktop (md+) appearance MUST be identical for every surface. Do not touch any vertical padding values.
  </action>
  <verify>
    <automated>cd /Users/abhisingh/my-os/dev/Operator-Zero && npx tsc --noEmit && grep -rn "px-10\b" app/app/*/loading.tsx | grep -v "md:px-10" || echo "no bare px-10 skeletons remain"</automated>
  </verify>
  <done>typecheck clean; SurfaceHeader and all listed loading skeletons use `px-4 md:px-10` (or inline-equivalent) for horizontal padding; md+ horizontal padding is unchanged at 40px; no surface header overflows at 390px.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean across all touched files.
- `npx vitest run` green (no `import "server-only"` added to any test-reachable lib module — memory constraint).
- Manual @390px (devtools): Activity lands on full-width log, drill-down works with focus-restoring Back; Chat active thread is full-width with "Back to threads"; chat index list is full-width; no header overflows.
- Manual @md+ (≥768px): Activity resizable split, Chat 260px sidebar + empty-state, and all header paddings are visually identical to before.
</verification>

<success_criteria>
- Activity, Chat, and headers are usable and non-overlapping at ~390px.
- Zero read-only stripping: every action works on mobile (CLAUDE.md build constraint).
- WCAG 2.1 AA preserved: keyboard nav, aria labels, reduced-motion, focus restoration on Back.
- Desktop (md+) layout and behavior unchanged for every surface.
- All gating uses Tailwind `md:` classes (matching shell/approvals/settings), avoiding JS-width hydration mismatch.
</success_criteria>

<output>
Create `.planning/quick/260608-j3y-mobile-optimization-fix-activity-and-cha/260608-j3y-SUMMARY.md` when done.
</output>
