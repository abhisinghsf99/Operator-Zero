---
phase: quick-260529-f4g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agent/tools/index.ts
  - lib/agent/runtime.ts
  - lib/agent/generation/optimize-description.ts
  - lib/agent/tools/write/index.ts
  - tests/unit/tool-validation.test.ts
  - tests/unit/optimize-description.test.ts
  - tests/unit/shopify-optimize-tool.test.ts
  - tests/unit/run-workflow-step.test.ts
  - scripts/seed-optimize-descriptions-workflow.mjs
autonomous: true
requirements:
  - QUICK-f4g-00  # extractProposedAction tool contract + runWorkflowStep surfaces it (backward-compatible)
  - QUICK-f4g-01  # generation helper (cost-cap-gated, provider-routed)
  - QUICK-f4g-02  # smart optimize tool (propose/write/L3) registered beside write tools
  - QUICK-f4g-03  # live "Optimized product descriptions" L2 workflow for connected store

must_haves:
  truths:
    - "ToolDefinition exposes an OPTIONAL extractProposedAction(result, input, ctx) method; runWorkflowStep prefers it when present and falls back to the raw input otherwise (backward-compatible — existing tools are unchanged)"
    - "A new tool shopify_optimize_product_description appears in getToolDefinitions() and in WRITE_TOOL_NAMES alongside the existing write tools"
    - "PROPOSE via runWorkflowStep returns proposedAction = {product_gid, body_html: <generated>} (NOT the bare input), so the engine's approval card shows the generated copy; updateProduct is NOT called during propose"
    - "WRITE phase (input HAS body_html — the L2 approval re-dispatch or direct L3-with-body_html) calls updateProduct and does NOT regenerate (no second LLM call, no content drift)"
    - "When automationLevel is L3 and no body_html is supplied, the tool generates then writes in a single dispatch"
    - "Existing write tools (e.g. shopify_update_product_description) still return proposedAction === input via runWorkflowStep — the contract change does not alter their behavior"
    - "checkCostCap(userId) is consulted before the LLM call; a 'hard' cap fails cleanly without generating or writing"
    - "Tool input is zod-validated (bad input returns a correctable tool_result error, never throws)"
    - "An 'Optimized product descriptions' L2 / manual workflow exists for the connected-store user (resolved via user_profiles.shopify_shop = 'operator-zero.myshopify.com' → user_profiles.user_id) with a workflow_versions definition whose steps reference shopify_optimize_product_description"
  artifacts:
    - path: "lib/agent/tools/index.ts"
      provides: "extractProposedAction added to the ToolDefinition contract (optional, documented)"
      contains: "extractProposedAction"
    - path: "lib/agent/runtime.ts"
      provides: "runWorkflowStep prefers toolDef.extractProposedAction over raw input for proposedAction"
      contains: "extractProposedAction"
    - path: "lib/agent/generation/optimize-description.ts"
      provides: "generateOptimizedDescription helper — cost-cap-gated, provider-routed (generateText), HTML-sanitized output"
      contains: "checkCostCap"
    - path: "lib/agent/tools/write/index.ts"
      provides: "shopify_optimize_product_description ToolDefinition (propose/write/L3 branching + extractProposedAction) + registration in writeTools"
      contains: "shopify_optimize_product_description"
    - path: "tests/unit/optimize-description.test.ts"
      provides: "Unit coverage for the generation helper — generate, cost-cap gate (hard blocks LLM), recordCost, HTML sanitization, placeholder brand voice"
      contains: "generateOptimizedDescription"
    - path: "tests/unit/shopify-optimize-tool.test.ts"
      provides: "Tool-level coverage — propose (no Shopify write) + extractProposedAction surfacing, write (no regenerate), L3, zod validation"
      contains: "shopify_optimize_product_description"
    - path: "tests/unit/run-workflow-step.test.ts"
      provides: "Coverage that runWorkflowStep surfaces extractProposedAction output for the optimize tool AND falls back to input for an existing tool (backward-compat guard)"
      contains: "extractProposedAction"
    - path: "scripts/seed-optimize-descriptions-workflow.mjs"
      provides: "Idempotent, user_id-scoped setup script creating the workflow + workflow_versions over DATABASE_URL; user_id resolved via user_profiles.shopify_shop"
      contains: "Optimized product descriptions"
  key_links:
    - from: "lib/agent/runtime.ts"
      to: "lib/agent/tools/index.ts (ToolDefinition.extractProposedAction)"
      via: "runWorkflowStep calls toolDef.extractProposedAction(toolResult, input, agentCtx) when present"
      pattern: "extractProposedAction"
    - from: "lib/agent/tools/write/index.ts"
      to: "lib/agent/generation/optimize-description.ts"
      via: "dynamic import of generateOptimizedDescription in the propose/L3 path"
      pattern: "optimize-description"
    - from: "lib/agent/tools/write/index.ts"
      to: "lib/integrations/shopify/mutations.ts"
      via: "updateProduct in the write/L3 path (the single Shopify write boundary)"
      pattern: "updateProduct"
    - from: "lib/agent/tools/index.ts"
      to: "lib/agent/tools/write/index.ts"
      via: "writeTools spread into getToolDefinitions registry"
      pattern: "writeTools"
    - from: "scripts/seed-optimize-descriptions-workflow.mjs"
      to: "workflow_versions.definition.steps[].tool"
      via: "steps reference shopify_optimize_product_description with params={product_gid}"
      pattern: "shopify_optimize_product_description"
---

<objective>
Build the "Optimized product descriptions" workflow end-to-end as a vertical slice: one small backward-compatible tool-contract extension (so a tool can surface a richer proposed action than its raw input), one real generation helper, one "smart" write tool, and one live L2 workflow for the connected Shopify dev store — proving the existing engine's generate→approve→write→activity-log pattern WITHOUT touching the durable engine file.

Purpose: Workflow #2 must do what L2 approval exists for — let the human review the GENERATED copy before it is written, and write exactly what was reviewed (no regeneration, no content drift, no double LLM cost). The engine (lib/inngest/functions/execute-workflow-run.ts) is UNCHANGED: it already reads `stepResult.proposedAction` for BOTH the approval preview (~264, ~299) and the approved re-dispatch input (~508). The only missing piece is that `runWorkflowStep` currently throws away the tool's generated output and substitutes the bare input as `proposedAction`. We fix that surgically — the fix lives ONLY in runtime.ts (use the tool's extracted action) + tools/index.ts (add the optional contract method) + the new tool. With `body_html` flowing through `proposedAction`, the approval card shows the generated copy AND the approved re-dispatch arrives with `body_html` so the tool's WRITE phase writes it directly (no regeneration).

Output:
- extractProposedAction added to the ToolDefinition contract (lib/agent/tools/index.ts) — optional, documented
- runWorkflowStep (lib/agent/runtime.ts) prefers the tool's extracted proposed action, falls back to input
- lib/agent/generation/optimize-description.ts — generation helper
- shopify_optimize_product_description tool in lib/agent/tools/write/index.ts (registered in writeTools, implements extractProposedAction)
- Updated tool-validation test count (11 → 12 write tools)
- tests/unit/optimize-description.test.ts — generation-helper coverage (generate/cost-cap/sanitize/brand-voice)
- tests/unit/shopify-optimize-tool.test.ts — tool-level coverage (propose/write/L3/zod/extractProposedAction)
- tests/unit/run-workflow-step.test.ts — proposedAction surfacing + backward-compat guard
- scripts/seed-optimize-descriptions-workflow.mjs — idempotent live workflow setup
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Contracts extracted from the codebase. Use these directly — no exploration needed. -->

ToolDefinition + AgentContext + ToolResult (from lib/agent/tools/index.ts):
```typescript
interface AgentContext { userId: string; automationLevel: "L1"|"L2"|"L3"; threadId?: string; workflowRunId?: string; }
interface ToolResult { type: "tool_result"; content: string; is_error?: boolean; }  // content is a STRING
interface ToolDefinition {
  name: string;
  description: string;
  inputSchema: z.ZodTypeAny;
  execute(input: unknown, ctx: AgentContext): Promise<ToolResult>;
  approvalRequired?: (input: unknown, ctx: AgentContext) => boolean;
  // ADDED in Task 1 (optional, backward-compatible):
  // extractProposedAction?: (result: ToolResult, input: unknown, ctx: AgentContext) => unknown;
}
```

`dispatchTool` (lib/agent/runtime.ts) is typed `(...) => Promise<ToolResult>`, so the `toolResult` binding in runWorkflowStep is ALREADY `ToolResult` — no cast and no extra import are needed when passing it to extractProposedAction.

ENGINE ↔ TOOL CONTRACT (verified — the engine file execute-workflow-run.ts is FROZEN; we do NOT edit it):
- `runWorkflowStep` (lib/agent/runtime.ts ~133-175) computes `requiresApproval` from `toolDef.approvalRequired(input, agentCtx)`, then returns
  `{ toolResult, requiresApproval, proposedAction: requiresApproval ? input : undefined, costUsd }`.
  THE DEFECT: it discards the tool's generated `toolResult` and substitutes the bare `input` as `proposedAction`.
- The engine reads `stepResult.proposedAction ?? workflowStep.params` as:
  - the approval preview / proposed_action (execute-workflow-run.ts ~264, ~297-299), AND
  - the input it RE-DISPATCHES the SAME tool with on approval, at automationLevel "L3" (~501-511, input at ~508).
- THEREFORE: if `proposedAction` carries the generated `{product_gid, body_html}`, the approval card shows the generated copy AND the approved re-dispatch arrives WITH `body_html`. The tool's WRITE phase ("input has body_html → updateProduct, no regenerate") then writes exactly what was reviewed. No engine edits required; no regeneration on approval.

THE FIX (this plan): give `ToolDefinition` an OPTIONAL `extractProposedAction(result, input, ctx)`. `runWorkflowStep` uses it when present to build `proposedAction`, else falls back to `input` (so every existing tool is byte-for-byte unchanged). The optimize tool's `extractProposedAction` JSON-parses its own propose-phase `ToolResult.content` and returns `{product_gid, body_html}`. Because `dispatchTool` already runs `execute()` during the propose step, the generated `body_html` is available on `toolResult` at the moment `runWorkflowStep` builds `proposedAction` — no second generation.

Existing write-tool shape to MIRROR (lib/agent/tools/write/index.ts — Tool 1 shopify_update_product_description):
```typescript
function defaultApprovalRequired(_input: unknown, ctx: AgentContext): boolean { return ctx.automationLevel !== "L3"; }
// schema → safeParse(input) → on fail return { type:"tool_result", is_error:true, content: formatZodError(parsed.error) }
// try { const { updateProduct } = await import("@/lib/integrations/shopify/mutations");
//   const result = await updateProduct(ctx.userId, { product_gid, body_html }); return JSON.stringify({ ok:true, idempotency_key: result.idempotency_key }); }
// catch (err) { return is_error tool_result `Failed to ...: ${String(err)}` }
```

Schema columns (verified):
- shopify_products (lib/db/schema/shopify-mirror.ts): composite PK (user_id, product_gid); columns title, body_html, vendor, product_type, meta_title — all on the row.
- brand_voice_profiles (lib/db/schema/brand-voice.ts): user_id is PK; columns profile_markdown (notNull), tone_tags (text[]), forbidden_phrases.
- user_profiles (lib/db/schema/users.ts): user_id is PK (references auth.users); column shopify_shop (the connected store domain). The store↔user binding lives HERE — there is NO shopify_shop column on auth.users.
</interfaces>

@lib/agent/runtime.ts
@lib/agent/tools/index.ts
@lib/agent/tools/write/index.ts
@lib/agent/tools/read/index.ts
@lib/agent/generation/optimize-description.ts
@lib/agent/llm/models.ts
@lib/agent/llm/pricing.ts
@lib/cost-cap.ts
@lib/integrations/shopify/mutations.ts
@lib/db/schema/brand-voice.ts
@lib/db/schema/shopify-mirror.ts
@lib/db/schema/users.ts
@lib/db/schema/workflows.ts
@lib/db/schema/workflow-versions.ts
@lib/demo/seed.ts
@tests/unit/catalog-audit.test.ts
@tests/unit/tool-validation.test.ts
@tests/unit/shopify-mutations.test.ts
@tests/unit/agent-errors.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tool contract — extractProposedAction + runWorkflowStep surfaces it (backward-compatible)</name>
  <files>lib/agent/tools/index.ts, lib/agent/runtime.ts, tests/unit/run-workflow-step.test.ts</files>
  <behavior>
    - ToolDefinition gains an OPTIONAL method extractProposedAction(result: ToolResult, input: unknown, ctx: AgentContext): unknown. Tools that omit it (every existing tool) behave exactly as today.
    - runWorkflowStep, when requiresApproval is true, sets proposedAction to toolDef.extractProposedAction(toolResult, input, agentCtx) if the method exists, otherwise to the raw input. When requiresApproval is false, proposedAction stays undefined.
    - A tool whose extractProposedAction returns { product_gid, body_html } makes runWorkflowStep return exactly that as proposedAction (proving the engine would preview/re-dispatch the generated copy).
    - An existing tool with no extractProposedAction still returns proposedAction === input (backward-compat regression guard).
  </behavior>
  <action>
    In lib/agent/tools/index.ts, add to the ToolDefinition interface an optional method:
    `extractProposedAction?: (result: ToolResult, input: unknown, ctx: AgentContext) => unknown;`
    Document it: "Optional. When a tool's propose-phase output (e.g. generated copy) must reach the engine's approval preview AND the approved re-dispatch — not just the raw input — implement this to derive the proposed action from the ToolResult. The runtime prefers it over the raw input; omit it to keep the default (proposedAction = input)." Do NOT change dispatchTool, the registry, or any other logic.

    In lib/agent/runtime.ts, inside runWorkflowStep, replace the return field
    `proposedAction: requiresApproval ? input : undefined`
    with logic that prefers the tool's extracted action. Compute exactly:
    ```ts
    const proposedAction = requiresApproval
      ? (toolDef?.extractProposedAction
          ? toolDef.extractProposedAction(toolResult, input, agentCtx)
          : input)
      : undefined;
    ```
    then return that in the result object. `toolDef` and `toolResult` are already in scope (toolDef from the existing dynamic-import + registry lookup at ~164, toolResult from dispatchTool at ~155). `toolResult` is ALREADY typed `ToolResult` because dispatchTool returns `Promise<ToolResult>` — do NOT add an `as ToolResult` cast and do NOT add a new `ToolResult` type import. Make NO other change to runtime.ts (cost cap, dispatch, error classification, the existing import statements all unchanged). This is backward-compatible: every current tool lacks extractProposedAction, so the ternary falls through to `input`.

    Write tests/unit/run-workflow-step.test.ts FIRST (RED). Mock "@/lib/cost-cap" (checkCostCap → "ok", recordCost). Mock "./tools/index" (or import path the test uses) so getToolDefinitions + dispatchTool are controllable — OR construct a fake registry by mocking getToolDefinitions to return a map with two fake tools and mocking dispatchTool to return a known ToolResult. Cases:
    (a) extractProposedAction present: a fake tool with approvalRequired → true and extractProposedAction returning { product_gid:"gid//1", body_html:"<p>x</p>" }; dispatchTool mocked to return a tool_result; assert runWorkflowStep(...) resolves with proposedAction deep-equal { product_gid:"gid//1", body_html:"<p>x</p>" } and requiresApproval true.
    (b) backward-compat: a fake tool with approvalRequired → true and NO extractProposedAction; assert proposedAction === the input object passed in stepDefinition.input.
    (c) no approval (L3 / approvalRequired false): assert proposedAction === undefined.
    Import the module under test AFTER the mocks.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/run-workflow-step.test.ts tests/unit/agent-errors.test.ts && npm run typecheck</automated>
  </verify>
  <done>ToolDefinition documents extractProposedAction; runWorkflowStep returns the extracted action when present and the raw input otherwise (no cast, no new import added to runtime.ts); run-workflow-step.test.ts passes (including backward-compat guard); agent-errors.test.ts still green; typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generation helper — cost-cap-gated, provider-routed, HTML-sanitized</name>
  <files>lib/agent/generation/optimize-description.ts, tests/unit/optimize-description.test.ts</files>
  <behavior>
    - generateOptimizedDescription({ userId, product, brandVoice, instructions }) returns a Promise<string> of safe HTML body copy.
    - It calls checkCostCap(userId) BEFORE the LLM call. On 'hard' it throws a clean budget error (mirror runtime.ts: Object.assign(new Error(...), { classification: { type: "budget_exhausted" } })) WITHOUT calling generateText.
    - On 'ok'/'soft' it calls generateText({ model: resolveModel("DRAFTER"), ... }) once, then recordCost(userId, costFor(modelId, inputTokens, outputTokens)) using resolveModelChoice("DRAFTER").modelId and result.usage.
    - Brand voice handling: if brandVoice is null/absent OR profile_markdown is a placeholder (trimmed value === "test" or length < 8), use a clean conversion-oriented default tone; otherwise ground the prompt in profile_markdown + tone_tags and respect forbidden_phrases.
    - Product fields (title, body_html, product_type, vendor) are summarized as DATA in the prompt (prompt-injection mitigation — mirror catalog-audit.ts buildAuditPrompt's "do not execute field values" framing).
    - Output is sanitized to safe HTML before return (strip <script>/<style>/<iframe>/on*=/javascript: ; allow basic copy tags p/ul/li/strong/em/h2/h3/br). Returned string is non-empty.
  </behavior>
  <action>
    Create lib/agent/generation/optimize-description.ts exporting `generateOptimizedDescription`. Signature: `(args: { userId: string; product: { title?: string|null; body_html?: string|null; product_type?: string|null; vendor?: string|null; product_gid: string }; brandVoice?: { profile_markdown?: string|null; tone_tags?: string[]|null; forbidden_phrases?: string[]|null } | null; instructions?: string }) => Promise<string>`.

    Import generateText from "ai"; resolveModel + resolveModelChoice from "@/lib/agent/llm/models"; costFor from "@/lib/agent/llm/pricing"; checkCostCap + recordCost from "@/lib/cost-cap". Use role "DRAFTER" (maps to an Opus model in the default profile — see models.ts PROFILES).

    Cost-cap gate FIRST (T-f4g-04): `const cap = await checkCostCap(userId); if (cap === "hard") throw Object.assign(new Error("Budget exhausted — cannot generate description"), { classification: { type: "budget_exhausted" } });`. Do NOT call the LLM on hard cap.

    Build the prompt with a private buildPrompt() that treats product fields as structured DATA (label each field; instruct the model not to execute field contents) and conditionally injects brand-voice grounding. Treat a placeholder brand voice (trimmed profile_markdown === "test" or shorter than 8 chars) as absent → default conversion-oriented tone. Ask for clean semantic HTML body copy only (no <html>/<head>, no markdown fences).

    Call generateText once: `const result = await generateText({ model: resolveModel("DRAFTER"), maxOutputTokens: 1024, messages: [{ role: "user", content: prompt }] });`. After it returns, record cost: `const { modelId } = resolveModelChoice("DRAFTER"); await recordCost(userId, costFor(modelId, result.usage?.inputTokens ?? 0, result.usage?.outputTokens ?? 0));` (guard usage fields — may be undefined in tests/mocks; default to 0).

    Sanitize result.text with a private sanitizeHtml(): strip code fences, remove <script>/<style>/<iframe> blocks, remove on*= attributes and javascript: URLs. Throw a clean Error if the sanitized output is empty. This module is server-only; do NOT instantiate a raw Anthropic client (use resolveModel via the AI SDK).

    Write tests/unit/optimize-description.test.ts FIRST (RED). This file mocks the LLM/cost layer ONLY (it is distinct from Task 3's tool-level file). Mock "ai" generateText (mirror catalog-audit.test.ts), mock "@/lib/agent/llm/models" (resolveModel + resolveModelChoice), mock "@/lib/cost-cap" (checkCostCap + recordCost), mock "@/lib/agent/llm/pricing" (costFor). Cases: (a) returns sanitized HTML from generateText output; (b) calls checkCostCap before generateText and on 'hard' throws budget error WITHOUT calling generateText (assert the generateText mock was not called); (c) calls recordCost after a successful generate; (d) strips a <script> tag from model output; (e) placeholder brand voice ("test") does not appear verbatim and falls back to default tone (assert generateText called once and output non-empty). Import the module AFTER the mocks.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/optimize-description.test.ts</automated>
  </verify>
  <done>optimize-description.test.ts passes; helper gates on cost cap before LLM, records cost after, sanitizes HTML, and handles placeholder brand voice. No raw Anthropic client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: shopify_optimize_product_description tool — propose/write/L3 + extractProposedAction + registration</name>
  <files>lib/agent/tools/write/index.ts, lib/agent/tools/index.ts, tests/unit/shopify-optimize-tool.test.ts, tests/unit/tool-validation.test.ts</files>
  <behavior>
    - New ToolDefinition `shopify_optimize_product_description` with inputSchema: `{ product_gid: string (min 1); body_html?: string; instructions?: string }`.
    - approvalRequired = defaultApprovalRequired (true unless ctx.automationLevel === 'L3') — reuse the existing helper in the file.
    - PROPOSE phase (input has product_gid, NO body_html, automationLevel !== 'L3'): read the product from shopify_products mirror scoped by ctx.userId (NOT from input); load brand_voice_profiles for ctx.userId; call generateOptimizedDescription; return ToolResult with is_error=false and content = JSON.stringify({ ok: true, phase: "propose", product_gid, body_html: <generated>, preview: <short summary> }). MUST NOT call updateProduct in this phase.
    - extractProposedAction(result, input): JSON.parse(result.content); if parsed.body_html present return { product_gid: parsed.product_gid, body_html: parsed.body_html }; on parse failure or missing field return input (fallback). This is what runWorkflowStep surfaces → flows into BOTH the engine's approval preview AND the approved re-dispatch.
    - WRITE phase (input HAS body_html — the L2 approval re-dispatch, or a direct L3-with-body_html): call updateProduct(ctx.userId, { product_gid, body_html }); MUST NOT call generateOptimizedDescription. Return JSON with ok + idempotency_key + phase:"write".
    - L3 single dispatch (NO body_html AND automationLevel === 'L3'): generate THEN updateProduct in one call; return JSON with ok + idempotency_key + phase: "l3".
    - Zod safeParse on entry; bad input → correctable tool_result error (never throws). Errors in generate/write → caught, returned as is_error tool_result (mirror existing write tools' try/catch).
    - Registered in the `writeTools` array so it appears in getToolDefinitions() and WRITE_TOOL_NAMES.
  </behavior>
  <action>
    In lib/agent/tools/write/index.ts add a new tool after the existing ones (before the `writeTools` export). Define `const optimizeProductDescriptionSchema = z.object({ product_gid: z.string().min(1, "product_gid is required"), body_html: z.string().optional(), instructions: z.string().optional() });`.

    Export `shopifyOptimizeProductDescription: ToolDefinition` with name "shopify_optimize_product_description", a clear description ("Generate an optimized, on-brand HTML product description; proposes the generated copy for approval (L1/L2) or writes directly (L3)."), inputSchema = the schema above, approvalRequired = defaultApprovalRequired.

    execute(input, ctx):
    1. safeParse → on failure return formatZodError tool_result (mirror Tool 1).
    2. try { ... } catch → return is_error tool_result with `Failed to optimize product description: ${String(err)}`.
    3. WRITE phase first: `if (parsed.data.body_html) { const { updateProduct } = await import("@/lib/integrations/shopify/mutations"); const result = await updateProduct(ctx.userId, { product_gid: parsed.data.product_gid, body_html: parsed.data.body_html }); return { type:"tool_result", is_error:false, content: JSON.stringify({ ok:true, phase:"write", idempotency_key: result.idempotency_key }) }; }` — NO generation here. This is the path the L2 approval re-dispatch lands on (because extractProposedAction put body_html into proposedAction, which the engine re-dispatches as input).
    4. Otherwise GENERATE: read the product from the mirror (serviceDb select from shopifyProducts where user_id = ctx.userId AND product_gid = parsed.data.product_gid, limit 1) — import serviceDb from "@/lib/db/client" and shopifyProducts from "@/lib/db/schema/shopify-mirror", eq/and from "drizzle-orm". If no row → return is_error tool_result "Product not found: <gid>". Load brand voice: serviceDb select from brandVoiceProfiles (import from "@/lib/db/schema/brand-voice") where user_id = ctx.userId, limit 1 (may be null). Call `const { generateOptimizedDescription } = await import("@/lib/agent/generation/optimize-description"); const bodyHtml = await generateOptimizedDescription({ userId: ctx.userId, product, brandVoice, instructions: parsed.data.instructions });`.
    5. If ctx.automationLevel === "L3": `const { updateProduct } = await import(...); const result = await updateProduct(ctx.userId, { product_gid: parsed.data.product_gid, body_html: bodyHtml }); return JSON({ ok:true, phase:"l3", idempotency_key: result.idempotency_key });`.
    6. Else (propose, L1/L2): return JSON({ ok:true, phase:"propose", product_gid: parsed.data.product_gid, body_html: bodyHtml, preview: bodyHtml.replace(/<[^>]+>/g, " ").trim().slice(0, 160) }) with is_error:false. NO updateProduct call.

    Add `extractProposedAction(result, _input)`: `try { const parsed = JSON.parse(result.content); if (parsed && typeof parsed.body_html === "string" && parsed.body_html.length > 0) { return { product_gid: parsed.product_gid, body_html: parsed.body_html }; } } catch { /* fall through */ } return _input;`. This guarantees the engine's approval preview shows the generated copy AND the approved re-dispatch carries body_html (→ WRITE phase, no regeneration). On any parse failure it degrades to the prior behavior (input).

    COST-CAP NOTE: this tool does NOT call checkCostCap directly — cost-cap enforcement is architecturally DELEGATED to generateOptimizedDescription (gated + covered by Task 2 tests). The WRITE phase makes no LLM call, so no cap check is needed there. If the tool is ever refactored to call the LLM directly (instead of via generateOptimizedDescription) it MUST re-add checkCostCap before the LLM call. No new cost-cap test is required in this task.

    Add `shopifyOptimizeProductDescription` to the `writeTools` array export and update the file header count/comment (11 → 12 write tools; list the new tool: "12. shopify_optimize_product_description").

    In lib/agent/tools/index.ts update the doc-comment counts ("11 write tools" → "12 write tools"; total tool count) — no logic change (getToolDefinitions spreads writeTools). (The ToolDefinition.extractProposedAction field was added in Task 1.)

    Update tests/unit/tool-validation.test.ts: change `expect(WRITE_TOOL_NAMES.length).toBe(11)` to `toBe(12)`. Add an assertion that `defs["shopify_optimize_product_description"]` is defined and exposes `approvalRequired`, `inputSchema`, and `extractProposedAction`.

    Create a NEW, SEPARATE test file tests/unit/shopify-optimize-tool.test.ts (distinct from Task 2's optimize-description.test.ts — this avoids a vitest mock collision: Task 2 mocks `ai`/generateText + cost-cap, this file mocks the higher-level helper + DB + Shopify). Mock "@/lib/agent/generation/optimize-description" (generateOptimizedDescription), mock "@/lib/integrations/shopify/mutations" (updateProduct), mock "@/lib/db/client" (serviceDb to return a product row + brand voice row). Import shopifyOptimizeProductDescription AFTER the mocks. Cases: (a) PROPOSE — execute({ product_gid }, { automationLevel:"L2" }): generateOptimizedDescription called once, updateProduct NOT called, result content includes body_html; AND shopifyOptimizeProductDescription.extractProposedAction(result, { product_gid }) returns { product_gid, body_html } matching the generated copy (NOT the bare input); (b) WRITE — execute({ product_gid, body_html:"<p>x</p>" }, { automationLevel:"L3" }): updateProduct called once with that body_html, generateOptimizedDescription NOT called (no regeneration); (c) L3 generate+write — execute({ product_gid }, { automationLevel:"L3" }): generateOptimizedDescription called AND updateProduct called; (d) zod — execute({}, ctx): is_error tool_result, neither generate nor updateProduct called; (e) extractProposedAction fallback — given a result whose content is non-JSON, returns the input unchanged.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/shopify-optimize-tool.test.ts tests/unit/tool-validation.test.ts && npm run typecheck</automated>
  </verify>
  <done>Tool present in getToolDefinitions()/WRITE_TOOL_NAMES (length 12); propose generates without writing AND extractProposedAction surfaces { product_gid, body_html }; write phase writes the supplied body_html without regenerating; L3 generates+writes; zod-validated; extractProposedAction degrades to input on parse failure; tool-level tests live in their own file (no mock collision with Task 2); typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 4: Idempotent live workflow setup script</name>
  <files>scripts/seed-optimize-descriptions-workflow.mjs</files>
  <action>
    Create scripts/seed-optimize-descriptions-workflow.mjs — a small idempotent Node script that connects via DATABASE_URL (the 6543 pooler — see MEMORY: MCP has no project access) using the project's postgres client, and creates the "Optimized product descriptions" workflow + a workflow_versions row for the connected-store user.

    Resolve the target user_id from the connected store via the user_profiles table (VERIFIED ground truth — the store↔user binding is in user_profiles.shopify_shop, NOT on auth.users). Run exactly:
    ```sql
    SELECT user_id FROM user_profiles WHERE shopify_shop = 'operator-zero.myshopify.com' LIMIT 1
    ```
    If it returns zero rows, the script MUST fail loudly with a clear message (e.g. "No user_profiles row for shopify_shop='operator-zero.myshopify.com' — connect the store first") and exit non-zero. Do NOT insert with a null or guessed user_id. Derive user_id from the store — never hardcode and never accept it from input (multi-tenant scoping).

    Idempotency: the script MUST be safe to re-run. Look up an existing workflow for that user_id with name = 'Optimized product descriptions'; if it exists, do nothing (or update its current_version_id only if missing) and exit 0. Otherwise insert workflow + version inside a single transaction.

    Build the version definition matching the verified shape (lib/demo/seed.ts §9 / WorkflowVersionDefinition in execute-workflow-run.ts): `{ entry_step, steps: [{ id, name, tool, type:"action", params, next_step }] }`. CRITICAL: each step's `params` IS the tool input the engine dispatches statically (engine reads `workflowStep.params`), and it MUST carry ONLY { product_gid } (no body_html) so the tool runs its PROPOSE phase. For each of ~15 target products with empty body_html, emit one step: `{ id: "opt-<n>", name: "Optimize description for <product_gid>", tool: "shopify_optimize_product_description", type: "action", params: { product_gid }, next_step: <next id|null> }`. entry_step = first step id. Select the target products: SELECT product_gid FROM shopify_products WHERE user_id = $userId AND (body_html IS NULL OR length(trim(body_html)) = 0) ORDER BY ... LIMIT 15.

    Insert the workflow row (workflows table): user_id, name 'Optimized product descriptions', description, automation_level 'L2', status 'active', trigger_type 'manual', trigger_config {}, current_version_id = the new version id, source 'chat'. Insert workflow_versions row: workflow_id, version_number 1, definition, schema_version 1. Match column names from the schema files (workflows.ts / workflow-versions.ts).

    Print a summary: resolved user_id, workflow_id, version_id, number of steps/products targeted. Use only server-side env (DATABASE_URL); no NEXT_PUBLIC_.

    NOTE on verification scope: idempotency and multi-tenant user_id scoping are validated by the human-check step only (seed scripts are not run in CI). The automated guards here are `node --check` (syntactic validity) and `npm run typecheck`.
  </action>
  <verify>
    <automated>node --check scripts/seed-optimize-descriptions-workflow.mjs && npm run typecheck</automated>
    <human-check>Run `DATABASE_URL=&lt;6543 pooler&gt; node scripts/seed-optimize-descriptions-workflow.mjs` against the dev store; confirm it resolves user_id via user_profiles.shopify_shop='operator-zero.myshopify.com' and prints that user_id + workflow_id + ~15 steps, and that re-running it is a no-op (idempotent — proves multi-tenant scoping + idempotency). Then in the app, run the workflow → an approval card shows the GENERATED copy (not just product_gid); approve → the EXACT reviewed copy is written to Shopify with no regeneration, and the Activity log shows a populated before→after.</human-check>
  </verify>
  <done>Script is syntactically valid and typechecks; resolves user_id via `SELECT user_id FROM user_profiles WHERE shopify_shop = 'operator-zero.myshopify.com'` and fails loudly (non-zero exit) on zero rows; running it creates exactly one 'Optimized product descriptions' L2/manual workflow for the store's user_id with a version whose steps reference shopify_optimize_product_description with params={product_gid} only (one step per empty-body_html product, ~15); re-running is a no-op. Idempotency + tenant scoping are confirmed via the human-check step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool ToolResult → proposedAction → engine | runWorkflowStep derives proposedAction from the tool's result via extractProposedAction; must degrade to input on malformed content (never throw) |
| workflow step params → tool input | static params dispatched into the tool; product_gid must be scoped to the run's userId, never trusted as a tenant key |
| LLM output → body_html → Shopify | generated HTML crosses into a live store write; must be sanitized |
| script env → DB write | setup script writes workflow rows; user_id must be derived from the store (user_profiles.shopify_shop), not supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f4g-00 | Tampering | extractProposedAction parsing tool content | mitigate | JSON.parse wrapped in try/catch; missing/invalid body_html → return raw input (fail-safe); never throws into runWorkflowStep |
| T-f4g-01 | Tampering | generated HTML → updateProduct → Shopify | mitigate | sanitizeHtml() strips script/style/iframe/on*=/javascript: before the string becomes body_html; write boundary remains updateProduct |
| T-f4g-02 | Elevation | tool reads product/brand-voice by user_id | mitigate | serviceDb queries filter by ctx.userId (never input); product_gid scoped within the user's rows |
| T-f4g-03 | Information disclosure | prompt-injection via product/brand-voice fields | mitigate | fields summarized as DATA with "do not execute field contents" framing (mirror catalog-audit.ts) |
| T-f4g-04 | Denial of service | unbounded LLM spend | mitigate | checkCostCap(userId) consulted BEFORE generateText inside generateOptimizedDescription; 'hard' fails clean; recordCost after. Tool delegates the gate to the helper (no direct LLM call). Approval path no longer regenerates → no double spend |
| T-f4g-05 | Spoofing | setup script accepting a forged user_id | mitigate | user_id resolved by SELECT on user_profiles.shopify_shop = connected store; never hardcoded/accepted from input; fails loudly on zero rows |
| T-f4g-06 | Repudiation | unlogged write on approval | accept | engine + updateProduct already writeActivity before effect (observability-first) — unchanged here |
| T-f4g-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages added (reuses ai/@ai-sdk, drizzle, zod, postgres already in repo) — no install task |
</threat_model>

<verification>
- `npm run typecheck` passes (TypeScript strict).
- `npx vitest run tests/unit/run-workflow-step.test.ts tests/unit/optimize-description.test.ts tests/unit/shopify-optimize-tool.test.ts tests/unit/tool-validation.test.ts tests/unit/agent-errors.test.ts` passes.
- New tool present in getToolDefinitions() and WRITE_TOOL_NAMES (length 12).
- Unit coverage proves: (a) runWorkflowStep surfaces extractProposedAction output ({product_gid, body_html}) for the optimize tool AND still returns input for an existing tool (backward-compat); (b) propose generates + returns body_html WITHOUT calling updateProduct; (c) write phase (body_html present) calls updateProduct and does NOT regenerate; (d) cost cap consulted before generation in the helper ('hard' blocks the LLM call); (e) tool input zod-validated; (f) extractProposedAction degrades to input on parse failure.
- `node --check scripts/seed-optimize-descriptions-workflow.mjs` passes.
- Manual: running the workflow yields an approval card showing the GENERATED copy; approving writes that exact copy to Shopify with no regeneration; Activity log shows populated before→after. Seed-script idempotency + tenant scoping confirmed in the same human-check.
- execute-workflow-run.ts is UNCHANGED (engine frozen — confirm no diff to that file).
</verification>

<success_criteria>
- ToolDefinition exposes optional extractProposedAction; runWorkflowStep prefers it and falls back to input — existing tools' behavior is provably unchanged.
- shopify_optimize_product_description registered beside the existing write tools (12 write tools total), input-dependent: propose (generate, surface body_html via extractProposedAction, no write) / write (write the supplied body_html, no regenerate) / L3 (generate+write).
- The L2 approval card shows the GENERATED description; approving writes exactly that copy (no content drift, no second LLM call).
- Generation helper reuses provider routing (generateText + resolveModel("DRAFTER")), gates on checkCostCap before the LLM call, records cost after, and sanitizes HTML.
- "Optimized product descriptions" L2 / manual workflow exists for the connected store's user (resolved via user_profiles.shopify_shop) with a workflow_versions definition referencing the new tool with params={product_gid} only.
- NO engine edits (execute-workflow-run.ts unchanged). The fix lives only in runtime.ts + tools/index.ts (contract) + the tool. Multi-tenant user_id scoping on every query. updateProduct is the only Shopify write boundary. typecheck + touched tests green.
</success_criteria>

<output>
Create `.planning/quick/260529-f4g-optimized-product-descriptions-workflow-/260529-f4g-SUMMARY.md` when done. Document that the fix surfaces the generated body_html through proposedAction via the new extractProposedAction contract, that the L2 approval card shows the generated copy and the approved write does NOT regenerate, and that execute-workflow-run.ts (the durable engine) was NOT modified.
</output>
