---
phase: quick-260526-r8o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(auth)/login/actions.ts
  - app/api/demo/reset/route.ts
  - components/layout/demo-reset-beacon.tsx
  - app/app/layout.tsx
autonomous: true
requirements: [DEMO-REAL-01]
must_haves:
  truths:
    - "Entering the demo no longer wipes/reseeds the demo account's data — a connected real Shopify dev store stays connected permanently."
    - "Closing a demo tab no longer triggers a reset (the beacon endpoint is gone)."
    - "Demo sign-in, DemoBanner, and all isDemoUser destructive-action guards still work unchanged."
    - "The demo account (DEMO_USER_ID) has zero app rows across every per-user table in the live DB, and its user_profiles row remains with shopify_shop NULL."
    - "The two other accounts (gmail + shopify integration test users) are untouched."
  artifacts:
    - path: app/(auth)/login/actions.ts
      provides: "enterDemo without reseedDemo call/import"
      contains: "export async function enterDemo"
    - path: app/app/layout.tsx
      provides: "App shell without DemoResetBeacon"
      contains: "DemoBanner"
  key_links:
    - from: "app/(auth)/login/actions.ts"
      to: "lib/demo/seed.ts"
      via: "NO import (removed)"
      pattern: "reseedDemo"
---

<objective>
Turn off the demo auto-reset so a real Shopify dev store connected to the demo
account stays connected permanently, and clear the demo account's existing
seeded data from the live database.

The demo account is going "fully real": no reseed on entry, no reset-on-tab-close
beacon, no reset API route. Demo sign-in, the DemoBanner, and the visitor
destructive-action guards (isDemoUser) all stay in place — only the data-wiping
machinery is removed.

Purpose: Sarah's demo account becomes a persistent real environment instead of a
self-resetting sandbox. The seeded Wanderbound dataset is cleared once so the real
store data is the only thing present.
Output: 1 committed code change (Task 1) + 1 data-only DB clear (Task 2, nothing committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<verified_facts>
- Demo user id is read from process.env.DEMO_USER_ID at runtime (= 274e81f5-18a9-4f79-8539-460cbf91388c). NEVER hardcode it.
- Two OTHER accounts must NOT be touched by the clear:
  43f6ce70-e5f9-4440-8279-43b1f1c0218b (gmail integration test user)
  c0c77de9-6165-416f-8ab4-efdbbd7287b8 (shopify integration test user)
- reseedDemo() has exactly 2 call sites: app/(auth)/login/actions.ts (enterDemo) and app/api/demo/reset/route.ts.
- DemoResetBeacon: imported (line 18) and mounted (line 38) in app/app/layout.tsx; component file is components/layout/demo-reset-beacon.tsx.
- isDemoUser destructive-action guards live in app/app/settings/actions.ts — KEEP them. Do not touch settings/actions.ts.
- Supabase MCP has NO project access. For Task 2 use the service-role DATABASE_URL with the postgres driver, run via `node --env-file=.env.local`. tsx is at node_modules/.bin/tsx.
</verified_facts>

<wipe_order>
The clear in Task 2 MUST mirror reseedDemo()'s wipe order exactly (child → parent),
all filtered by user_id = DEMO_USER_ID:

1. approvals
2. activity_entries
3. messages
4. threads
5. workflow_runs
6. UPDATE workflows SET current_version_id = NULL  (break the self-ref first)
7. workflows  (cascades workflow_versions)
8. memory_items
9. brand_voice_samples
10. brand_voice_profiles
11. autonomy_thresholds
12. user_sessions
13. integrations
14. shopify_product_variants
15. shopify_products
16. gmail_messages
17. gmail_threads
18. gmail_sync_state

Then, instead of deleting user_profiles:
- UPDATE user_profiles SET shopify_shop = NULL WHERE user_id = DEMO  (keep onboarding_completed_at, keep the row)
</wipe_order>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disable the demo auto-reset (committed code change)</name>
  <files>app/(auth)/login/actions.ts, app/api/demo/reset/route.ts, components/layout/demo-reset-beacon.tsx, app/app/layout.tsx</files>
  <action>
Remove all demo reset machinery while keeping demo sign-in, the banner, and the guards intact:

1. app/(auth)/login/actions.ts — in enterDemo(), DELETE the reseed try/catch block
   (the `try { await reseedDemo(); } catch (e) { console.error(...demo.reseed_failed...) }`
   that runs after the `getDemoCredentials()` check and before `createClient()`).
   Also DELETE the import `import { reseedDemo } from "@/lib/demo/seed";` (line 32).
   Keep everything else in enterDemo: getDemoCredentials guard, signInWithPassword,
   the session-registry try/catch (recordSession + cancelDeletionIfPending), and the
   redirect("/app/workflows").

2. DELETE the file app/api/demo/reset/route.ts. If app/api/demo/ is now empty, remove
   the empty directory too (rmdir; do not force-delete other files).

3. DELETE the file components/layout/demo-reset-beacon.tsx.

4. app/app/layout.tsx — remove the import
   `import { DemoResetBeacon } from "@/components/layout/demo-reset-beacon";` (line 18)
   and remove the `<DemoResetBeacon />` element inside the `{isDemo && (...)}` block,
   leaving `{isDemo && <DemoBanner />}` (or the fragment with just DemoBanner). Keep the
   DemoBanner import and the isDemoUser logic.

DO NOT delete lib/demo/seed.ts (reseedDemo stays defined, just uncalled). DO NOT touch
lib/auth/demo.ts, the demo sign-in, DemoBanner, or app/app/settings/actions.ts guards.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; echo "---refs (expect 0 outside lib/demo/seed.ts)---"; grep -rn "DemoResetBeacon\|reseedDemo" app components lib --include="*.ts" --include="*.tsx" | grep -v "lib/demo/seed.ts"</automated>
  </verify>
  <done>`npx tsc --noEmit` passes with no errors. The grep prints zero matches for DemoResetBeacon or reseedDemo outside lib/demo/seed.ts. app/api/demo/reset/route.ts and components/layout/demo-reset-beacon.tsx no longer exist. enterDemo still signs in and redirects to /app/workflows. Commit: `feat(quick-260526-r8o-01): turn off demo auto-reset (keep demo sign-in + guards)`.</done>
</task>

<task type="auto">
  <name>Task 2: Clear the demo account's data in the live DB (data-only, nothing committed)</name>
  <files>(none committed — throwaway script in /tmp only)</files>
  <action>
Write a throwaway Node script (in /tmp, e.g. /tmp/oz_clear_demo.mjs — NOT inside the repo,
or delete it after) that connects with the service-role DATABASE_URL and reads DEMO_USER_ID
from env. Run it with `node --env-file=.env.local /tmp/oz_clear_demo.mjs`.

The script MUST:
- Read const USER = process.env.DEMO_USER_ID and abort immediately if it is unset/empty
  (mirror reseedDemo's safety invariant — never wipe an arbitrary user).
- Use the `postgres` driver already in node_modules (import postgres from 'postgres').
- Inside a single transaction, DELETE FROM each table WHERE user_id = USER in the exact
  wipe order from <wipe_order> (child → parent). Set workflows.current_version_id = NULL
  before deleting workflows so the self-reference doesn't block the delete.
- Do NOT delete user_profiles. Instead: UPDATE user_profiles SET shopify_shop = NULL
  WHERE user_id = USER (keep the row and onboarding_completed_at).
- Every statement filters on user_id = USER (or, for the profiles update, the same). NEVER
  issue an unfiltered DELETE/UPDATE. The two other accounts
  (43f6ce70-..., c0c77de9-...) must remain completely untouched.
- After the transaction, print a verification report: SELECT count(*) for each cleared
  table WHERE user_id = USER (all must be 0), plus
  SELECT count(*), shopify_shop FROM user_profiles WHERE user_id = USER (must be 1 row,
  shopify_shop = NULL). Also print counts for the two other user ids on `integrations` to
  prove they were not touched (expect their pre-existing rows unchanged / non-zero).

This task makes NO repo changes — do not stage, commit, or leave any file in the repo. The
SUMMARY must explicitly note that Task 2 produced no commit and record the printed counts.
  </action>
  <verify>
    <automated>node --env-file=.env.local /tmp/oz_clear_demo.mjs</automated>
  </verify>
  <done>Script run prints 0 rows for every cleared table for DEMO_USER_ID; user_profiles still has exactly 1 row for the demo user with shopify_shop = NULL; the gmail (43f6ce70-...) and shopify (c0c77de9-...) accounts' integration rows are unchanged. No file is committed or left in the repo (script lives in /tmp). Counts recorded in SUMMARY.</done>
</task>

</tasks>

<verification>
- Task 1: `npx tsc --noEmit` clean; grep confirms no DemoResetBeacon/reseedDemo references outside lib/demo/seed.ts; the two deleted files are gone.
- Task 2: verification report shows 0 rows across all cleared tables for the demo user, user_profiles preserved with shopify_shop NULL, other two accounts untouched.
- Ordering enforced: Task 1 committed before Task 2 runs.
</verification>

<success_criteria>
- Demo entry no longer reseeds; reset route + beacon removed; demo sign-in/banner/guards intact (`tsc` passes, grep clean).
- Demo account data cleared in the live DB (all per-user tables at 0 rows; user_profiles kept with shopify_shop NULL).
- Two non-demo accounts untouched.
- Task 1 = one commit; Task 2 = no commit (data-only), noted in SUMMARY.
</success_criteria>

<output>
Create `.planning/quick/260526-r8o-disable-demo-reset-clear-data/260526-r8o-SUMMARY.md` when done.
The SUMMARY MUST: note Task 2 produced no commit (data-only), and record the printed
verification counts (cleared-table zeros + user_profiles row + the two untouched accounts).
</output>
