---
phase: quick-260529-jk4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agent/generation/optimize-meta.ts
  - lib/agent/tools/write/index.ts
  - lib/agent/tools/index.ts
  - tests/unit/optimize-meta.test.ts
  - tests/unit/shopify-optimize-meta-tool.test.ts
  - tests/unit/tool-validation.test.ts
  - scripts/seed-optimize-meta-workflow.mjs
autonomous: false
requirements:
  - QUICK-jk4-01  # generation helper for meta (cost-cap-gated, provider-routed, length-guarded)
  - QUICK-jk4-02  # shopify_optimize_meta smart tool (propose/write/L3) registered beside write tools (12→13)
  - QUICK-jk4-03  # live "Optimize SEO meta" L2 workflow for the connected store

must_haves:
  truths:
    - "A new tool shopify_optimize_meta appears in getToolDefinitions() and in WRITE_TOOL_NAMES (count goes 12→13) alongside the existing write tools"
    - "PROPOSE via runWorkflowStep returns proposedAction = {product_gid, meta_title, meta_description} (NOT the bare input), so the engine's approval card shows the generated meta title + description; updateProduct is NOT called during propose"
    - "WRITE phase (input HAS meta_title and/or meta_description — the L2 approval re-dispatch or direct L3-with-meta) calls updateProduct and does NOT regenerate (no second LLM call, no content drift)"
    - "When automationLevel is L3 and no meta is supplied, the tool generates THEN writes in a single dispatch"
    - "generateOptimizedMeta consults checkCostCap(userId) BEFORE the LLM call; a 'hard' cap fails cleanly without generating or writing; recordCost is called after a successful generate"
    - "Generated meta_title is length-guarded to ~≤60 chars and meta_description to ~≤160 chars (truncated/trimmed plain text, not HTML)"
    - "Tool input is zod-validated (bad input returns a correctable tool_result error, never throws)"
    - "An 'Optimize SEO meta' L2 / manual workflow exists for the connected-store user (resolved via user_profiles.shopify_shop = 'operator-zero.myshopify.com' → user_profiles.user_id) with a workflow_versions definition whose steps reference shopify_optimize_meta with params={product_gid} only (one step per product MISSING meta)"
    - "The durable engine (lib/inngest/functions/execute-workflow-run.ts) and runWorkflowStep (lib/agent/runtime.ts) are UNCHANGED — the extractProposedAction contract already exists and is only USED here"
  artifacts:
    - path: "lib/agent/generation/optimize-meta.ts"
      provides: "generateOptimizedMeta helper — cost-cap-gated, provider-routed (generateText/resolveModel DRAFTER), returns { meta_title, meta_description } with SEO length guards"
      contains: "checkCostCap"
    - path: "lib/agent/tools/write/index.ts"
      provides: "shopify_optimize_meta ToolDefinition (propose/write/L3 branching + extractProposedAction) + registration in writeTools (12→13)"
      contains: "shopify_optimize_meta"
    - path: "tests/unit/optimize-meta.test.ts"
      provides: "Unit coverage for the meta generation helper — generate, cost-cap gate (hard blocks LLM), recordCost, length guards, placeholder brand voice"
      contains: "generateOptimizedMeta"
    - path: "tests/unit/shopify-optimize-meta-tool.test.ts"
      provides: "Tool-level coverage — propose (no Shopify write) + extractProposedAction surfacing {meta_title, meta_description}, write (no regenerate), L3, zod validation, parse fallback"
      contains: "shopify_optimize_meta"
    - path: "tests/unit/tool-validation.test.ts"
      provides: "WRITE_TOOL_NAMES count assertion updated 12→13; assertion that shopify_optimize_meta exposes approvalRequired + inputSchema + extractProposedAction"
      contains: "shopify_optimize_meta"
    - path: "scripts/seed-optimize-meta-workflow.mjs"
      provides: "Idempotent, user_id-scoped setup script creating the 'Optimize SEO meta' workflow + workflow_versions over DATABASE_URL; user_id resolved via user_profiles.shopify_shop"
      contains: "Optimize SEO meta"
  key_links:
    - from: "lib/agent/tools/write/index.ts"
      to: "lib/agent/generation/optimize-meta.ts"
      via: "dynamic import of generateOptimizedMeta in the propose/L3 path"
      pattern: "optimize-meta"
    - from: "lib/agent/tools/write/index.ts"
      to: "lib/integrations/shopify/mutations.ts"
      via: "updateProduct(userId, { product_gid, meta_title, meta_description }) in the write/L3 path (the single Shopify write boundary)"
      pattern: "updateProduct"
    - from: "lib/agent/runtime.ts (UNCHANGED)"
      to: "lib/agent/tools/write/index.ts (shopify_optimize_meta.extractProposedAction)"
      via: "runWorkflowStep already calls toolDef.extractProposedAction(toolResult, input, ctx) — contract pre-exists from f4g"
      pattern: "extractProposedAction"
    - from: "scripts/seed-optimize-meta-workflow.mjs"
      to: "workflow_versions.definition.steps[].tool"
      via: "steps reference shopify_optimize_meta with params={product_gid}"
      pattern: "shopify_optimize_meta"
---

<objective>
Build the "Optimize SEO meta" workflow (#1) as a near-clone of the just-shipped "Optimized product descriptions" workflow (#2, quick-260529-f4g). One new generation helper (returns meta TITLE + meta DESCRIPTION, not body HTML), one new "smart" write tool `shopify_optimize_meta` (propose/write/L3, mirroring `shopify_optimize_product_description`), and one live L2/manual workflow for the connected Shopify dev store.

Purpose: Reuse the proven generate→approve→write→activity-log pattern for SEO meta. The L2 approval card must show the GENERATED meta title + description before anything is written, and approving must write exactly what was reviewed (no regeneration, no content drift, no double LLM cost). The contract that makes this work — `ToolDefinition.extractProposedAction` surfaced by `runWorkflowStep` — ALREADY EXISTS (added in f4g). This plan only USES it. The durable engine (lib/inngest/functions/execute-workflow-run.ts) and runWorkflowStep (lib/agent/runtime.ts) are FROZEN and receive NO edits.

Output:
- lib/agent/generation/optimize-meta.ts — generation helper returning { meta_title, meta_description } (cost-cap-gated, provider-routed, SEO-length-guarded plain text)
- shopify_optimize_meta tool in lib/agent/tools/write/index.ts (registered in writeTools, implements extractProposedAction) — WRITE_TOOL_NAMES 12→13
- Updated tool-validation test (count 12→13 + tool-shape assertion)
- tests/unit/optimize-meta.test.ts — generation-helper coverage (generate/cost-cap/length-guard/brand-voice)
- tests/unit/shopify-optimize-meta-tool.test.ts — tool-level coverage (propose/write/L3/zod/extractProposedAction) in its OWN file to avoid vitest mock collision
- scripts/seed-optimize-meta-workflow.mjs — idempotent live workflow setup
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Contracts extracted from the codebase. Use these directly — no exploration needed. -->

ToolDefinition + AgentContext + ToolResult (from lib/agent/tools/index.ts) — extractProposedAction ALREADY EXISTS on the contract (added in f4g):
```typescript
interface AgentContext { userId: string; automationLevel: "L1"|"L2"|"L3"; threadId?: string; workflowRunId?: string; }
interface ToolResult { type: "tool_result"; content: string; is_error?: boolean; }  // content is a STRING
interface ToolDefinition {
  name: string;
  description: string;
  inputSchema: z.ZodTypeAny;
  execute(input: unknown, ctx: AgentContext): Promise<ToolResult>;
  approvalRequired?: (input: unknown, ctx: AgentContext) => boolean;
  extractProposedAction?: (result: ToolResult, input: unknown, ctx: AgentContext) => unknown;  // PRE-EXISTS
}
```

ENGINE ↔ TOOL CONTRACT (verified, FROZEN — do NOT edit runtime.ts or execute-workflow-run.ts):
- runWorkflowStep already builds `proposedAction = requiresApproval ? (toolDef.extractProposedAction ? toolDef.extractProposedAction(toolResult, input, ctx) : input) : undefined`.
- The engine reads `stepResult.proposedAction ?? workflowStep.params` as BOTH the approval preview AND the input it re-dispatches the SAME tool with (at L3) on approval.
- THEREFORE: if `proposedAction` carries the generated `{product_gid, meta_title, meta_description}`, the approval card shows the generated meta AND the approved re-dispatch arrives WITH meta. The tool's WRITE phase ("input has meta → updateProduct, no regenerate") writes exactly what was reviewed.

DIRECT ANALOG — shopify_optimize_product_description (lib/agent/tools/write/index.ts, Tool 12). Copy its structure exactly, swapping body_html → { meta_title, meta_description }:
- approvalRequired = defaultApprovalRequired (true unless ctx.automationLevel === "L3") — the existing helper in the file.
- WRITE phase first: if input has meta → updateProduct, no generation.
- GENERATE path: read product (user_id-scoped from shopifyProducts mirror) + brand voice (brandVoiceProfiles), call the helper.
- L3: generate then updateProduct in one call.
- extractProposedAction: JSON.parse(result.content) → return the generated fields; degrade to input on parse failure.

The write primitives ALREADY exist (shopify_update_meta_title Tool 2, shopify_update_meta_description Tool 3) — do NOT add new write primitives; the NEW tool calls updateProduct directly like Tool 12 does.

updateProduct (lib/integrations/shopify/mutations.ts) — the SINGLE Shopify write boundary. Accepts a partial patch and returns `{ idempotency_key, before_state, after_state, skipped }`:
```typescript
updateProduct(userId: string, patch: { product_gid: string; body_html?: string; meta_title?: string; meta_description?: string; status?: ... }): Promise<MutationResult<...>>
```
Both meta_title and meta_description are valid patch fields; pass whichever were generated/supplied. Activity log + before→after are handled inside updateProduct (observability-first — unchanged).

generateOptimizedDescription (lib/agent/generation/optimize-description.ts) — the helper to MIRROR. It: (1) `const cap = await checkCostCap(userId); if (cap === "hard") throw Object.assign(new Error(...), { classification: { type: "budget_exhausted" } });` BEFORE the LLM; (2) `generateText({ model: resolveModel("DRAFTER"), maxOutputTokens, messages })`; (3) `const { modelId } = resolveModelChoice("DRAFTER"); await recordCost(userId, costFor(modelId, result.usage?.inputTokens ?? 0, result.usage?.outputTokens ?? 0));`; (4) prompt builder treats product fields as DATA ("do not execute field contents"); (5) placeholder brand voice (trimmed profile_markdown === "test" or length < 8) → default tone. The meta helper mirrors all of this but returns parsed { meta_title, meta_description } plain-text values with SEO length guards instead of sanitized HTML.

Schema columns (verified):
- shopify_products (lib/db/schema/shopify-mirror.ts): composite PK (user_id, product_gid); columns title, body_html, vendor, product_type, meta_title, meta_description — all on the row.
- brand_voice_profiles (lib/db/schema/brand-voice.ts): user_id is PK; profile_markdown (notNull), tone_tags (text[]), forbidden_phrases.
- user_profiles (lib/db/schema/users.ts): user_id is PK; column shopify_shop (the connected store domain). The store↔user binding lives HERE.

Existing length conventions (from write primitives): shopify_update_meta_title max 70 chars; shopify_update_meta_description max 160 chars. Target generation guards: meta_title ~≤60, meta_description ~≤160 (truncate/trim to stay within updateProduct's accepted ranges).
</interfaces>

@lib/agent/runtime.ts
@lib/agent/tools/index.ts
@lib/agent/tools/write/index.ts
@lib/agent/tools/read/index.ts
@lib/agent/generation/optimize-description.ts
@lib/agent/llm/models.ts
@lib/agent/llm/pricing.ts
@lib/cost-cap.ts
@lib/integrations/shopify/mutations.ts
@lib/db/schema/brand-voice.ts
@lib/db/schema/shopify-mirror.ts
@lib/db/schema/users.ts
@lib/db/schema/workflows.ts
@lib/db/schema/workflow-versions.ts
@scripts/seed-optimize-descriptions-workflow.mjs
@tests/unit/optimize-description.test.ts
@tests/unit/shopify-optimize-tool.test.ts
@tests/unit/tool-validation.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Meta generation helper — cost-cap-gated, provider-routed, SEO-length-guarded</name>
  <files>lib/agent/generation/optimize-meta.ts, tests/unit/optimize-meta.test.ts</files>
  <behavior>
    - generateOptimizedMeta({ userId, product, brandVoice?, instructions? }) returns Promise<{ meta_title: string; meta_description: string }> (plain text, NOT HTML).
    - It calls checkCostCap(userId) BEFORE the LLM call. On 'hard' it throws a clean budget error (Object.assign(new Error("Budget exhausted — cannot generate meta"), { classification: { type: "budget_exhausted" } })) WITHOUT calling generateText.
    - On 'ok'/'soft' it calls generateText({ model: resolveModel("DRAFTER"), ... }) once, then recordCost(userId, costFor(modelId, inputTokens, outputTokens)) using resolveModelChoice("DRAFTER").modelId and result.usage (guard undefined usage → 0).
    - Brand voice handling mirrors optimize-description: null/absent OR placeholder (trimmed profile_markdown === "test" or length < 8) → default conversion/SEO-oriented tone; otherwise ground in profile_markdown + tone_tags + forbidden_phrases.
    - Product fields (title, body_html-derived summary, product_type, vendor, existing meta_title/meta_description) summarized as DATA ("do not execute field contents" framing — mirror optimize-description buildPrompt).
    - SEO length guards: meta_title trimmed and truncated to ≤60 chars; meta_description trimmed and truncated to ≤160 chars. Both must be non-empty after parsing; throw a clean Error if parsing yields empty values.
    - Output parsing: the model is asked to return JSON { "meta_title": "...", "meta_description": "..." }; helper strips markdown fences then JSON.parses; on parse failure falls back to a deterministic extraction (e.g. first non-empty line → title, remainder → description) so it never returns empty. Plain text only — strip any stray HTML tags from both fields.
  </behavior>
  <action>
    Create lib/agent/generation/optimize-meta.ts exporting `generateOptimizedMeta`. Signature: `(args: { userId: string; product: { product_gid: string; title?: string|null; body_html?: string|null; product_type?: string|null; vendor?: string|null; meta_title?: string|null; meta_description?: string|null }; brandVoice?: { profile_markdown?: string|null; tone_tags?: string[]|null; forbidden_phrases?: string[]|null } | null; instructions?: string }) => Promise<{ meta_title: string; meta_description: string }>`. Export an interface `OptimizeMetaArgs` and a result type if useful.

    Imports (identical to optimize-description.ts): generateText from "ai"; resolveModel + resolveModelChoice from "@/lib/agent/llm/models"; costFor from "@/lib/agent/llm/pricing"; checkCostCap + recordCost from "@/lib/cost-cap". Use role "DRAFTER".

    Cost-cap gate FIRST: `const cap = await checkCostCap(userId); if (cap === "hard") throw Object.assign(new Error("Budget exhausted — cannot generate meta"), { classification: { type: "budget_exhausted" } });`. Do NOT call the LLM on hard cap.

    Reuse the placeholder-brand-voice detection (trimmed profile_markdown === "test" or length < 8 → treat as absent) and the DATA-framed prompt structure from optimize-description.ts's buildPrompt. Build a private buildPrompt() that labels product fields as structured DATA, conditionally injects brand-voice grounding, and instructs the model to return ONLY a JSON object with keys "meta_title" (≤60 chars, compelling, keyword-aware) and "meta_description" (≤160 chars, benefit-led, action-oriented) — no markdown fences, no commentary, plain text values.

    Call generateText once: `const result = await generateText({ model: resolveModel("DRAFTER"), maxOutputTokens: 512, messages: [{ role: "user", content: prompt }] });`. After it returns, record cost: `const { modelId } = resolveModelChoice("DRAFTER"); await recordCost(userId, costFor(modelId, result.usage?.inputTokens ?? 0, result.usage?.outputTokens ?? 0));`.

    Parse result.text with a private parseMeta(): strip markdown code fences (mirror optimize-description sanitizeHtml's fence strip), JSON.parse → read meta_title / meta_description; on JSON failure fall back to a simple line-based extraction so output is never empty. Strip stray HTML tags from both values (replace `/<[^>]+>/g`), collapse whitespace, trim. Then length-guard: meta_title = title.slice(0, 60).trim(); meta_description = desc.slice(0, 160).trim(). If either is empty after parsing, throw a clean Error ("Generated meta was empty after parsing"). Return { meta_title, meta_description }. Server-only; do NOT instantiate a raw Anthropic client (use resolveModel via the AI SDK).

    Write tests/unit/optimize-meta.test.ts FIRST (RED). Mirror tests/unit/optimize-description.test.ts's mock setup EXACTLY (this file mocks the LLM/cost layer ONLY; it is a SEPARATE file from Task 2's tool test). Mock "ai" generateText, mock "@/lib/agent/llm/models" (resolveModel + resolveModelChoice), mock "@/lib/cost-cap" (checkCostCap + recordCost), mock "@/lib/agent/llm/pricing" (costFor). Default generateText mock returns text = JSON.stringify({ meta_title: "Great Mug — Buy Now", meta_description: "A durable, dishwasher-safe mug your customers will love. Order today." }) with usage { inputTokens: 100, outputTokens: 50 }. Cases:
    (a) returns { meta_title, meta_description } parsed from the JSON output (both non-empty strings);
    (b) calls checkCostCap before generateText and on 'hard' throws budget error WITHOUT calling generateText (assert the generateText mock was not called);
    (c) calls recordCost after a successful generate with costFor(modelId, inputTokens, outputTokens);
    (d) truncates an over-length meta_title (mock returns a >60-char title → assert result.meta_title.length ≤ 60) and an over-length meta_description (>160 → ≤160);
    (e) placeholder brand voice ("test") does not appear verbatim in the prompt and output is still produced (generateText called once);
    (f) resolveModel called with "DRAFTER"; (g) handles undefined usage gracefully; (h) malformed (non-JSON) model output still yields non-empty meta_title + meta_description via the fallback extraction.
    Import the module AFTER the mocks.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/optimize-meta.test.ts</automated>
  </verify>
  <done>optimize-meta.test.ts passes; helper gates on cost cap before LLM, records cost after, parses { meta_title, meta_description }, length-guards (≤60 / ≤160), handles placeholder brand voice + malformed output. No raw Anthropic client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: shopify_optimize_meta tool — propose/write/L3 + extractProposedAction + registration (12→13)</name>
  <files>lib/agent/tools/write/index.ts, lib/agent/tools/index.ts, tests/unit/shopify-optimize-meta-tool.test.ts, tests/unit/tool-validation.test.ts</files>
  <behavior>
    - New ToolDefinition `shopify_optimize_meta` with inputSchema: `{ product_gid: string (min 1); meta_title?: string; meta_description?: string; instructions?: string }`.
    - approvalRequired = defaultApprovalRequired (true unless ctx.automationLevel === "L3") — reuse the existing helper.
    - PROPOSE phase (input has product_gid, NO meta_title AND NO meta_description, automationLevel !== "L3"): read the product from shopify_products mirror scoped by ctx.userId (NOT from input); load brand_voice_profiles for ctx.userId; call generateOptimizedMeta; return ToolResult is_error=false, content = JSON.stringify({ ok: true, phase: "propose", product_gid, meta_title, meta_description, preview }). MUST NOT call updateProduct.
    - extractProposedAction(result, input): JSON.parse(result.content); if meta_title or meta_description present return { product_gid, meta_title, meta_description }; on parse failure or missing both fields return input (fallback).
    - WRITE phase (input HAS meta_title and/or meta_description — the L2 approval re-dispatch, or a direct L3-with-meta): call updateProduct(ctx.userId, { product_gid, meta_title?, meta_description? }) passing whichever fields are present; MUST NOT call generateOptimizedMeta. Return JSON ok + idempotency_key + phase:"write".
    - L3 single dispatch (NO meta AND automationLevel === "L3"): generate THEN updateProduct(both fields) in one call; return JSON ok + idempotency_key + phase:"l3".
    - Zod safeParse on entry; bad input → correctable tool_result error (never throws). Errors in generate/write → caught, returned as is_error tool_result.
    - Registered in the `writeTools` array → appears in getToolDefinitions() and WRITE_TOOL_NAMES (count 12→13).
  </behavior>
  <action>
    In lib/agent/tools/write/index.ts add a new tool (Tool 13) after shopify_optimize_product_description, before the `writeTools` export. Mirror the structure of shopify_optimize_product_description EXACTLY, swapping body_html → meta fields.

    Define `const optimizeMetaSchema = z.object({ product_gid: z.string().min(1, "product_gid is required"), meta_title: z.string().optional(), meta_description: z.string().optional(), instructions: z.string().optional() });`.

    Export `shopifyOptimizeMeta: ToolDefinition` with name "shopify_optimize_meta", description ("Generate an optimized SEO meta title + description; proposes the generated meta for approval (L1/L2) or writes directly (L3)."), inputSchema = the schema above, approvalRequired = defaultApprovalRequired.

    execute(input, ctx):
    1. safeParse → on failure return formatZodError tool_result (mirror Tool 1/12).
    2. try { ... } catch → return is_error tool_result `Failed to optimize meta: ${String(err)}`.
    3. WRITE phase FIRST: `const hasMeta = (parsed.data.meta_title !== undefined && parsed.data.meta_title !== "") || (parsed.data.meta_description !== undefined && parsed.data.meta_description !== "");` — if hasMeta: `const { updateProduct } = await import("@/lib/integrations/shopify/mutations"); const patch = { product_gid: parsed.data.product_gid }; if (parsed.data.meta_title) patch.meta_title = parsed.data.meta_title; if (parsed.data.meta_description) patch.meta_description = parsed.data.meta_description; const result = await updateProduct(ctx.userId, patch); return JSON({ ok:true, phase:"write", idempotency_key: result.idempotency_key });` — NO generation here. This is the path the L2 approval re-dispatch lands on (extractProposedAction put meta into proposedAction → engine re-dispatches it as input).
    4. Otherwise GENERATE: read the product from the mirror (serviceDb select from shopifyProducts where user_id = ctx.userId AND product_gid = parsed.data.product_gid, limit 1 — serviceDb + shopifyProducts + brandVoiceProfiles + eq/and are ALREADY imported in the file). If no row → return is_error tool_result "Product not found: <gid>". Load brand voice (serviceDb select brandVoiceProfiles where user_id = ctx.userId, limit 1; may be null). Call `const { generateOptimizedMeta } = await import("@/lib/agent/generation/optimize-meta"); const { meta_title, meta_description } = await generateOptimizedMeta({ userId: ctx.userId, product: { product_gid: productRow.product_gid, title: productRow.title, body_html: productRow.body_html, product_type: productRow.product_type, vendor: productRow.vendor, meta_title: productRow.meta_title, meta_description: productRow.meta_description }, brandVoice: brandVoiceRow ? { profile_markdown: brandVoiceRow.profile_markdown, tone_tags: brandVoiceRow.tone_tags ?? null, forbidden_phrases: brandVoiceRow.forbidden_phrases ?? null } : null, instructions: parsed.data.instructions });`.
    5. If ctx.automationLevel === "L3": import updateProduct, `const result = await updateProduct(ctx.userId, { product_gid: parsed.data.product_gid, meta_title, meta_description }); return JSON({ ok:true, phase:"l3", idempotency_key: result.idempotency_key });`.
    6. Else (propose, L1/L2): return JSON({ ok:true, phase:"propose", product_gid: parsed.data.product_gid, meta_title, meta_description, preview: `${meta_title} — ${meta_description}`.slice(0, 200) }) with is_error:false. NO updateProduct call.

    Add `extractProposedAction(result, _input)`: `try { const parsed = JSON.parse(result.content); if (parsed && (typeof parsed.meta_title === "string" && parsed.meta_title.length > 0) || (typeof parsed.meta_description === "string" && parsed.meta_description.length > 0)) { return { product_gid: parsed.product_gid, meta_title: parsed.meta_title, meta_description: parsed.meta_description }; } } catch { /* fall through */ } return _input;`. Mind operator precedence — wrap the OR'd presence checks in parentheses so a present-and-nonempty meta_title OR meta_description triggers the return. Degrades to input on parse failure or when neither field is present.

    COST-CAP NOTE: this tool does NOT call checkCostCap directly — cost-cap enforcement is DELEGATED to generateOptimizedMeta (gated + covered by Task 1 tests). The WRITE phase makes no LLM call, so no cap check is needed there. (Same architectural delegation as shopify_optimize_product_description.)

    Add `shopifyOptimizeMeta` to the `writeTools` array export and update the file header count/comment (12 → 13 write tools; add line "13. shopify_optimize_meta — generate + propose/write/L3").

    In lib/agent/tools/index.ts update the doc-comment counts ("12 write" → "13 write"; "WRITE_TOOL_NAMES — array of 12 write tool names" → 13; and the "11 read + 12 write + 5 meta" aggregate line → 13 write) — no logic change (getToolDefinitions spreads writeTools; WRITE_TOOL_NAMES is derived).

    Update tests/unit/tool-validation.test.ts: change the "all 12 write tools" describe/it title + `expect(WRITE_TOOL_NAMES.length).toBe(12)` to 13. Add an `it(...)` asserting `getToolDefinitions()["shopify_optimize_meta"]` is defined and exposes `approvalRequired` (function), `inputSchema`, and `extractProposedAction` (function). Leave the existing shopify_optimize_product_description assertion intact.

    Create a NEW, SEPARATE test file tests/unit/shopify-optimize-meta-tool.test.ts (distinct from Task 1's optimize-meta.test.ts — this avoids a vitest mock collision: Task 1 mocks `ai`/generateText + cost-cap; this file mocks the higher-level helper + DB + Shopify). MIRROR tests/unit/shopify-optimize-tool.test.ts exactly, swapping the helper + fields. Mock "@/lib/agent/generation/optimize-meta" (generateOptimizedMeta → resolves { meta_title: "Great Widget SEO Title", meta_description: "The best widget for modern professionals. Shop now." }), mock "@/lib/integrations/shopify/mutations" (updateProduct → { idempotency_key, before_state:null, after_state:null, skipped:false }), mock "@/lib/db/client" (serviceDb returning a product row then a brand voice row via the alternating-callCount pattern from shopify-optimize-tool.test.ts). Import shopifyOptimizeMeta AFTER the mocks. Cases:
    (a) PROPOSE — execute({ product_gid }, L2): generateOptimizedMeta called once, updateProduct NOT called, result content includes meta_title + meta_description and phase=propose; AND shopifyOptimizeMeta.extractProposedAction(result, { product_gid }) returns { product_gid, meta_title, meta_description } matching the generated values (NOT the bare input);
    (b) WRITE — execute({ product_gid, meta_title:"T", meta_description:"D" }, L3): updateProduct called once with a patch containing meta_title + meta_description, generateOptimizedMeta NOT called (no regeneration), phase=write;
    (b2) WRITE with only meta_title supplied → updateProduct patch has meta_title and no meta_description key; generateOptimizedMeta NOT called;
    (c) L3 generate+write — execute({ product_gid }, L3): generateOptimizedMeta called AND updateProduct called with both meta fields; phase=l3;
    (d) zod — execute({}, ctx): is_error tool_result, neither generate nor updateProduct called;
    (e) extractProposedAction fallback — content non-JSON → returns input unchanged; AND content JSON with neither meta field → returns input;
    (f) product not found (productQueryResult = []) → is_error tool_result, generateOptimizedMeta NOT called;
    plus the smoke tests from the analog (tool defined, correct name, approvalRequired true for L2 / false for L3).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/shopify-optimize-meta-tool.test.ts tests/unit/tool-validation.test.ts && npm run typecheck</automated>
  </verify>
  <done>Tool present in getToolDefinitions()/WRITE_TOOL_NAMES (length 13); propose generates without writing AND extractProposedAction surfaces { product_gid, meta_title, meta_description }; write phase writes the supplied meta without regenerating; L3 generates+writes both fields; zod-validated; extractProposedAction degrades to input on parse failure / missing fields; tool-level tests live in their own file (no mock collision with Task 1); tool-validation count is 13; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotent live "Optimize SEO meta" workflow setup script</name>
  <files>scripts/seed-optimize-meta-workflow.mjs</files>
  <action>
    Create scripts/seed-optimize-meta-workflow.mjs by MIRRORING scripts/seed-optimize-descriptions-workflow.mjs exactly. A small idempotent Node script connecting via DATABASE_URL (the 6543 session pooler — see MEMORY: MCP has no project access) using postgres.js, creating the "Optimize SEO meta" workflow + a workflow_versions row for the connected-store user.

    Config constants: SHOPIFY_SHOP = "operator-zero.myshopify.com"; WORKFLOW_NAME = "Optimize SEO meta"; WORKFLOW_DESCRIPTION = "Generate optimized SEO meta titles + descriptions for products missing meta. Each step proposes the generated meta for your review before writing to Shopify.".

    Resolve user_id from user_profiles.shopify_shop (T-jk4-05 — the store↔user binding is in user_profiles, NOT auth.users). Run exactly:
    ```sql
    SELECT user_id FROM user_profiles WHERE shopify_shop = 'operator-zero.myshopify.com' LIMIT 1
    ```
    On zero rows the script MUST fail loudly with a clear message ("No user_profiles row for shopify_shop='operator-zero.myshopify.com' — connect the store first") and exit non-zero. Never insert a null/guessed user_id; never accept user_id from input (multi-tenant scoping).

    Idempotency: look up an existing workflow for that user_id with name = 'Optimize SEO meta'; if it exists, log + exit 0 (no DB writes). Otherwise insert workflow + version inside a single sql.begin transaction.

    Target products — those MISSING meta (CHANGED from the descriptions script's body_html filter):
    ```sql
    SELECT product_gid FROM shopify_products
    WHERE user_id = $userId
      AND (meta_title IS NULL OR length(trim(meta_title)) = 0
           OR meta_description IS NULL OR length(trim(meta_description)) = 0)
    ORDER BY product_gid
    LIMIT 15
    ```
    Build the version definition matching the verified shape (lib/demo/seed.ts / WorkflowVersionDefinition): `{ entry_step, steps: [{ id, name, tool, type:"action", params, next_step }] }`. Each step: `{ id: "meta-<n>", name: "Optimize SEO meta for <product_gid>", tool: "shopify_optimize_meta", type: "action", params: { product_gid }, next_step: <next id|null> }`. CRITICAL: params carries ONLY { product_gid } (no meta_title/meta_description) so the tool runs its PROPOSE phase. entry_step = first step id (or null if zero products). If zero target products, warn (workflow created with 0 steps).

    Insert the workflow row (workflows table): id (randomUUID), user_id, name 'Optimize SEO meta', description, automation_level 'L2', status 'active', trigger_type 'manual', trigger_config '{}', current_version_id NULL initially, source 'chat', created_at/updated_at NOW(). Insert workflow_versions row: id (randomUUID), workflow_id, version_number 1, definition (tx.json(definition)), schema_version 1, created_at NOW(). Then UPDATE workflows SET current_version_id = versionId. (Same insert order as the descriptions script.)

    Print a summary: resolved user_id, workflow_id, version_id, number of steps/products targeted. Use only server-side env (DATABASE_URL); no NEXT_PUBLIC_.

    NOTE on verification scope: idempotency + multi-tenant scoping are validated by the human-check step only (seed scripts are not run in CI). Automated guards: `node --check` (syntactic validity) + `npm run typecheck`.
  </action>
  <verify>
    <automated>node --check scripts/seed-optimize-meta-workflow.mjs &amp;&amp; npm run typecheck</automated>
    <human-check>Run `DATABASE_URL=&lt;6543 pooler&gt; node scripts/seed-optimize-meta-workflow.mjs` against the dev store; confirm it resolves user_id via user_profiles.shopify_shop='operator-zero.myshopify.com', prints that user_id + workflow_id + N steps (products missing meta), and that re-running is a no-op (idempotent). Then in the app, run the "Optimize SEO meta" workflow → an approval card shows the GENERATED meta TITLE + DESCRIPTION (not just product_gid); approve → the EXACT reviewed meta is written to Shopify via updateProduct with no regeneration, and the Activity log shows a populated before→after on meta_title / meta_description.</human-check>
  </verify>
  <done>Script is syntactically valid and typechecks; resolves user_id via `SELECT user_id FROM user_profiles WHERE shopify_shop = 'operator-zero.myshopify.com'` and fails loudly (non-zero exit) on zero rows; running it creates exactly one 'Optimize SEO meta' L2/manual workflow for the store's user_id with a version whose steps reference shopify_optimize_meta with params={product_gid} only (one step per product missing meta, ≤15); re-running is a no-op. Idempotency + tenant scoping confirmed via the human-check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool ToolResult → proposedAction → engine | runWorkflowStep (FROZEN) derives proposedAction from the tool's result via extractProposedAction; must degrade to input on malformed content (never throw) |
| workflow step params → tool input | static params dispatched into the tool; product_gid scoped to the run's userId, never trusted as a tenant key |
| LLM output → meta_title/meta_description → Shopify | generated meta crosses into a live store write; must be plain text, length-guarded, stray-tag-stripped |
| script env → DB write | setup script writes workflow rows; user_id must be derived from the store (user_profiles.shopify_shop), not supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-jk4-00 | Tampering | extractProposedAction parsing tool content | mitigate | JSON.parse wrapped in try/catch; missing meta fields → return raw input (fail-safe); never throws into the frozen runWorkflowStep |
| T-jk4-01 | Tampering | generated meta → updateProduct → Shopify | mitigate | parseMeta strips markdown fences + stray HTML tags and length-guards (≤60 / ≤160) before the strings become meta_title/meta_description; write boundary remains updateProduct |
| T-jk4-02 | Elevation | tool reads product/brand-voice by user_id | mitigate | serviceDb queries filter by ctx.userId (never input); product_gid scoped within the user's rows |
| T-jk4-03 | Information disclosure | prompt-injection via product/brand-voice fields | mitigate | fields summarized as DATA with "do not execute field contents" framing (mirror optimize-description buildPrompt) |
| T-jk4-04 | Denial of service | unbounded LLM spend | mitigate | checkCostCap(userId) consulted BEFORE generateText inside generateOptimizedMeta; 'hard' fails clean; recordCost after. Tool delegates the gate to the helper (no direct LLM call). Approval path no longer regenerates → no double spend |
| T-jk4-05 | Spoofing | setup script accepting a forged user_id | mitigate | user_id resolved by SELECT on user_profiles.shopify_shop = connected store; never hardcoded/accepted from input; fails loudly on zero rows |
| T-jk4-06 | Repudiation | unlogged write on approval | accept | engine + updateProduct already writeActivity before effect (observability-first) — unchanged here |
| T-jk4-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages added (reuses ai/@ai-sdk, drizzle, zod, postgres already in repo) — no install task |
</threat_model>

<verification>
- `npm run typecheck` passes (TypeScript strict).
- `npx vitest run tests/unit/optimize-meta.test.ts tests/unit/shopify-optimize-meta-tool.test.ts tests/unit/tool-validation.test.ts` passes.
- New tool present in getToolDefinitions() and WRITE_TOOL_NAMES (length 13); tool-validation count assertion updated 12→13.
- Unit coverage proves: (a) PROPOSE generates + returns meta_title/meta_description WITHOUT calling updateProduct, and extractProposedAction surfaces { product_gid, meta_title, meta_description } (NOT bare input); (b) WRITE phase (meta present) calls updateProduct and does NOT regenerate; (c) L3 generates+writes both fields; (d) cost cap consulted before generation in the helper ('hard' blocks the LLM call); (e) tool input zod-validated; (f) extractProposedAction degrades to input on parse failure / missing fields; (g) meta length guards (≤60 / ≤160).
- `node --check scripts/seed-optimize-meta-workflow.mjs` passes.
- Manual (human-check): running the "Optimize SEO meta" workflow yields an approval card showing the GENERATED meta title + description; approving writes that exact meta to Shopify with no regeneration; Activity log shows populated before→after on meta_title/meta_description. Seed-script idempotency + tenant scoping confirmed in the same human-check.
- FROZEN files UNCHANGED (confirm no diff): lib/inngest/functions/execute-workflow-run.ts, lib/agent/runtime.ts. The extractProposedAction contract on lib/agent/tools/index.ts already exists from f4g and is only USED (no contract change; only doc-comment count bumps 12→13).
</verification>

<success_criteria>
- shopify_optimize_meta registered beside the existing write tools (13 write tools total), input-dependent: propose (generate meta, surface via extractProposedAction, no write) / write (write the supplied meta, no regenerate) / L3 (generate+write both fields).
- The L2 approval card shows the GENERATED meta title + description; approving writes exactly that meta (no content drift, no second LLM call).
- Generation helper reuses provider routing (generateText + resolveModel("DRAFTER")), gates on checkCostCap before the LLM call, records cost after, parses { meta_title, meta_description } as plain text, and enforces SEO length guards (≤60 / ≤160).
- "Optimize SEO meta" L2 / manual workflow exists for the connected store's user (resolved via user_profiles.shopify_shop) with a workflow_versions definition referencing shopify_optimize_meta with params={product_gid} only (one step per product missing meta).
- NO engine/runtime edits (execute-workflow-run.ts and runtime.ts unchanged — the extractProposedAction contract pre-exists from f4g and is only USED). Multi-tenant user_id scoping on every query. updateProduct is the only Shopify write boundary. typecheck + touched tests green. SCOPE FENCE: workflow #1 only — do NOT build #3 (inventory).
</success_criteria>

<output>
Create `.planning/quick/260529-jk4-meta-optimization-workflow-shopify-optim/260529-jk4-SUMMARY.md` when done. Document that shopify_optimize_meta mirrors shopify_optimize_product_description (body_html → meta_title/meta_description), that it surfaces the generated meta through proposedAction via the pre-existing extractProposedAction contract, that the L2 approval card shows the generated meta and the approved write does NOT regenerate, and that lib/inngest/functions/execute-workflow-run.ts + lib/agent/runtime.ts (the durable engine/contract) were NOT modified — only USED.
</output>
