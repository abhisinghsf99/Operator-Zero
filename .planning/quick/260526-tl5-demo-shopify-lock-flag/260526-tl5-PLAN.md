---
phase: quick-260526-tl5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auth/demo.ts
  - app/app/settings/actions.ts
  - app/api/integrations/shopify/connect/route.ts
  - components/layout/demo-banner.tsx
autonomous: true
requirements: [QUICK-260526-tl5]

must_haves:
  truths:
    - "Demo account can connect AND disconnect Shopify right now (flag unset = unlocked)"
    - "Setting DEMO_SHOPIFY_LOCKED=true (no code change) freezes demo connect + disconnect"
    - "Non-demo users are never affected by the new flag"
    - "Other demo guards (account deletion, sign-out-everywhere, revoke session, email/password update) still always block the demo user"
    - "Demo banner reads the new copy"
  artifacts:
    - path: "lib/auth/demo.ts"
      provides: "isDemoConnectionLocked() env-driven flag helper"
      contains: "export function isDemoConnectionLocked"
    - path: "app/app/settings/actions.ts"
      provides: "disconnect demo guard gated on isDemoConnectionLocked()"
      contains: "isDemoUser(userId) && isDemoConnectionLocked()"
    - path: "app/api/integrations/shopify/connect/route.ts"
      provides: "connect route demo guard gated on isDemoConnectionLocked()"
      contains: "isDemoUser(userId) && isDemoConnectionLocked()"
    - path: "components/layout/demo-banner.tsx"
      provides: "reworded demo banner copy"
      contains: "connected to a real Shopify store"
  key_links:
    - from: "app/app/settings/actions.ts"
      to: "lib/auth/demo.ts"
      via: "isDemoConnectionLocked import"
      pattern: "isDemoConnectionLocked"
    - from: "app/api/integrations/shopify/connect/route.ts"
      to: "lib/auth/demo.ts"
      via: "isDemoUser + isDemoConnectionLocked import"
      pattern: "isDemoConnectionLocked"
---

<objective>
Add a demo Shopify-connection lock flag so the demo account can connect AND disconnect now, while preserving the ability to "freeze" the connection later via a single env flip (no code change).

Purpose: The live portfolio demo is connected to a real Shopify store. Right now we want visitors to be able to exercise the full connect/disconnect flow. Later, when we want to freeze the connection, we flip `DEMO_SHOPIFY_LOCKED=true` and redeploy — no code edit.

Output:
- `isDemoConnectionLocked()` helper (env-driven, default UNLOCKED)
- Disconnect server-action guard gated on the flag
- Shopify connect route guard gated on the flag
- Reworded, non-dismissible demo banner copy
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Current contracts the executor must extend. Use these directly. -->

From lib/auth/demo.ts:
```typescript
export const DEMO_DISABLED_MESSAGE: string;
export function isDemoUser(userId: string | null | undefined): boolean;
export function getDemoCredentials(): { email: string; password: string } | null;
```

From app/app/settings/actions.ts (disconnectIntegration, ~line 183-187) — current demo guard:
```typescript
const userId = claims.sub as string;
const providerValue = parsed.data;

// Demo guard: block destructive action for the demo user
if (isDemoUser(userId)) return { error: DEMO_DISABLED_MESSAGE };
```
NOTE: this file has OTHER isDemoUser guards (requestAccountDeletion, signOutEverywhere,
revokeSession, updateEmail, updatePassword) — those MUST remain unchanged and keep
always-blocking the demo user.

From app/api/integrations/shopify/connect/route.ts (~line 38) — insertion point:
```typescript
  const userId = claims.sub as string;
  // ── Shop validation (T-2-03-03) ──
```
`origin` (= request.nextUrl.origin) and `NextResponse` are already in scope.

From components/layout/demo-banner.tsx (~line 28) — current copy:
```tsx
Demo — you&apos;re viewing a portfolio demo of Operator Zero with sample data, not the live product.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lock-flag helper, gate connect + disconnect guards, reword banner</name>
  <files>lib/auth/demo.ts, app/app/settings/actions.ts, app/api/integrations/shopify/connect/route.ts, components/layout/demo-banner.tsx</files>
  <action>
Four edits across four files. Default behavior with the env unset MUST be UNLOCKED (demo can connect/disconnect now).

1. lib/auth/demo.ts — append an exported helper after getDemoCredentials():
   - Name: `isDemoConnectionLocked(): boolean`, returning `process.env.DEMO_SHOPIFY_LOCKED === "true"`.
   - Doc comment: when true, the demo account's Shopify connect/disconnect is frozen; controlled by env so "freezing" is a config flip + redeploy (no code change); default (unset) = UNLOCKED.
   - Keep this module server-only (no "use client") consistent with the file's existing SECURITY note.

2. app/app/settings/actions.ts — in `disconnectIntegration` only:
   - Add `isDemoConnectionLocked` to the existing `from "@/lib/auth/demo"` import (alongside isDemoUser, DEMO_DISABLED_MESSAGE).
   - Change the demo guard from `if (isDemoUser(userId)) return { error: DEMO_DISABLED_MESSAGE };` to `if (isDemoUser(userId) && isDemoConnectionLocked()) return { error: DEMO_DISABLED_MESSAGE };`.
   - Update the adjacent comment to say it blocks the disconnect only when the demo connection is frozen (DEMO_SHOPIFY_LOCKED=true).
   - DO NOT touch the isDemoUser guards in requestAccountDeletion, signOutEverywhere, revokeSession, updateEmail, or updatePassword — they must still always block the demo user.

3. app/api/integrations/shopify/connect/route.ts:
   - Add an import of `{ isDemoUser, isDemoConnectionLocked }` from "@/lib/auth/demo".
   - Immediately after `const userId = claims.sub as string;` (line ~38, before the shop-validation block), add a guard that, when `isDemoUser(userId) && isDemoConnectionLocked()` is true, returns `NextResponse.redirect(\`${origin}/app/settings\`)`. `origin` and `NextResponse` are already in scope.

4. components/layout/demo-banner.tsx:
   - Replace ONLY the banner sentence text with: `Demo — a live portfolio demo of Operator Zero, connected to a real Shopify store.`
   - Keep all markup, styles, role, and aria-label identical. The new copy has no apostrophe, so the `you&apos;re` entity is removed naturally as part of the sentence swap.

5. Do NOT set DEMO_SHOPIFY_LOCKED anywhere (unset = unlocked = desired now). No DB writes, no env file changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- `isDemoConnectionLocked` is exported from lib/auth/demo.ts and returns true only when DEMO_SHOPIFY_LOCKED === "true".
- disconnectIntegration blocks the demo user only when the flag is set; the other five isDemoUser guards in actions.ts are unchanged.
- connect route redirects the demo user to /app/settings only when the flag is set; otherwise OAuth proceeds.
- Demo banner reads the new "connected to a real Shopify store" copy with unchanged markup/styles.
- `npx tsc --noEmit` passes.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- Grep confirms `isDemoConnectionLocked` is defined in lib/auth/demo.ts and referenced in both actions.ts and connect/route.ts.
- Grep confirms exactly the five untouched isDemoUser guards (requestAccountDeletion, signOutEverywhere, revokeSession, updateEmail, updatePassword) remain bare `isDemoUser(...)` checks in actions.ts, while disconnectIntegration uses the `&& isDemoConnectionLocked()` form.
- Demo banner contains "connected to a real Shopify store" and no longer contains "sample data, not the live product".
</verification>

<success_criteria>
- Flag unset → demo account can both connect and disconnect Shopify.
- Flag set to "true" → demo connect route redirects to /app/settings and disconnect returns DEMO_DISABLED_MESSAGE.
- Non-demo users unaffected.
- Banner copy updated, no other settings demo guards changed.
- Type-check passes.
</success_criteria>

<output>
Create `.planning/quick/260526-tl5-demo-shopify-lock-flag/260526-tl5-SUMMARY.md` when done.

SUMMARY must document the freeze procedure: to FREEZE the demo Shopify connection later,
set `DEMO_SHOPIFY_LOCKED=true` in Vercel project env (and `.env.local` for local dev) and
redeploy — no code change required. Unset/any-other-value = UNLOCKED.

Commit message: `feat(quick-260526-tl5-01): demo shopify connection lock flag (unlocked) + banner copy`
</output>
