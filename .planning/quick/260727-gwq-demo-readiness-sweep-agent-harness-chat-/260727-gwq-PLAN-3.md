---
task: 260727-gwq
plan: 3
title: Data + guards + docs — seeded workflows on real tools, seed-data gaps, sandbox guard, catalog-audit unblock, honesty fixes
type: execute
wave: 3
depends_on: ["260727-gwq-PLAN-1", "260727-gwq-PLAN-2"]
autonomous: true
sequential: true
files_modified:
  - lib/demo/seed.ts
  - app/onboarding/actions.ts
  - app/app/settings/actions.ts
  - lib/inngest/functions/catalog-audit.ts
  - lib/integrations/adapter.ts
  - lib/integrations/gmail/client.ts
  - README.md
  - CLAUDE.md
  - tests/unit/seed-registry.test.ts
  - tests/unit/settings.test.ts
  - tests/unit/catalog-audit.test.ts
covers:
  - WS1  seeded demo workflows call nonexistent tools (DEMO-FATAL)
  - WS8  sandbox self-destruct guard on disconnectIntegration
  - WS11 seed data gaps (orders, embeddings, dangling GIDs, archived thread, sync state, wipe completeness, model ids, onboarding starter steps, catalog-audit hang)
  - WS12 honesty/polish (adapter docblock, gmail provider_account_id, README/CLAUDE LLM stack + specialist-agent claim)

must_haves:
  truths:
    - "Every tool named by a seeded workflow step exists in the live tool registry, and every step carries params that pass that tool's Zod schema (WS1)."
    - "Pressing Run now on a seeded workflow produces a run whose green status is earned — no step returns Unknown tool (WS1 + plan 1 WS3)."
    - "shopify_list_orders returns rows, and order #WB-48217 exists and matches the seeded customer email (WS11)."
    - "Semantic recall returns results for the demo user because memory_embeddings is populated (WS11)."
    - "A sandbox visitor cannot disconnect the Shopify integration and wipe the demo (WS8)."
    - "Onboarding a real non-empty store shows suggestions instead of spinning forever (WS11)."
    - "README and CLAUDE.md describe the system that actually exists (WS12)."
  artifacts:
    - path: tests/unit/seed-registry.test.ts
      provides: "Registry-conformance gate — fails the build if any seeded step names a tool that does not exist"
  key_links:
    - from: lib/demo/seed.ts
      to: lib/agent/tools/index.ts
      via: "seeded workflow step.tool ∈ getToolDefinitions()"
    - from: lib/demo/seed.ts
      to: lib/db/schema/shopify-mirror.ts
      via: "shopifyOrders / shopifyPages / shopifyRedirects / shopifySyncState seeded and wiped"
---

<objective>
Make the demo dataset true: seeded workflows that call real tools with valid parameters, orders that back the inventory and Q&A stories, embeddings that make semantic recall work, activity and approvals that point at products that exist, and documentation that does not overstate the architecture. Plus close the sandbox self-destruct hole and unblock real-store onboarding.

Purpose: plan 1 makes the engine honest about failure. Without this plan, that honesty turns every seeded workflow red on stage.
Output: a demo dataset that survives being clicked on, and docs that survive being read.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260727-gwq-demo-readiness-sweep-agent-harness-chat-/260727-gwq-AUDIT.md
@.planning/quick/260727-gwq-demo-readiness-sweep-agent-harness-chat-/260727-gwq-PLAN-1.md
@CLAUDE.md

Key source files:
@lib/demo/seed.ts
@lib/agent/tools/index.ts
@app/app/settings/actions.ts
@lib/inngest/functions/catalog-audit.ts
</context>

<interfaces>
<!-- Everything the executor needs. Current as of planning; plans 1 and 2 have already landed. -->

CRITICAL DEPENDENCY ON PLAN 1: runWorkflowStep now propagates tool errors and executeWorkflowRun
finalizes the run as "failed" on a tool error. Seeded workflow steps currently carry NO `params`
key at all (lib/demo/seed.ts builds each step as { id, name, tool, type, next_step }), so
workflowStep.params is undefined, every tool's Zod safeParse fails, and — post-plan-1 — every
seeded run finishes RED. Rewriting the tools without also adding valid params does not fix the demo.
Both are required.

Live tool registry (lib/agent/tools) — the ONLY legal step tool values:
  READ  shopify_list_products, shopify_get_product, shopify_list_orders, shopify_get_inventory,
        shopify_list_pages, shopify_list_redirects, gmail_list_threads, gmail_get_thread,
        recall_memory, search_activity, get_brand_voice
  WRITE shopify_update_product_description, shopify_update_meta_title, shopify_update_meta_description,
        shopify_update_product_image_alt, shopify_update_product_status, shopify_update_variant_price,
        shopify_update_variant_inventory, shopify_create_redirect, shopify_update_page_content,
        gmail_draft_reply, gmail_send_email, shopify_optimize_product_description,
        shopify_optimize_meta, shopify_propose_restock
  META  record_memory_item, update_memory_item, soft_delete_memory_item, propose_workflow_plan,
        ask_user_clarification

Input schemas for the tools used below (exact key names — Zod is strict about these):
  shopify_list_products   { status?: "active"|"draft"|"archived"; limit?: int 1..50; search?: string }
  shopify_get_inventory   { product_gid?: string; variant_gid?: string; low_stock_threshold?: int >= 0 }
  shopify_list_orders     { financial_status?: string; fulfillment_status?: string; limit?: int 1..50 }
  gmail_list_threads      { support_only?: boolean; limit?: int 1..50 }
  gmail_get_thread        { gmail_thread_id: string }
  gmail_draft_reply       { thread_id: string; body: string; subject?: string }
  shopify_optimize_meta   { product_gid: string; meta_title?; meta_description?; instructions? }
  shopify_optimize_product_description { product_gid: string; body_html?; instructions? }
  shopify_propose_restock { variant_gid: string; inventory_qty?: int >= 0; instructions? }
  shopify_update_product_status { product_gid: string; status: "active"|"draft"|"archived" }
  shopify_update_product_image_alt { product_gid: string; image_id: string; alt_text: string (<=512) }
  shopify_update_variant_price { variant_gid: string; price: positive number }
  shopify_create_redirect { path: string starting with "/"; target: string }

Seeded GID scheme (lib/demo/seed.ts, the products loop starting at `let pidx = 100000001`):
  product i (1-based) → gid://shopify/Product/1000000{i}  … i.e. 100000001 … 100000014
  variant i           → gid://shopify/ProductVariant/{pidx + 500000} … i.e. 100500001 … 100500014
  sku                 → "WB-" + pidx
  Product order in the array: 1 Voyager Weekender, 2 Voyager 16" Laptop Sleeve, 3 Passport Wallet,
  4 Field Notebook A5, 5 Field Notebook A6, 6 Daily Carry Journal, 7 Explorer Dopp Kit,
  8 Card Sleeve, 9 Key Fob, 10 The Cartographer Roll (draft, qty 0), 11 Notebook Refill Dot Grid,
  12 Notebook Refill Lined, 13 Travel Tag Set (qty 7), 14 The Summit Backpack (qty 4).
  There is NO product 100000201/202/203 — those are the dangling GIDs the audit flags.

Mirror tables to seed (lib/db/schema/shopify-mirror.ts, lib/db/schema/gmail-mirror.ts,
lib/db/schema/memory-embeddings.ts):
  shopifyOrders    (PK user_id+order_gid) total numeric, financial_status, fulfillment_status,
                    shopify_created_at, last_synced_at
  shopifyPages     (PK user_id+page_gid) title, body_html, handle, last_synced_at
  shopifyRedirects (PK user_id+redirect_id) path, target, last_synced_at
  shopifySyncState (PK user_id) last_full_sync_at, last_webhook_at, last_poll_at, sync_status,
                    webhook_subscriptions
  gmailSyncState   (PK user_id) last_history_id, last_poll_at, sync_status
  memoryEmbeddings (id uuid default, user_id, source_type, source_id uuid, content, embedding
                    vector(1024), created_at)

lib/agent/embeddings.ts: embedText(text, inputType?) → Promise<number[]> (1024 dims, Voyage voyage-4).
  Voyage free tier is 3 requests/minute — see the pacing requirement in task 2.

lib/auth/demo.ts: isSandboxClaims(claims) returns true for anonymous users AND the shared demo user;
  isDemoUser(userId) only covers the shared demo user. DEMO_DISABLED_MESSAGE is the standard copy.
  Existing correct usages: app/app/settings/actions.ts lines 635, 675, 802, 830, 981.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite seeded and onboarding workflow definitions onto real registry tools with valid params</name>
  <files>lib/demo/seed.ts, app/onboarding/actions.ts, tests/unit/seed-registry.test.ts</files>
  <behavior>
    - For every seeded workflow version definition, every step.tool is a key of getToolDefinitions().
    - For every seeded step, the tool's inputSchema.safeParse(step.params) succeeds.
    - The seeded chat workflow_plan inline block names only real tools.
    - Every onboarding starter workflow has a non-empty steps array whose tools and params also pass the same two checks.
  </behavior>
  <action>
Part A — lib/demo/seed.ts, workflow definitions (section 9, the `wfDefs` array at ~line 560).
Widen `type StepTuple` from [id, name, tool] to [id, name, tool, params] where params is
`Record<string, unknown>`, and include `params: s[3]` in the object built inside the definition
mapper (~line 725) alongside id/name/tool/type/next_step. Then rewrite all ten workflows. Use exactly
these tools and params — they are chosen to exercise the propose/approve path against real seeded GIDs:

 1. "Alt text for new product images" (L3, schedule):
    scan → shopify_list_products, params { status: "active", limit: 50 }
    write → shopify_update_product_image_alt, params { product_gid: "gid://shopify/Product/100000001",
            image_id: "gid://shopify/MediaImage/100000001",
            alt_text: "Full-grain leather weekender duffel in tan, standing upright" }
 2. "Fix empty meta descriptions" (L3, schedule):
    scan → shopify_list_products, params { status: "active", limit: 50 }
    optimize → shopify_optimize_meta, params { product_gid: "gid://shopify/Product/100000002" }
 3. "Answer customer product questions" (L2, event):
    triage → gmail_list_threads, params { support_only: true, limit: 10 }
    read → gmail_get_thread, params { gmail_thread_id: "gmail-thread-aa12" }
    draft → gmail_draft_reply, params { thread_id: "gmail-thread-aa12",
            subject: "Re: Leather care after rain?",
            body: "Hi Maria — good news, a little rain won't hurt it. Let it air-dry away from heat, then rub in a thin coat of leather conditioner once it's fully dry. It'll darken slightly and even out over time." }
 4. "Low-stock flash-sale proposals" (L2, event):
    scan → shopify_get_inventory, params { low_stock_threshold: 5 }
    propose → shopify_propose_restock, params { variant_gid: "gid://shopify/ProductVariant/100500014" }
 5. "Weekly SEO audit — Journals" (L2, schedule):
    pull → shopify_list_products, params { search: "Journals", limit: 50 }
    optimize → shopify_optimize_meta, params { product_gid: "gid://shopify/Product/100000004" }
 6. "Retire long-out-of-stock products" (L2, schedule):
    scan → shopify_list_products, params { status: "draft", limit: 50 }
    archive → shopify_update_product_status, params { product_gid: "gid://shopify/Product/100000010", status: "archived" }
 7. "Tag cleanup — dedupe & fix" (L1, manual):
    scan → shopify_list_products, params { limit: 50 }
    (single step — L1 pauses at the first step by design, WR-05)
 8. "New collection copy — Explorer Series" (L1, paused, manual):
    draft → shopify_optimize_product_description, params { product_gid: "gid://shopify/Product/100000001",
            instructions: "Collection launch copy for the Explorer Series. Lead with what it is for." }
 9. "Fix 'Default Title' products" (L3, schedule):
    scan → shopify_list_products, params { status: "active", limit: 50 }
    rewrite → shopify_optimize_product_description, params { product_gid: "gid://shopify/Product/100000005" }
10. "Weekend discount planner" (L2, draft, manual):
    scan → shopify_get_inventory, params { low_stock_threshold: 20 }
    price → shopify_update_variant_price, params { variant_gid: "gid://shopify/ProductVariant/100500007", price: 54.4 }

Keep each workflow's existing name, level, status, trigger_type, description, source and updatedAgo
values so the Workflows surface copy is unchanged; only the step tuples change. Update the step
`name` strings where the old label described a fake tool (e.g. "Score titles + meta" → "Optimize meta
title + description"), keeping them human and demo-legible.

Part B — the seeded chat workflow_plan block (~line 1547, thread A). Replace the three steps so they
match workflow 5 above: pull → shopify_list_products, optimize → shopify_optimize_meta, and drop the
`request_approval` step entirely (approval is an engine behavior, not a tool) — replace it with a
third step `apply` → shopify_update_meta_title with params { product_gid:
"gid://shopify/Product/100000004", meta_title: "Refillable A5 Leather Notebook | Wanderbound" } so
the visualizer still shows a three-step plan and Save-as-workflow produces a runnable workflow.

Part C — app/onboarding/actions.ts (~line 313). The starter workflows are inserted with
`definition: { steps: [], version: 1, source: "onboarding" }`. Give each starter a real minimal
definition. Derive the steps from the starter's own domain; at minimum every starter gets a
`{ id: "scan", name, tool: "shopify_list_products", type: "action", params: { status: "active",
limit: 20 }, next_step: "act" }` first step plus a domain-appropriate second step drawn from the same
tool list as Part A (SEO starters → shopify_optimize_meta on the first seeded product gid is NOT
valid here because a real user's catalog has different GIDs — instead use a single-step scan-only
definition for any starter whose second step would need a concrete GID, and say so in a comment).
Rule: a starter workflow must never contain a step whose params reference a GID that may not exist
in the user's store. Keep entry_step set to the first step id.

Part D — create tests/unit/seed-registry.test.ts, the guard that stops WS1 from ever regressing.
It must not touch the DB: export or extract the workflow-definition table from seed.ts as a pure
const (add a named export such as `DEMO_WORKFLOW_DEFS` for the wfDefs array and
`DEMO_CHAT_PLAN_STEPS` for the thread-A plan block; do not restructure the seeding logic itself),
import getToolDefinitions from the live registry, and assert:
  - every step tool in DEMO_WORKFLOW_DEFS is a registry key;
  - every step's params pass that tool's inputSchema.safeParse;
  - the same two assertions for DEMO_CHAT_PLAN_STEPS;
  - the same two assertions for the onboarding starter definitions (export them from
    app/onboarding/actions.ts as a pure const too if needed, or move them to a small shared module).
Failure messages must name the offending workflow, step id and tool.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/seed-registry.test.ts tests/unit/onboarding-progress.test.ts</automated>
    <automated>! grep -nE '"(seo_score|request_approval|compute|preview|read|anthropic_generate|anthropic_classify|shopify_update_status|shopify_update_product|shopify_update_image_alt|shopify_update_meta_desc|gmail_trigger|gmail_send|shopify_inventory_trigger|activity_log)"' lib/demo/seed.ts</automated>
  </verify>
  <done>No seeded or onboarding workflow references a nonexistent tool; every step carries params that pass its tool's Zod schema; a registry-conformance test fails loudly if that ever regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Close the seed-data gaps — orders, embeddings, dangling GIDs, archived thread, sync state, wipe completeness, model ids</name>
  <files>lib/demo/seed.ts</files>
  <action>
 1. ORDERS (WS11). Add a new seed section inserting 18 shopify_orders rows spread over the last 30
    days for the user. Requirements:
      - order_gid follows "gid://shopify/Order/48###" and the SKU-style display name referenced by the
        seeded inbox must exist: one order MUST be order_gid "gid://shopify/Order/48217" with
        financial_status "paid", fulfillment_status "unfulfilled", shopify_created_at 5 days ago, and a
        total consistent with the Voyager Weekender price — this is the order greg.m@example.com asks
        about in "Where's my order #WB-48217?" (seed.ts line ~186). Add a second matching the
        "#WB-47990" thread: order_gid "gid://shopify/Order/47990", fulfilled, ~12 days ago.
      - the remaining 16 spread across the window with a realistic mix (roughly 70% paid+fulfilled,
        20% paid+unfulfilled, 10% refunded), totals drawn from the seeded product price points so the
        inventory-velocity story is coherent.
      - last_synced_at set from the existing MIN() helper.
    Add a comment tying the two named orders to the two gmail threads so the coupling is discoverable.
 2. PAGES + REDIRECTS. Seed 3 shopify_pages rows (About, Shipping & Returns, Leather Care — the last
    one is what the Q&A playbook grounds care answers in) and 2 shopify_redirects rows, so
    shopify_list_pages and shopify_list_redirects return data instead of empty arrays.
 3. SYNC STATE. Seed a shopifySyncState row (last_full_sync_at MIN(12), last_poll_at MIN(12),
    sync_status "healthy", webhook_subscriptions an empty array) and a gmailSyncState row
    (last_history_id a plausible numeric string, last_poll_at MIN(4), sync_status "healthy") so the
    Settings health checks read correctly.
 4. DANGLING GIDs (WS11). Replace every occurrence of gid://shopify/Product/100000201, 100000202 and
    100000203 with real seeded GIDs. Specifically: the "Retire 3 products out of stock 90+ days"
    approval's proposed_action.product_gids (~line 999) becomes
    ["gid://shopify/Product/100000010", "gid://shopify/Product/100000013", "gid://shopify/Product/100000009"],
    and its preview `items` titles must be updated to the matching real product titles
    (The Cartographer Roll, Travel Tag Set, Key Fob — Riveted) so the approval card and the catalog
    agree. The activity entry "Tagged 3 out-of-stock items for review" (~line 1134) points at
    gid://shopify/Product/100000010. Grep for "10000020" afterwards and confirm zero hits.
 5. ARCHIVED THREAD D (~line 1660). mkThread currently returns an id that is discarded. Capture it and
    add three messages via the existing msg() helper: a user message about connecting the store, an
    assistant reply confirming the Shopify connection and 143 products synced, and a closing user
    message about setting the brand voice. Use createdAgoMin values consistent with the thread's
    380-hour-old last_message_at.
 6. MODEL IDS (WS11). In the msg() helper (~line 1523), change the assistant model_id literal from
    "claude-opus-4-7" to "gemini-2.5-flash" so the seeded transcripts match the provider the demo
    actually runs on after plan 1's WS6 work. Add a comment naming MODEL_PROFILE=google as the source
    of truth so a future provider flip updates both.
 7. WIPE COMPLETENESS (WS11). Extend wipeUserData (~line 70) to delete, in child→parent order, the
    tables the audit found missing: shopifyOrders, shopifyPages, shopifyRedirects, shopifySyncState,
    gmailSyncState, memoryEmbeddings and agentTelemetry — each filtered by user_id. Add
    memoryEmbeddings BEFORE memoryItems so the FK-free but semantically dependent rows go first. Import
    the new schema tables at the top. Without this a reseed leaves stale orders and embeddings behind.
 8. MEMORY EMBEDDINGS (WS11). Seed embeddings for the six memory items so semantic recall works.
    Constraints that must be honoured:
      - Do this OUTSIDE the serviceDb.transaction — network calls inside a long transaction on a
        max:1 pooled client will stall the whole seed. Restructure seedDemoFor so the transaction
        returns the inserted memory item ids, then run a best-effort embedding pass after it commits.
      - Voyage free tier is 3 requests per minute. Pace the calls: await a delay of at least 21
        seconds between embedText calls, or batch if the SDK supports a single multi-input call —
        check lib/agent/embeddings.ts first and add a batch helper there if the SDK accepts an array
        input (voyage.embed takes `input`, so a single call with all six strings is preferred and
        removes the pacing problem entirely; fall back to paced single calls only if that fails).
      - Wrap the whole pass in try/catch. On any failure (429, missing VOYAGE_API_KEY, network) log a
        structured warning and continue — seeding must never fail because embeddings are unavailable.
      - Insert one memoryEmbeddings row per memory item with source_type "memory_item", source_id the
        memory item id, content the item's content text, embedding the 1024-dim vector.
    Add a `skipEmbeddings?: boolean` option to seedDemoFor (default false) so the sandbox per-visitor
    path can skip the network cost if the caller wants; default behavior seeds them.
 9. Leave reseedDemo() and wipeDemoFor() signatures alone — reseedDemo becomes genuinely usable once
    wipeUserData is complete.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
    <automated>! grep -q "10000020" lib/demo/seed.ts && grep -q "48217" lib/demo/seed.ts && grep -q "gemini-2.5-flash" lib/demo/seed.ts && ! grep -q "claude-opus-4-7" lib/demo/seed.ts</automated>
    <automated>grep -v '^\s*\*' lib/demo/seed.ts | grep -c "memoryEmbeddings\|shopifyOrders\|gmailSyncState" | grep -qv '^0$'</automated>
  </verify>
  <done>Orders (including #WB-48217 and #WB-47990), pages, redirects and both sync-state rows are seeded; embeddings are populated best-effort outside the transaction; no dangling product GIDs remain; the archived thread has messages; seeded model ids read gemini; wipeUserData cleans every table the seed writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sandbox guard, catalog-audit unblock, and documentation honesty</name>
  <files>app/app/settings/actions.ts, lib/inngest/functions/catalog-audit.ts, lib/integrations/adapter.ts, lib/integrations/gmail/client.ts, README.md, CLAUDE.md, tests/unit/settings.test.ts, tests/unit/catalog-audit.test.ts</files>
  <behavior>
    - disconnectIntegration called with anonymous (sandbox) claims returns the demo-disabled error and performs no delete.
    - disconnectIntegration called with the shared demo user's claims returns the demo-disabled error regardless of DEMO_SHOPIFY_LOCKED.
    - getCatalogAuditSuggestions for a user with products returns at least one suggestion derived from deterministic mirror checks, never an empty array.
  </behavior>
  <action>
Part A — WS8, app/app/settings/actions.ts around line 190. The disconnect guard currently reads
`if (isDemoUser(userId) && isDemoConnectionLocked()) return { error: DEMO_DISABLED_MESSAGE };`, which
lets an anonymous sandbox visitor disconnect Shopify — clearShopifyMirror() then wipes the demo
dataset and reconnect is blocked. Replace it with `if (isSandboxClaims(claims)) return { error:
DEMO_DISABLED_MESSAGE };`, matching the five other destructive actions in this file (lines 635, 675,
802, 830, 981). Keep the isDemoConnectionLocked import only if it is still used elsewhere in the file;
remove it from the import list if it becomes unused. Add a comment recording that the old
demo-lock-flag nuance is intentionally dropped: no sandbox or demo identity may disconnect, period.
Extend tests/unit/settings.test.ts with two cases — anonymous claims blocked, shared-demo claims
blocked — following the existing mocking style in that file.

Part B — WS11, lib/inngest/functions/catalog-audit.ts, getCatalogAuditSuggestions (~line 219). Today a
non-empty store returns [] and app/onboarding/page.tsx never re-polls, so real-store onboarding spins
forever. Replace the `return [];` branch with a synchronous, deterministic computation from the mirror
— no LLM call:
  - select the user's shopify_products (limit 200, user_id filtered via serviceDb) plus their variants;
  - compute three counts: products with a null/empty meta_description; products with a null/empty
    body_html or a body_html under 120 characters; products whose title suggests a placeholder
    (contains "Default Title");
  - emit one WorkflowSuggestion per non-zero count, reusing the exact WorkflowSuggestion shape already
    returned by emptyStoreSuggestions() (same field names, domain and level values) with the count
    interpolated into the description, e.g. "12 products have no meta description";
  - when all three counts are zero, fall back to emptyStoreSuggestions() rather than returning [] —
    an empty list is what causes the hang.
  Keep the existing try/catch that returns [] when the DB is unavailable in test/build context, and
  keep the Inngest catalogAudit function itself unchanged. Extend tests/unit/catalog-audit.test.ts
  with a case asserting a non-empty store with meta gaps yields at least one suggestion.

Part C — WS12 honesty fixes:
  - lib/integrations/adapter.ts lines 9-21: the docblock says "Phase 1: Shopify + Gmail are
    compile-only skeletons" and "Phase 1 skeletons always return false / throw 'Not implemented until
    Phase 2'". Both are false. Rewrite it to describe the shipped behavior: real Shopify and Gmail
    adapters implement this interface, isHealthy performs a live token ping, and the Shopify adapter
    additionally exposes isSimulated() for the sandbox sentinel-token path.
  - lib/integrations/gmail/client.ts line ~99: `provider_account_id: userId` with a placeholder
    comment. Fetch the connected mailbox address after the token exchange (call
    gmail.users.getProfile({ userId: "me" }) with the freshly built client, or read the `email` claim
    from the id_token when present) and store that address; fall back to the existing userId
    placeholder only if the lookup fails, and log a structured warning when it does. The onConflict
    update branch must also set provider_account_id when a real address was resolved.
  - README.md line 32: "An Orchestrator reads each request and routes it to one of four specialist
    agents: Content, SEO, Q&A, and Inventory." No routing exists — threads.agent_context is always
    'orchestrator'. Rewrite to describe the real design: a single Orchestrator agent carrying four
    embedded domain playbooks (Catalog, SEO, Q&A, Inventory), with per-domain specialist agents named
    as a v2 direction. Line 51: change the LLM stack row to "Google Gemini (primary, via Google AI
    Studio) · Anthropic Claude and Groq supported via MODEL_PROFILE — all through the Vercel AI SDK".
    Line 79: update the prerequisites sentence to name a Google AI Studio key first. Line 116 v1
    bullet: keep "one Orchestrator chat" but drop any implication of four separate agents.
  - CLAUDE.md, the Constraints block: update the locked tech-stack line's LLM clause from "Anthropic
    Claude (Opus-class primary) with Groq (fast path for classification)" to name Google Gemini as
    the primary with Anthropic and Groq retained as MODEL_PROFILE alternatives, keeping the "both via
    the Vercel AI SDK" framing.
  - vercel.json currently contains only $schema and regions. Add a `functions` entry raising
    maxDuration for the chat route to 120 and the Inngest route to 300, mirroring the values already
    declared in code (app/api/chat/[threadId]/send/route.ts exports maxDuration 120;
    app/api/inngest/route.ts exports 300), so the platform config and the code agree.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/unit/settings.test.ts tests/unit/catalog-audit.test.ts</automated>
    <automated>grep -n "isSandboxClaims(claims)" app/app/settings/actions.ts | wc -l | grep -qv '^5$'</automated>
    <automated>! grep -qi "four specialist agents" README.md && ! grep -q "compile-only skeletons" lib/integrations/adapter.ts</automated>
  </verify>
  <done>Sandbox visitors cannot disconnect the demo integration; real-store onboarding always receives suggestions; the adapter docblock, Gmail provider_account_id, README, CLAUDE.md and vercel.json describe the system as built.</done>
</task>

</tasks>

<verification>
1. `npm run typecheck` — zero errors.
2. `npm test` — full suite green, count not reduced. tests/unit/seed-registry.test.ts is new and must
   pass; update (never delete) any existing assertion that encoded the old seed shape.
3. `npm run build` — production build succeeds.
4. Per-task grep gates above, in particular the fake-tool grep and the dangling-GID grep.

OUT OF SCOPE for this executor (orchestrator handles after merge): running the seed against the prod
demo user, Vercel deploy, prod browser smoke test, and setting MODEL_PROFILE=google once the Google AI
Studio key is supplied.
</verification>

<success_criteria>
- Every seeded and onboarding workflow step names a real tool and carries schema-valid params, enforced
  by a test.
- The demo dataset backs every story the UI tells: orders for the inventory and Q&A threads, pages and
  redirects for the SEO tools, embeddings for semantic recall, real GIDs everywhere, a non-empty
  archived thread, healthy sync state.
- A reseed leaves no stale rows behind.
- A sandbox visitor cannot destroy the demo.
- Onboarding a real store never hangs.
- README, CLAUDE.md, the adapter docblock and vercel.json are all true.
- typecheck, full vitest suite and production build all pass.
</success_criteria>

<output>
Write `.planning/quick/260727-gwq-demo-readiness-sweep-agent-harness-chat-/260727-gwq-PLAN-3-SUMMARY.md` on
completion, recording: the final tool+params mapping for the ten seeded workflows, the embedding-seeding
approach actually used (batched vs paced) and its failure behavior, the onboarding starter step policy
(no concrete GIDs), and any tests whose assertions were intentionally changed.
</output>
</content>
