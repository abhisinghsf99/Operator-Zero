---
phase: quick-260531-w26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agent/generation/optimize-meta.ts
  - tests/unit/optimize-meta.test.ts
  - lib/integrations/shopify/mutations.ts
  - tests/unit/shopify-mutations.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Meta approval shows a generated title + description and writes live product SEO (no longer null) — Groq's ```json-fenced JSON object now parses to {meta_title, meta_description}"
    - "parseMeta extracts the JSON object WITHOUT deleting its contents: bare JSON, ```json-fenced, ```-fenced, and JSON-with-preamble all parse; true garbage falls back to line-based; truly-empty still throws"
    - "Restock approval shows target qty and sets live on-hand with a real before→after activity — inventorySetOnHandQuantities no longer throws 'must include changeFromQuantity'"
    - "updateInventory reads current on_hand at the resolved level and passes changeFromQuantity (compare-and-set) in each setQuantities entry, preserving all quick-260531-vhh behavior"
    - "npm run typecheck passes (TS strict, no `any` — GraphQL quantities response is typed); existing + new unit tests in both suites stay green"
  artifacts:
    - path: "lib/agent/generation/optimize-meta.ts"
      provides: "Robust parseMeta (balanced-JSON-substring extraction → fence-delimiter-only strip → line-based fallback) + unchanged throw-if-empty guard"
      contains: "meta_title"
    - path: "tests/unit/optimize-meta.test.ts"
      provides: "Cases (a) bare JSON, (b) ```json-fenced, (c) ```-fenced, (d) JSON-with-preamble, (e) garbage→line-fallback, (f) empty→throws"
      contains: "json"
    - path: "lib/integrations/shopify/mutations.ts"
      provides: "updateInventory resolution query reads quantities(names:[\"on_hand\"]); setQuantities carries changeFromQuantity=currentOnHand"
      contains: "changeFromQuantity"
    - path: "tests/unit/shopify-mutations.test.ts"
      provides: "Resolution mocks return on_hand quantities; assertion that set is called with changeFromQuantity === resolved currentOnHand"
      contains: "changeFromQuantity"
  key_links:
    - from: "generateOptimizedMeta"
      to: "parseMeta"
      via: "result.text → {meta_title, meta_description}, throw if either empty"
      pattern: "parseMeta\\(result\\.text\\)"
    - from: "updateInventory resolution query"
      to: "inventorySetOnHandQuantities setQuantities"
      via: "currentOnHand read from quantities(names:[\"on_hand\"]) → changeFromQuantity"
      pattern: "changeFromQuantity"
---

<objective>
Two independent, already-diagnosed live-write fixes so the meta (#1) and restock (#3) workflows complete end-to-end. No re-derivation — both root causes are confirmed live.

- BUG C (meta): `parseMeta` deletes JSON content when Groq wraps the object in ```json fences, producing empty output → "Generated meta was empty after parsing"; live SEO stayed null.
- BUG D (restock): `inventorySetOnHandQuantities` (API 2024-10) requires `changeFromQuantity` per `setQuantities` entry; the set threw and inventory stayed 0 with `after_state` null.

Purpose: Restore live writes for both workflows without touching the frozen engine, runtime, tools, preview renderer, or seed files.
Output: 2 source fixes + 2 test updates. Scope is exactly these 4 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Both files already read in planning; do not re-explore. -->

optimize-meta.ts (current parseMeta, ~L160-200) — the bug is line ~164:
```
text = text.replace(/```[\s\S]*?```/g, "").replace(/`[^`]+`/g, "").trim();
```
This deletes the ENTIRE fenced block (including the JSON inside). generateOptimizedMeta keeps:
- `const { meta_title, meta_description } = parseMeta(result.text);`
- `if (!meta_title || !meta_description) throw new Error("Generated meta was empty after parsing");`
- SEO guards: `meta_title.slice(0,60)`, `meta_description.slice(0,160)`, HTML-tag strip on both.
parseMeta is currently NOT exported (module-private). Existing tests drive it via generateOptimizedMeta + the `ai` generateText mock.

mutations.ts updateInventory (already fixed by quick-260531-vhh):
- Resolution query `GetVariantInventory($id)` → `productVariant.inventoryItem { id tracked inventoryLevels(first:1){ edges { node { location { id } } } } }`
- Resolves `inventoryItemId`, `locationId`, `tracked`; throws "could not resolve Shopify inventory item / location for variant …" when either is missing.
- Enables tracking via `inventoryItemUpdate(input:{tracked:true})` when `tracked === false`.
- Set mutation `inventorySetOnHandQuantities(input:{ reason:"correction", setQuantities:[{ inventoryItemId, locationId, quantity }] })` — MISSING `changeFromQuantity`.
- userErrors checks throw on every mutation; re-read GetVariant → upsert mirror → backfillAfterState; writeActivity-before-effect; idempotency; MutationResult shape; user_id scoping. ALL must be preserved.

shopify-mutations.test.ts — the inventory mocks already branch on query string and return the resolution shape with `inventoryItem.inventoryLevels`. They currently do NOT include `quantities`. `makeInventoryAdapter` (in the "Bug B fix" describe) and the two inline inventory mocks (WF-06 + after_state backfill describes) are the resolution mocks to extend.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Robust parseMeta (BUG C) + tests</name>
  <files>lib/agent/generation/optimize-meta.ts, tests/unit/optimize-meta.test.ts</files>
  <behavior>
    - bare JSON `{"meta_title":"X","meta_description":"Y"}` → {X, Y}
    - ```json-fenced JSON → {X, Y} (fence no longer deletes content)
    - ```-fenced (no lang) JSON → {X, Y}
    - JSON with leading preamble prose then the object → {X, Y}
    - non-JSON garbage (e.g. "Title line\nDescription line") → line-based fallback, both non-empty
    - text that yields empty title or empty description after all strategies → generateOptimizedMeta throws "Generated meta was empty after parsing"
    - existing cases stay green: (d) length truncation 60/160, HTML-tag strip, undefined usage, hard/soft cap, resolveModel("DRAFTER")
  </behavior>
  <action>
Rewrite parseMeta in lib/agent/generation/optimize-meta.ts to be robust WITHOUT deleting content. Remove the content-deleting first line `text = text.replace(/```[\s\S]*?```/g, "")...`. Apply strategies in this order, stopping at the first that yields a parseable object with at least one of meta_title/meta_description:
  1) PRIMARY: extract the first balanced JSON object substring — locate the first `{` and the last `}` in raw, slice that substring, JSON.parse it; read string meta_title / meta_description.
  2) If that yields nothing parseable: strip ONLY the fence DELIMITER markers (a leading fence with optional language label and a trailing fence) preserving inner text, then JSON.parse the remaining text and read the two keys.
  3) LAST RESORT: keep the existing line-based extraction (first non-empty trimmed line → title; remaining lines joined → description, defaulting description to title if absent).
Keep the existing HTML-tag strip (`replace(/<[^>]+>/g,"")`) + whitespace-collapse on both values, and the SEO length guards (meta_title ≤60, meta_description ≤160). Do NOT change generateOptimizedMeta's throw-if-empty behavior, the cost-cap gate, resolveModel("DRAFTER"), or recordCost ordering. Do NOT bump maxOutputTokens (parser robustness is the fix). parseMeta may stay private — drive new tests via generateOptimizedMeta with the existing `ai` generateText mock (mirror existing tests). TypeScript strict, no `any` — type the parsed object as `Record<string, unknown>` and narrow with `typeof === "string"` (existing pattern).
Add tests to tests/unit/optimize-meta.test.ts covering the six behavior cases above by setting `mockGenerateText.mockResolvedValue({ text: <case input>, usage: {...} })`. For the ```json-fenced case use a template string like "```json\n" + DEFAULT_META_JSON + "\n```". Assert parsed title/description equal the embedded values for cases a–d, non-empty for case e, and `.rejects.toThrow(/empty after parsing/)` for case f (e.g. text `""` or `"{}"`). Keep every existing test unchanged and green.
  </action>
  <verify>
    <automated>cd /Users/abhisingh/my-os/dev/Operator-Zero && npx vitest run tests/unit/optimize-meta.test.ts && grep -n "replace(/\`\`\`\[\\\\s\\\\S\]\*?\`\`\`/" lib/agent/generation/optimize-meta.ts; test $? -ne 0 && echo "GUARD_OK: content-deleting fence regex removed"</automated>
  </verify>
  <done>optimize-meta.test.ts passes (existing + 6 new cases). optimize-meta.ts no longer contains the content-deleting `replace(/```[\s\S]*?```/...` pattern. ```json-fenced JSON parses to {meta_title, meta_description}; truly-empty still throws.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: updateInventory changeFromQuantity (BUG D) + tests</name>
  <files>lib/integrations/shopify/mutations.ts, tests/unit/shopify-mutations.test.ts</files>
  <behavior>
    - resolution query also reads current on_hand at the resolved level via quantities(names:["on_hand"])
    - currentOnHand = the on_hand quantities entry's quantity; defaults to 0 when absent/empty/null
    - inventorySetOnHandQuantities setQuantities[0] carries changeFromQuantity === currentOnHand
    - tracked=false still enables tracking before the set (vhh behavior preserved)
    - all existing updateInventory tests stay green: resolved IDs, resolution-before-set order, enable-tracking order, throw-on-null-variant, throw-on-userErrors, after_state backfill, writeActivity-before-effect
  </behavior>
  <action>
Edit updateInventory in lib/integrations/shopify/mutations.ts, preserving ALL quick-260531-vhh behavior (ID/location resolution, enable-tracking via inventoryItemUpdate when tracked:false, userErrors throws on every mutation, GetVariant re-read + upsert + backfillAfterState, writeActivity-before-effect ordering, idempotency key, MutationResult shape, public signatures, multi-tenant user_id scoping).
  1) Extend the GetVariantInventory resolution query: inside `inventoryItem { id tracked inventoryLevels(first:1){ edges { node { location { id } ... } } } }`, add `quantities(names:["on_hand"]){ name quantity }` to the level node. Extend the typed response: the level node becomes `{ location: { id: string }; quantities: Array<{ name: string; quantity: number }> }` (no `any`).
  2) Compute `const currentOnHand = resolutionData.productVariant?.inventoryItem?.inventoryLevels?.edges?.[0]?.node.quantities?.find(q => q.name === "on_hand")?.quantity ?? 0;` — default 0 if the level, the array, or the on_hand entry is absent/null.
  3) Add `changeFromQuantity: currentOnHand` to the single setQuantities entry alongside the existing inventoryItemId/locationId/quantity.
Do not touch updateProduct, the idempotency helpers, or any other export.
Update tests/unit/shopify-mutations.test.ts: extend every updateInventory resolution mock so the GetVariantInventory branch returns `quantities: [{ name: "on_hand", quantity: 0 }]` (or a chosen baseline) on the level node — this includes `makeInventoryAdapter` (Bug B describe), the WF-06 `inventoryGraphQLSpy`, and the after_state-backfill `MockShopifyAdapterInvBackfill`. Add an assertion in the "set mutation is called with RESOLVED …" test (or a new sibling test) that `setQuantities[0].changeFromQuantity === currentOnHand` (the resolved on_hand baseline, e.g. 0). Keep all existing updateProduct/updateInventory/after_state tests green (they branch on query string).
TypeScript strict, no `any`.
  </action>
  <verify>
    <automated>cd /Users/abhisingh/my-os/dev/Operator-Zero && npx vitest run tests/unit/shopify-mutations.test.ts && grep -q "changeFromQuantity" lib/integrations/shopify/mutations.ts && echo "GUARD_OK: changeFromQuantity present in setQuantities"</automated>
  </verify>
  <done>shopify-mutations.test.ts passes (existing + changeFromQuantity assertion). mutations.ts updateInventory setQuantities includes changeFromQuantity sourced from the resolved on_hand baseline; the resolution query reads quantities(names:["on_hand"]); all vhh behavior intact.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes (TS strict, no `any`; GraphQL quantities response typed).
- `npx vitest run tests/unit/optimize-meta.test.ts tests/unit/shopify-mutations.test.ts` passes — existing + new.
- Grep guards: optimize-meta.ts no longer contains the content-deleting `replace(/```[\s\S]*?```/...` pattern; mutations.ts updateInventory setQuantities includes `changeFromQuantity`.
- Manual (orchestrator re-runs live smoke): meta approval shows generated title+description and writes live SEO; restock approval shows target qty and sets live on-hand with before→after activity.
</verification>

<success_criteria>
- Both bugs fixed in their respective source files; no other files modified.
- ```json-fenced JSON parses correctly; restock set carries changeFromQuantity.
- Full typecheck + both test suites green.
</success_criteria>

<output>
Create `.planning/quick/260531-w26-fix-meta-json-fence-parsing-restock-chan/260531-w26-SUMMARY.md` when done.
</output>
