---
phase: 03-ownership-the-portfolio
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/app/workflows/page.tsx
  - app/app/workflows/_workflows-view.tsx
  - components/workflows/workflow-row.tsx
  - components/workflows/workflow-group.tsx
  - components/workflows/inline-editable-text.tsx
  - components/workflows/recent-activity-strip.tsx
  - components/workflows/workflow-search.tsx
autonomous: true
requirements: [WF-07, WF-08, WF-09, WF-10]
user_setup: []

must_haves:
  truths:
    - "Visiting /app/workflows shows the user's workflows grouped by status: Scheduled / Triggered / Manual / Paused / Drafts"
    - "Each workflow row has an inline L1/L2/L3 level toggle that saves immediately; switching to L3 shows a one-time confirmation"
    - "Each workflow row can be paused/resumed without deleting it (history + config retained)"
    - "The recent-activity strip shows three real stats: Decisions outstanding (pending approvals), Ran while you slept (L3 last 12h), Time saved this week (labeled estimate)"
    - "Strip counts update live via Supabase Realtime without a page refresh"
    - "Find-a-workflow search filters the loaded list client-side by name/description/domain"
    - "+ New Workflow opens a new Conversation thread (no context_workflow_id)"
  artifacts:
    - path: "app/app/workflows/page.tsx"
      provides: "My Workflows RSC: parallel fetch of workflows + strip stats, grouping, onboarding gate"
      min_lines: 30
    - path: "components/workflows/workflow-row.tsx"
      provides: "WorkflowRow with LevelToggle (L3 confirm), pause/resume, status dot"
    - path: "components/workflows/recent-activity-strip.tsx"
      provides: "3-stat strip with Realtime subscription"
  key_links:
    - from: "app/app/workflows/page.tsx"
      to: "lib/workflows/grouping.ts"
      via: "groupWorkflowsByStatus"
      pattern: "groupWorkflowsByStatus"
    - from: "components/workflows/workflow-row.tsx"
      to: "lib/actions/workflows.ts"
      via: "editWorkflow / togglePause Server Actions"
      pattern: "editWorkflow|togglePause"
    - from: "components/workflows/recent-activity-strip.tsx"
      to: "activity_entries / approvals"
      via: "Realtime postgres_changes (activity:<userId>)"
      pattern: "postgres_changes"
---

<objective>
Deliver the My Workflows landing surface (WF-07/08/09/10) — the new default home. Workflows grouped by status, inline L1/L2/L3 toggle with L3 confirm, pause/resume, the three-stat recent-activity strip with live Realtime counts, client-side fuzzy search, and "+ New Workflow" → Conversation.

Purpose: Sarah's portfolio becomes visible and directly manageable. This is the surface success-criterion 2 measures (60% visit My Workflows 3×/wk) — the calm "anti-dashboard" portfolio.
Output: /app/workflows route + its client view and components, all wired to plan 01's grouping lib, editWorkflow/togglePause Server Actions, and Realtime infra.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ownership-the-portfolio/03-CONTEXT.md
@.planning/phases/03-ownership-the-portfolio/03-RESEARCH.md
@.planning/phases/03-ownership-the-portfolio/03-PATTERNS.md

<interfaces>
<!-- Contracts CONSUMED from plan 03-01 (already built). Use directly — do not reimplement. -->
lib/workflows/grouping.ts: groupWorkflowsByStatus(workflows) -> { scheduled, triggered, manual, paused, drafts }
lib/actions/workflows.ts: editWorkflow(workflowId, patch) ; togglePause(workflowId)  -> { success: true } | { error: string }
</interfaces>

# UI design contract (canonical — read as visual/layout spec)
@Operator Zero Design Files/surface-workflows.jsx
@Operator Zero Design Files/components.jsx
@Operator Zero Design Files/icons.jsx

# Analogs (read before implementing — excerpts in 03-PATTERNS.md)
@app/app/settings/page.tsx
@components/chat/inline-approval-card.tsx
@components/layout/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: My Workflows RSC route + grouped view + recent-activity strip (Realtime)</name>
  <files>app/app/workflows/page.tsx, app/app/workflows/_workflows-view.tsx, components/workflows/workflow-group.tsx, components/workflows/recent-activity-strip.tsx, components/workflows/workflow-search.tsx</files>
  <read_first>
    - Operator Zero Design Files/surface-workflows.jsx (CANONICAL layout: status grouping, recent-activity strip, "what just happened" ticker, WorkflowGroup section headers, empty state)
    - Operator Zero Design Files/components.jsx (SectionHeader, SurfaceHeader, Card, StatusDot, Badge primitives) + icons.jsx
    - app/app/settings/page.tsx lines 1-68 (RSC shell: getOrCreateProfile onboarding gate, Promise.all parallel fetch, px-10 py-7 header + max-w container, data-testid, OKLCH token classes)
    - components/chat/inline-approval-card.tsx lines 136-181 (canonical Realtime: createBrowserClient + getSession + setAuth + channel.subscribe + cancelled-flag cleanup with removeChannel)
    - components/chat/message-stream.tsx lines 82-104 (public-channel removeChannel cleanup variant — use removeChannel, not unsubscribe)
    - lib/workflows/approvals.ts (pending-approvals count source for "Decisions outstanding")
    - lib/workflows/grouping.ts (plan 01 — groupWorkflowsByStatus)
    - lib/db/schema/workflows.ts, lib/db/schema/activity-entries.ts (status, automation_level, occurred_at, action_type columns)
    - 03-RESEARCH.md Discretion 5 (time-saved minutes-per-action-type constants, labeled "estimated"), Pattern 7 (Realtime strip), Resolved Discretion 1 (RSC fetch + Realtime)
  </read_first>
  <action>
    Create `app/app/workflows/page.tsx` as an RSC mirroring settings/page.tsx: getOrCreateProfile onboarding gate, then Promise.all to fetch workflows (filtered by user_id), pending-approvals count (lib/workflows/approvals.ts), L3-actions-last-12h count, and a 7-day per-action-type time-saved aggregate (RESEARCH Discretion 5 constants; render with an "estimated" label per D-15). Group via groupWorkflowsByStatus. Pass data to `_workflows-view.tsx` client shell. Use the surface-workflows.jsx layout + OKLCH token classes; header in the px-10 py-7 / max-w pattern.
    Create `_workflows-view.tsx` ("use client") rendering: SurfaceHeader, `recent-activity-strip`, `workflow-search`, then a `workflow-group` per non-empty status bucket in the design-file order (Scheduled / Triggered / Manual / Paused / Drafts), and the empty state from the design file when the portfolio is empty.
    Create `recent-activity-strip.tsx` ("use client") rendering the three stats (Decisions outstanding / Ran while you slept / Time saved this week — estimated label) and subscribing via Realtime to `activity:${userId}` (INSERT on activity_entries) and the approvals channel for live count updates using the canonical createBrowserClient+setAuth+cancelled-flag+removeChannel pattern. Also render the "what just happened" ticker from the latest entries.
    Create `workflow-group.tsx` (section header + list of WorkflowRow — receives the bucket + a status label) and `workflow-search.tsx` ("+ Find a workflow" client-side fuzzy filter over name/description/domain per D-17, lifting filtered state to the view).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/unit/workflows/grouping.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/app/workflows/page.tsx` imports and calls `groupWorkflowsByStatus` (`grep -c groupWorkflowsByStatus app/app/workflows/page.tsx` ≥ 1)
    - `recent-activity-strip.tsx` subscribes to Realtime (`grep -c "postgres_changes\|setAuth" components/workflows/recent-activity-strip.tsx` ≥ 1) and renders an "estimated" label for time-saved (`grep -ci estimated components/workflows/recent-activity-strip.tsx` ≥ 1)
    - cleanup uses removeChannel (`grep -c removeChannel components/workflows/recent-activity-strip.tsx` ≥ 1)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>/app/workflows renders grouped workflows + live strip + search; onboarding-gated RSC; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: WorkflowRow with inline level toggle (L3 confirm), pause/resume, inline-editable name, + New Workflow</name>
  <files>components/workflows/workflow-row.tsx, components/workflows/inline-editable-text.tsx</files>
  <read_first>
    - Operator Zero Design Files/surface-workflows.jsx (WorkflowRow visual: LevelToggle, pause/resume control, status dot, row layout)
    - Operator Zero Design Files/components.jsx (LevelToggle, StatusDot, Button primitives)
    - components/chat/inline-approval-card.tsx lines 186-203 (async mutation with isPending guard + try/catch + error state) and lines 567-572 (accessible disabled button: aria-label, opacity/cursor)
    - components/ui/dialog.tsx (Dialog/DialogContent/DialogFooter — for the L3 one-time confirm)
    - 03-RESEARCH.md "Inline Edit Click-to-Edit Pattern" code example (blur+Enter saves, Esc cancels, no save button — Pitfall 3) and Pattern for editWorkflow
    - lib/actions/workflows.ts (plan 01 — editWorkflow, togglePause)
    - app/app/chat/page.tsx (existing — analog for "new thread" creation path that "+ New Workflow" reuses; new thread carries NO context_workflow_id per WF-10)
  </read_first>
  <behavior>
    - LevelToggle: clicking L1/L2/L3 calls editWorkflow with automation_level patch and saves immediately (WF-08); selecting L3 first shows a one-time confirmation dialog and only saves on confirm
    - pause/resume: calls togglePause; status flips active<->paused; row stays in list (WF-09)
    - inline name edit: click → input → Enter/blur saves via editWorkflow, Esc cancels; no double-save on blur+click (Pitfall 3)
    - + New Workflow: navigates to a new Conversation thread with no workflow context (WF-10)
  </behavior>
  <action>
    Create `inline-editable-text.tsx` ("use client") per RESEARCH code example: click-to-edit span → autoFocus input, Enter/blur saves (only if changed) via an injected onSave async handler, Escape reverts; isPending guard; no separate Save button (Pitfall 3 — blur fires once).
    Create `workflow-row.tsx` ("use client") matching the surface-workflows.jsx WorkflowRow: status dot, name (optionally inline-editable on the list — keep full inline edit to Detail per D-01, list shows name as a link to /app/workflows/[id]), a LevelToggle that calls `editWorkflow(id, { automation_level })` with optimistic UI + isPending guard, and a one-time L3 confirm dialog (components/ui/dialog.tsx) gating the L3 selection (WF-08), and a pause/resume control calling `togglePause(id)` (WF-09). Accessible disabled states (aria-label, opacity/cursor) per the inline-approval-card pattern. Wire "+ New Workflow" (in the view header) to the existing new-thread creation path with no context_workflow_id (WF-10).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "editWorkflow\|togglePause" components/workflows/workflow-row.tsx` ≥ 2 (level toggle + pause/resume both wired)
    - L3 confirmation present: `grep -ci "L3" components/workflows/workflow-row.tsx` ≥ 1 and a Dialog import (`grep -c "@/components/ui/dialog" components/workflows/workflow-row.tsx` ≥ 1)
    - inline-editable-text saves on Enter and cancels on Escape (`grep -c "Enter\|Escape" components/workflows/inline-editable-text.tsx` ≥ 2) with no separate save button
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>WorkflowRow with working level toggle (L3 confirm), pause/resume, inline-edit primitive, and + New Workflow → Conversation; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | level/pause patches carry workflowId from the browser |
| browser → Supabase Realtime | strip subscribes to activity:<userId> + approvals channels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02-01 | Spoofing/Elevation | editWorkflow / togglePause from a row | mitigate | Server Actions (plan 01) verify workflow ownership via withUserRls + user_id filter; UI cannot bypass |
| T-3-02-02 | Information Disclosure | recent-activity-strip Realtime subscription | mitigate | activity:<userId> RLS policy (migration 0005) verifies userId==auth.uid() |
| T-3-02-03 | Tampering | Realtime double-subscription leak (StrictMode) | accept→mitigate | cancelled-flag + removeChannel cleanup pattern (Phase 2) prevents duplicate state updates (RESEARCH Pitfall 2) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run tests/unit/` green (grouping logic backs WF-07)
- Manual (per 03-VALIDATION.md): Realtime strip counts update live across two tabs; level toggle L3 confirm; pause/resume retains the row
</verification>

<success_criteria>
- /app/workflows is the visible default landing surface, grouped by status with the recent-activity strip (WF-07)
- Inline L1/L2/L3 toggle saves immediately, L3 one-time confirm (WF-08)
- Pause/resume works without deleting (WF-09)
- + New Workflow opens a Conversation thread (WF-10)
- Search filters client-side (D-17); strip stats live + estimated label (D-15)
</success_criteria>

<output>
Create `.planning/phases/03-ownership-the-portfolio/03-02-SUMMARY.md` when done
</output>
