---
phase: 02-foundation-prove-the-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components.json
  - lib/utils.ts
  - app/globals.css
  - app/layout.tsx
  - app/app/layout.tsx
  - components/ui/button.tsx
  - components/ui/badge.tsx
  - components/ui/card.tsx
  - components/ui/input.tsx
  - components/ui/dialog.tsx
  - components/ui/sonner.tsx
  - components/layout/sidebar.tsx
  - components/layout/bottom-tabs.tsx
autonomous: false
requirements: [CONV-03]
must_haves:
  truths:
    - "App renders with the OKLCH paper palette from the design file (light + dark theme tokens)"
    - "shadcn/ui primitives (Button, Badge, Card, Input, Dialog) exist in components/ui and consume the design tokens"
    - "App shell shows desktop sidebar nav and mobile bottom-tab nav (5 core surfaces)"
    - "Toast notifications (Sonner) and Framer Motion are installed and usable"
  artifacts:
    - path: "lib/utils.ts"
      provides: "cn() class-merge utility"
      contains: "export function cn"
    - path: "app/globals.css"
      provides: "OKLCH tokens wired into Tailwind v4 @theme inline"
      contains: "@theme inline"
    - path: "components/layout/sidebar.tsx"
      provides: "Desktop nav chrome"
      min_lines: 25
    - path: "components/layout/bottom-tabs.tsx"
      provides: "Mobile bottom-tab nav (5 surfaces)"
      min_lines: 20
  key_links:
    - from: "app/layout.tsx"
      to: "globals.css + Geist/Instrument Serif fonts"
      via: "import + next/font or <link>"
      pattern: "globals\\.css|Geist"
    - from: "components/ui/*.tsx"
      to: "lib/utils.ts"
      via: "cn() import"
      pattern: "from \"@/lib/utils\""
---

<objective>
Stand up the UI foundation so every Phase 2 surface can be built against a real component system. Install shadcn/ui + the locked UI packages, translate the OKLCH design tokens from the design file into Tailwind v4 `@theme inline`, wire fonts, and build the app-shell nav chrome (desktop sidebar + mobile bottom tabs).

Purpose: `components/` is currently empty. RESEARCH.md is explicit that this MUST precede all surface work — no Conversation, Onboarding, or Settings UI can be built without it. This plan unblocks Waves 3-4.
Output: `lib/utils.ts` (`cn()`), `app/globals.css` (token theme), `components/ui/*` primitives, `components/layout/sidebar.tsx` + `bottom-tabs.tsx`, installed packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-prove-the-agent/02-RESEARCH.md
@.planning/phases/02-foundation-prove-the-agent/02-PATTERNS.md

<interfaces>
<!-- Token source: Operator Zero Design Files/index.html lines 14-139 (:root, [data-density], [data-theme="dark"]) -->
<!-- Component visual contract: Operator Zero Design Files/components.jsx (Button, Badge, Card, LevelToggle, Sidebar) -->
<!-- App shell + nav routing contract: Operator Zero Design Files/main.jsx -->
Current app/globals.css imports Tailwind defaults; replace with the OKLCH palette.
Current app/layout.tsx wires fonts via no custom font; add Geist + Geist Mono + Instrument Serif.
Current app/app/layout.tsx renders a minimal header; replace with sidebar + bottom-tabs shell.
The 5 core surface routes for nav: /app/chat (Chat), /app/workflows (Workflows — Phase 3 placeholder OK), /app/approvals (Approvals — Phase 4 placeholder OK), /app/activity (Activity — Phase 3 placeholder OK), /app/settings (More/Settings).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify and install the 10 Phase 2 npm packages</name>
  <read_first>
    - .planning/phases/02-foundation-prove-the-agent/02-RESEARCH.md (## Package Legitimacy Audit; ## Standard Stack → New Installs)
    - package.json (current dependency set — confirm none already present)
  </read_first>
  <action>Present the 10 [ASSUMED] packages for human verification against their official registry/repo, then on approval run the single `npm install` command below. Do NOT install before approval (T-2-01-SC).</action>
  <what-built>
    The executor will install: `@shopify/shopify-api@13`, `googleapis@172`, `framer-motion@12`, `sonner@2`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `react-markdown`, `zustand`. All 10 are tagged [ASSUMED] in RESEARCH.md (slopcheck unavailable). T-2-01-SC requires human confirmation before install.
  </what-built>
  <how-to-verify>
    Confirm each package name on its registry page before approving:
    1. npmjs.com/package/@shopify/shopify-api — official Shopify org (github.com/Shopify/shopify-app-js)
    2. npmjs.com/package/googleapis — official Google org
    3. npmjs.com/package/framer-motion — github.com/framer/motion (confirm name is `framer-motion`, NOT `motion`)
    4. npmjs.com/package/sonner, /class-variance-authority, /clsx, /tailwind-merge, /lucide-react, /react-markdown, /zustand
    Then approve to run: `npm install @shopify/shopify-api googleapis framer-motion sonner class-variance-authority clsx tailwind-merge lucide-react react-markdown zustand`
  </how-to-verify>
  <acceptance_criteria>
    - All 10 packages appear in package.json dependencies after install
    - `npx tsc --noEmit` still passes (no type breakage from new deps)
  </acceptance_criteria>
  <resume-signal>Type "approved" after verifying package names, or name any package to reject</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui, translate OKLCH tokens, wire fonts</name>
  <files>components.json, lib/utils.ts, app/globals.css, app/layout.tsx, components/ui/button.tsx, components/ui/badge.tsx, components/ui/card.tsx, components/ui/input.tsx, components/ui/dialog.tsx, components/ui/sonner.tsx</files>
  <read_first>
    - Operator Zero Design Files/index.html (lines 14-139: all :root tokens, [data-density], [data-theme="dark"])
    - Operator Zero Design Files/components.jsx (Button/Badge/Card visual targets)
    - app/globals.css (file being replaced)
    - app/layout.tsx (file being extended for fonts)
    - .planning/phases/02-foundation-prove-the-agent/02-RESEARCH.md (Area 1: UI Foundation; State of the Art → shadcn v4 data-slot)
  </read_first>
  <action>
    Run `npx shadcn@latest init` (creates components.json, lib/utils.ts with `cn()` using clsx + tailwind-merge, scaffolds components/ui/). In app/globals.css, after `@import "tailwindcss"`, declare every CSS custom property from index.html `:root` (--bg, --bg-elevated, --bg-subtle, --bg-deeper, --bg-inset, --border, --border-strong, --border-hairline, --text, --text-secondary, --text-tertiary, --text-faint, all --acc-* groups, --success, --warning, --danger, --r-* radii, --shadow-* ), the [data-density="compact"|"comfortable"] overrides, and the [data-theme="dark"] block verbatim. Map them into a `@theme inline` block (--color-bg → var(--bg), --color-text, --color-border, --color-acc-workflow, --color-success/warning/danger, --font-sans/serif/mono, --radius-sm/md/lg). Add Geist, Geist Mono, Instrument Serif via next/font/google in app/layout.tsx (replace the design file's `@import` font URL). Add `npx shadcn@latest add button badge card input dialog sonner` — shadcn v4 components use data-slot (no manual forwardRef). Mount `<Toaster />` from components/ui/sonner.tsx in app/layout.tsx.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "@theme inline" app/globals.css && grep -q "oklch" app/globals.css && test -f lib/utils.ts && grep -q "export function cn\|export const cn" lib/utils.ts</automated>
  </verify>
  <acceptance_criteria>
    - app/globals.css contains `@theme inline` and OKLCH token values for both light and dark (`[data-theme="dark"]`)
    - lib/utils.ts exports `cn()`
    - components/ui/{button,badge,card,input,dialog,sonner}.tsx exist
    - app/layout.tsx loads Geist + Instrument Serif fonts and mounts the Sonner Toaster
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Design tokens render through Tailwind utilities; shadcn primitives compile; fonts load.</done>
</task>

<task type="auto">
  <name>Task 3: Build app-shell nav chrome (desktop sidebar + mobile bottom tabs)</name>
  <files>components/layout/sidebar.tsx, components/layout/bottom-tabs.tsx, app/app/layout.tsx</files>
  <read_first>
    - Operator Zero Design Files/main.jsx (app shell + sidebar nav + surface routing contract)
    - Operator Zero Design Files/index.html (nav chrome styling)
    - app/app/layout.tsx (file being extended)
    - components/ui/button.tsx (just-created primitive to reuse)
  </read_first>
  <action>
    Create components/layout/sidebar.tsx (desktop nav: 5 entries — Chat→/app/chat, Workflows→/app/workflows, Approvals→/app/approvals, Activity→/app/activity, More→/app/settings; lucide-react icons; active-state via usePathname; use bg-bg-subtle, border tokens; WCAG: each link has aria-label and visible focus ring). Create components/layout/bottom-tabs.tsx (`"use client"`, visible only below the md breakpoint, same 5 tabs, role="navigation", keyboard-focusable). Extend app/app/layout.tsx to render the sidebar (hidden below md) + bottom-tabs (hidden at/above md) + `<main>` using `bg-bg` token. Respect `prefers-reduced-motion` on any hover transitions.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f components/layout/sidebar.tsx && test -f components/layout/bottom-tabs.tsx && grep -q "aria-label\|aria-current" components/layout/sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - sidebar.tsx and bottom-tabs.tsx exist and render the 5 core surfaces
    - Nav links carry aria-label or aria-current and a visible focus indicator (WCAG 2.1 AA)
    - app/app/layout.tsx composes sidebar (desktop) + bottom-tabs (mobile) + main
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>App shell shows responsive nav; the 5 surfaces are reachable; keyboard-navigable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Supply-chain risk on 10 new [ASSUMED] packages |
| browser DOM → app | UI chrome is non-privileged; no untrusted input crosses here yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01-SC | Tampering | npm install of 10 packages | mitigate | Blocking human checkpoint (Task 1) verifies each package name against its official registry/repo before install; framer-motion name explicitly disambiguated from `motion` |
| T-2-01-01 | Information Disclosure | next/font remote font fetch | accept | Fonts are public Google Fonts; no user data in request; self-hosted by next/font at build |
</threat_model>

<verification>
- `npx tsc --noEmit` passes after all tasks
- `npx vitest run` (existing Phase 1 suite) stays green — no regressions
- Manual: load `/app/home`, confirm paper palette renders and nav chrome is present desktop + mobile
</verification>

<success_criteria>
shadcn/ui + OKLCH tokens + fonts + responsive nav chrome exist and compile. All downstream surface plans can import from components/ui and components/layout.
</success_criteria>

<output>
Create `.planning/phases/02-foundation-prove-the-agent/02-01-SUMMARY.md` when done
</output>
