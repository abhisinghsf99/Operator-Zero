---
phase: 04-polish-effortless-daily-use
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema/user-sessions.ts
  - lib/db/schema/user-exports.ts
  - lib/db/schema/index.ts
  - supabase/migrations/0006_phase4_sessions_exports.sql
  - playwright.config.ts
  - package.json
  - tests/unit/approvals.test.ts
  - tests/unit/settings.test.ts
  - tests/unit/autonomy.test.ts
  - tests/unit/sessions.test.ts
  - tests/unit/export.test.ts
  - tests/unit/purge.test.ts
  - tests/e2e/a11y.spec.ts
  - tests/e2e/mobile.spec.ts
  - tests/e2e/keyboard.spec.ts
  - tests/e2e/approvals-inline.spec.ts
  - tests/e2e/perf.spec.ts
autonomous: false
requirements: [AUTH-04, AUTH-05, SET-06, UX-04]
user_setup: []

must_haves:
  truths:
    - "user_sessions table exists in the live database with RLS enforcing per-user isolation"
    - "user_exports table exists in the live database to surface export job status + signed URL"
    - "Performance indexes for the approvals pending-list query and My Workflows query exist in the live database"
    - "@axe-core/playwright is installed and a Playwright mobile-chrome project is configured"
    - "Failing Wave 0 test scaffolds exist for every Phase 4 requirement (Nyquist baseline)"
  artifacts:
    - path: "lib/db/schema/user-sessions.ts"
      provides: "user_sessions Drizzle table (AUTH-04/05) with RLS policy"
      contains: "userSessions"
    - path: "lib/db/schema/user-exports.ts"
      provides: "user_exports Drizzle table (SET-06 job status + signed URL)"
      contains: "userExports"
    - path: "supabase/migrations/0006_phase4_sessions_exports.sql"
      provides: "Forward-only migration: tables + RLS + perf indexes"
      contains: "user_sessions"
    - path: "playwright.config.ts"
      provides: "mobile-chrome project for UX-01 mobile parity tests"
      contains: "mobile-chrome"
    - path: "tests/unit/autonomy.test.ts"
      provides: "Failing scaffold for autonomy override one-directionality (D-06)"
      contains: "describe"
  key_links:
    - from: "lib/db/schema/index.ts"
      to: "lib/db/schema/user-sessions.ts"
      via: "barrel export"
      pattern: "export.*userSessions"
    - from: "supabase/migrations/0006_phase4_sessions_exports.sql"
      to: "live database"
      via: "supabase db push over session pooler (port 5432)"
      pattern: "user_sessions"
---

<objective>
Lay the data + test foundation for Phase 4: the `user_sessions` registry (AUTH-04/05), the `user_exports` job-status table (SET-06), performance indexes (UX-04), the `@axe-core/playwright` devDependency + a Playwright `mobile-chrome` project (UX-01/02), and ALL Wave 0 failing test scaffolds so every Phase 4 requirement has an automated verification baseline (Nyquist). Apply the migration to the live database so downstream slices verify against real schema.

Purpose: Phase 4's session/export/delete features and the a11y/mobile/perf pass cannot be built or verified without these tables, indexes, and test harness. Build/typecheck pass without the live push (Drizzle types come from schema files), so the push is mandatory to avoid a false-positive verification state.
Output: Two new schema files + barrel export, migration 0006 applied to live DB, Playwright mobile project + axe install, and failing test scaffolds for all phase requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-effortless-daily-use/04-RESEARCH.md
@.planning/phases/04-polish-effortless-daily-use/04-PATTERNS.md
@.planning/phases/04-polish-effortless-daily-use/04-VALIDATION.md

<interfaces>
<!-- Extracted from live code. Use directly — no exploration needed. -->

Drizzle table + RLS pattern (lib/db/schema/memory-items.ts — the closest analog):
  pgTable("name", { ...columns }, (table) => [ index(...), pgPolicy("name_user_policy", { as:"permissive", for:"all", to: authenticatedRole, using: sql`(SELECT auth.uid()) = ${table.user_id}`, withCheck: sql`(SELECT auth.uid()) = ${table.user_id}` }) ]).enableRLS()

Existing migrations: 0001…0005 applied. Next is 0006.
  Migration apply (project convention, NOT Supabase MCP): `supabase db push` over the session pooler (port 5432). MCP lacks project-write permission. Recorded precedent: [03-01] 0005 applied via `supabase db push --db-url <session-pooler>`.

Playwright config (playwright.config.ts) currently has ONE project: { name: "chromium", use: { ...devices["Desktop Chrome"] } }. vitest.config.mts excludes tests/e2e/**.

package.json scripts: test → vitest run; test:e2e → npx playwright test; typecheck → tsc --noEmit; lint → next lint; build → next build.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify @axe-core/playwright legitimacy before install</name>
  <read_first>
    - .planning/phases/04-polish-effortless-daily-use/04-RESEARCH.md (## Package Legitimacy Audit — @axe-core/playwright tagged [ASSUMED], slopcheck unavailable at research time)
  </read_first>
  <what-built>RESEARCH.md flagged `@axe-core/playwright` 4.11.3 as `[ASSUMED]` because slopcheck was unavailable at research time. This is the only new dependency in Phase 4. Per the package legitimacy gate, an [ASSUMED] package requires a blocking human verification before install.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@axe-core/playwright
    2. Confirm publisher is Deque Systems (dequelabs), source repo github.com/dequelabs/axe-core-npm, weekly downloads in the hundreds-of-thousands, and no postinstall script.
    3. Confirm version 4.11.3 exists.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the install in Task 2, or describe concerns.</resume-signal>
  <acceptance_criteria>
    - Human has confirmed @axe-core/playwright provenance (Deque Systems official package)
    - This checkpoint is NOT auto-approvable (workflow.auto_advance ignored per legitimacy gate)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add user_sessions + user_exports schema, barrel export, migration 0006, axe install, mobile Playwright project</name>
  <read_first>
    - lib/db/schema/memory-items.ts (exact analog for pgTable + index + pgPolicy + enableRLS — copy this structure)
    - lib/db/schema/index.ts (barrel export pattern — Phase 4 export goes after the autonomyThresholds export)
    - lib/db/schema/users.ts (user_profiles already has deletion_requested_at, avatar_url, display_name — do NOT re-add)
    - supabase/migrations/0004_realtime_authz.sql (migration header convention + RLS policy SQL form)
    - supabase/migrations/0005_activity_indexes.sql (CREATE INDEX IF NOT EXISTS partial-index form)
    - playwright.config.ts (current single-project config — add mobile-chrome alongside chromium)
    - .planning/phases/04-polish-effortless-daily-use/04-RESEARCH.md (§2 user_sessions schema, §3 user-exports rationale, §5 perf indexes, §6 mobile)
  </read_first>
  <action>
    Create `lib/db/schema/user-sessions.ts` exporting `userSessions` (table `user_sessions`): columns id (uuid pk defaultRandom), user_id (uuid notNull — FK to auth.users ON DELETE CASCADE declared in migration SQL), supabase_session_id (text), refresh_token_hash (text), device_label (text notNull), raw_ua (text), ip_geo_label (text), last_seen_at (timestamptz notNull defaultNow), created_at (timestamptz notNull defaultNow), revoked_at (timestamptz nullable). Add index `idx_user_sessions_user` on (user_id, last_seen_at) and pgPolicy `user_sessions_user_policy` (permissive, all, authenticatedRole, using/withCheck `(SELECT auth.uid()) = user_id`). Call `.enableRLS()`.
    Create `lib/db/schema/user-exports.ts` exporting `userExports` (table `user_exports`): columns id (uuid pk defaultRandom), user_id (uuid notNull), status (text notNull default 'pending' — values pending|ready|failed), signed_url (text nullable), object_path (text nullable), error (text nullable), created_at (timestamptz notNull defaultNow), completed_at (timestamptz nullable). Add index `idx_user_exports_user` on (user_id, created_at) and the same RLS policy shape (`user_exports_user_policy`). Call `.enableRLS()`.
    Add to `lib/db/schema/index.ts`: `export { userSessions } from "./user-sessions";` and `export { userExports } from "./user-exports";` under a `// Phase 4` comment.
    Create `supabase/migrations/0006_phase4_sessions_exports.sql` (forward-only, IF NOT EXISTS guards throughout): CREATE TABLE user_sessions + user_exports with the columns above, both with `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`, ALTER TABLE … ENABLE ROW LEVEL SECURITY, the two RLS policies, the two per-table indexes, plus the two performance indexes from RESEARCH §5: `idx_approvals_user_pending_stakes` ON approvals (user_id, stakes, created_at DESC) WHERE status='pending', and `idx_workflows_user_status` ON workflows (user_id, status, updated_at DESC). Add a header comment block noting "Closes: D-10 (session registry), SET-06 (export job status), UX-04 (perf indexes)".
    Run `npm install --save-dev @axe-core/playwright@4.11.3` (only after Task 1 approval).
    Edit `playwright.config.ts` to add a second project `{ name: "mobile-chrome", use: { ...devices["Pixel 7"] } }` alongside the existing chromium project (375px-class viewport for UX-01).
    Do NOT push the migration in this task — that is Task 4 ([BLOCKING]).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `lib/db/schema/user-sessions.ts` contains `export const userSessions = pgTable("user_sessions"` and a `pgPolicy("user_sessions_user_policy"` with `(SELECT auth.uid()) = ${table.user_id}`
    - `lib/db/schema/user-exports.ts` contains `export const userExports = pgTable("user_exports"` and status default `'pending'`
    - `lib/db/schema/index.ts` contains `export { userSessions }` and `export { userExports }`
    - `supabase/migrations/0006_phase4_sessions_exports.sql` contains `user_sessions`, `user_exports`, `ON DELETE CASCADE`, `idx_approvals_user_pending_stakes`, `idx_workflows_user_status`, and uses `IF NOT EXISTS` on every CREATE
    - `package.json` devDependencies contains `@axe-core/playwright`
    - `playwright.config.ts` contains `mobile-chrome`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Schema files, barrel export, migration 0006, axe install, and mobile Playwright project all present; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write Wave 0 failing test scaffolds for all Phase 4 requirements</name>
  <read_first>
    - .planning/phases/04-polish-effortless-daily-use/04-RESEARCH.md (## Validation Architecture — Phase Requirements → Test Map: the exact test files + -t filters per requirement)
    - tests/unit/l2-approval-flow.test.ts (existing approval-flow test style — vitest describe/it, serviceDb mocking conventions)
    - tests/unit/crypto.test.ts (existing encrypt/decrypt test style for SET-02 brand voice scaffold)
    - tests/e2e/settings-connections.spec.ts (existing Playwright spec style for the new e2e scaffolds)
    - vitest.config.mts (confirms tests/e2e/** excluded from vitest)
  </read_first>
  <behavior>
    - tests/unit/approvals.test.ts: failing it() for "pending list" (APRV-01 stakes-desc sort), "snooze" (APRV-02/06 sets status=snoozed + snoozed_until, does NOT fire approval.resolved), "reject reason" (APRV-02 stores memory), "bulk" (APRV-03 atomic, only pending rows), "snooze filter" (APRV-06 hidden by default), "revert" (APRV-07 canRevert ≤24h)
    - tests/unit/settings.test.ts: failing it() for "brand voice" (SET-02 saveBrandVoice encrypts, regenerate returns draft without saving)
    - tests/unit/autonomy.test.ts: failing it() for override one-directionality (D-06/SET-03 — L3 workflow + L2 override → effective L2; L2 workflow + L3 override → stays L2, never loosens)
    - tests/unit/sessions.test.ts: failing it() for AUTH-04 revokeSession sets revoked_at; AUTH-05 signOutEverywhere marks all rows revoked
    - tests/unit/export.test.ts: failing it() for SET-06 exportAccountData function returns signedUrl (mocked step.run)
    - tests/unit/purge.test.ts: failing it() for SET-07 purgeAccount cancelOn CEL `async.data.userId == event.data.userId`
    - tests/e2e/a11y.spec.ts, mobile.spec.ts, keyboard.spec.ts, approvals-inline.spec.ts, perf.spec.ts: failing/skipped scaffolds per UX-01…04 + APRV-04
  </behavior>
  <action>
    Create the unit + e2e scaffold files listed in `files_modified` matching the Validation Architecture test map. Each unit scaffold uses vitest `describe`/`it` with the exact `-t` filter strings from RESEARCH (e.g. "pending list", "snooze", "bulk", "snooze filter", "revert", "brand voice"). Each `it` body is a real assertion expected to FAIL now (RED) — import the not-yet-built Server Action / function and assert intended behavior, or `expect(true).toBe(false)` with a `// TODO(04-0X)` marker referencing the plan that will make it green. Do NOT stub passing tests. e2e specs may use `test.fixme()` or a failing assertion so they are visible-but-red until their slice ships. Mark each test with a comment citing its requirement ID and the plan that turns it green (04-02 approvals, 04-03 settings/autonomy, 04-04 sessions/export/purge, 04-05 a11y/mobile/keyboard/perf).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/autonomy.test.ts tests/unit/sessions.test.ts tests/unit/export.test.ts tests/unit/purge.test.ts 2>&1 | grep -qE "fail|FAIL" && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - All scaffold files in `files_modified` exist
    - `tests/unit/autonomy.test.ts` references override one-directionality and contains `describe`
    - `tests/unit/approvals.test.ts` contains the filter strings "snooze", "bulk", "revert"
    - Running the new unit scaffolds reports failures (RED) — they are NOT green stubs
    - `npx playwright test --list` includes the new e2e spec files
    - No watch-mode flags used
  </acceptance_criteria>
  <done>Every Phase 4 requirement has a failing/red automated test scaffold; Nyquist baseline established.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Apply migration 0006 + create user-exports Storage bucket</name>
  <read_first>
    - supabase/migrations/0006_phase4_sessions_exports.sql (the migration to apply)
    - $HOME/.claude/projects memory: supabase-migration-apply-method (MCP lacks project permission — use `supabase db push` over session pooler port 5432; precedent [03-01] 0005)
    - .planning/phases/04-polish-effortless-daily-use/04-RESEARCH.md (## Environment Availability — user-exports bucket "Not yet created")
  </read_first>
  <action>
    Apply migration 0006 to the live database using the project convention: `supabase db push` over the session pooler (port 5432) — do NOT use Supabase MCP `apply_migration` (it lacks project-write permission for this project). Ensure `SUPABASE_ACCESS_TOKEN` or the pooler connection string is set in the environment for non-TTY operation; if the push requires an un-suppressable interactive confirmation, perform it manually.
    Create the private Supabase Storage bucket `user-exports` (service-role access only, not public) — via `supabase` CLI or the Supabase dashboard Storage section. This bucket holds export JSON bundles; downloads are via time-limited signed URL only (24h).
    After push, regenerate/align Drizzle types if the project has a generation step; otherwise confirm the schema-file types still match.
  </action>
  <verify>
    <automated>supabase migration list 2>/dev/null | grep -q "0006" && echo "0006-applied"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list` shows 0006 in BOTH local and remote columns
    - Tables `user_sessions` and `user_exports` exist in the live database with RLS enabled
    - Indexes `idx_approvals_user_pending_stakes` and `idx_workflows_user_status` exist in the live database
    - Private Storage bucket `user-exports` exists (not public)
    - This task is BLOCKING — phase verification cannot pass without it
  </acceptance_criteria>
  <done>Migration 0006 recorded in remote migration history; user-exports bucket created; live schema matches schema files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | DDL applied to production database; RLS must be enabled on every new user-data table before any data is written |
| npm registry → build | new devDependency (`@axe-core/playwright`) crosses the supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01-01 | Information Disclosure | user_sessions table | mitigate | RLS policy `(SELECT auth.uid()) = user_id` enabled in same migration as table creation; refresh_token_hash stored (never plaintext token) |
| T-4-01-02 | Information Disclosure | user_exports table + bucket | mitigate | RLS on table; `user-exports` bucket is PRIVATE (service-role only); downloads via 24h signed URL only — never a public object |
| T-4-01-SC | Tampering | npm install @axe-core/playwright | mitigate | slopcheck unavailable → [ASSUMED]; blocking-human legitimacy checkpoint (Task 1) before install; pinned version 4.11.3 |
| T-4-01-03 | Tampering | migration forward-only | accept | IF NOT EXISTS guards make re-run idempotent; forward-only convention prevents destructive rollback |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `supabase migration list` shows 0006 local + remote
- New unit test scaffolds run RED (Nyquist baseline established)
- `npx playwright test --list` includes mobile-chrome project + new specs
</verification>

<success_criteria>
- user_sessions + user_exports tables live with RLS; perf indexes live
- @axe-core/playwright installed; mobile-chrome Playwright project configured
- Failing test scaffolds exist for all 21 Phase 4 requirement areas
- user-exports private Storage bucket created
</success_criteria>

<output>
Create `.planning/phases/04-polish-effortless-daily-use/04-01-SUMMARY.md` when done
</output>
